# 이어짐 — 아이디어 메모 (누구나 추가, 다음 작업 시 확인)

> 떠오르는 아이디어를 여기에 적어두면, 다음에 누가(구흥모님 쪽 Claude든 협업자님 쪽 Claude든) 작업을
> 시작하기 전에 이 파일을 먼저 확인해서 반영 여부를 판단합니다. **html 파일을 직접 열어서 작업 예약하듯
> 손대지 말고, 아이디어는 이 md 파일에만 적어주세요** — html은 실제 작업할 때만 건드려야 동시 수정으로
> 인한 git 충돌을 피할 수 있습니다.
>
> 작업자는 반영한 항목을 지우지 말고 아래처럼 상태만 바꿔주세요: `[ ]` 미반영 → `[x] (vX.XX 반영, 날짜)`.

## 대기 중

- (없음 — 아래 "반영 완료" 참고, 새 아이디어는 이 섹션에 추가)

## 🔍 적대적 리뷰 2차 대응 — 나머지 항목 전체 triage (2026-09-09, sunny님+Claude 세션)

구흥모 쌤이 push한 `이어짐_적대적리뷰_2026-09-09.md`(전체 13항목) 원문을 읽고, a32에서 이미 처리한
4건을 뺀 **나머지 9건 전부**를 "클라이언트(v4.html)만으로 안전하게 고칠 수 있나"로 갈라 정리했습니다.

### ✅ 이번에 반영 (a33) — 클라이언트만으로 안전, 리뷰 권고대로

- **[x] 치명 2 · 가명화 경로 통일 (부분)** — `requestExtractionV4`가 서버로 원문을 그대로 보내던 것을,
  전송 전 `scrubForServerV4()`로 **형식이 확실한 4종(이메일·주민번호·카드번호·전화번호)만** 기기 안에서
  먼저 마스킹. 리뷰가 우려한 "이름 마스킹이 추출 정확도(성별 추론 등) 떨어뜨림"을 피하려고 **이름은
  일부러 안 지움** — 대신 소득·재산 금액(맨숫자)과 구조적으로 안 겹치는 식별정보만 제거(node 검증:
  150000·1500000·100000000·0 전부 보존, PII만 마스킹). → **이름 자동 가명화는 여전히 정책 결정 대상(아래)**.
- **[x] 🟡 질문 순서(트라우마 배려)** — 건강상태 화면(TPL_HEALTH_A) 맨 위에 "정신질환·폭행 상해 등은
  신뢰 쌓인 뒤 필요한 만큼만, 화면 순서는 질문 순서가 아님" 안내 문구 추가(리뷰가 권고한 즉시 완화책 그대로).

### ⏸ 보류 — 서버/DB/정책 결정 필요(클라이언트만으론 불가). 구흥모 쌤·기관 판단 대상

- **[ ] 치명 1 · 인증 토큰 구조** — STT·ai-draft가 공개 publishable 키를 정적 Bearer로 사용 → 소스만 보면
  누구나 호출(비용 폭탄·오남용). 해결은 **로그인 세션 토큰(access_token)으로 교체 + Edge Function에서
  role=authenticated 검증**인데, 이는 (a) 익명 사용 흐름을 막을지(정책), (b) Edge Function 코드 수정이
  모두 필요 → 클라이언트만으로 못 고침. 임시방편: 함수별 시간당 호출 카운터로 비용 폭탄만 먼저 차단.
- **[ ] 치명 2 나머지 · 실명/지명 자동 가명화** — 상담원이 이름 목록을 넣어야 치환되는데 녹음 직후엔
  목록이 비어 자동 불가. "이름을 언제·어떻게 자동 식별/저장할지"는 기관 개인정보 정책 결정 먼저.
- **[ ] 🟠 조직 접근 권한 구현** — 9/7 "조직도 접근 가능" 정책을 정했으나 RLS는 아직 "본인만". 해결은
  `counselor_profiles.is_admin` 컬럼 + `cases` admin SELECT 정책 추가(DB 작업). 발표 스코프 축소도 대안.
- **[ ] 🟡 암호화 단일키 SPOF** — `decrypt_case_field`가 전역 단일키 + `authenticated` 전체 실행권한 +
  소유권 미검증. 해결은 함수 내부 소유권 검증 또는 `REVOKE EXECUTE ... FROM authenticated`(DB 작업).
- **[ ] 🟡 기관/작성자 이름 위조** — `counselor_profiles.org/display_name`을 본인이 자유 수정 → 위조 문서
  가능. 해결: 가입을 기관 도메인(@homelesshot.or.kr)으로 제한(Supabase Auth 설정) + `org`을 고정값으로.
  ※ `org` 하드코딩은 클라 변경 여지 있으나 profile 표시 로직(구흥모 쌤 영역)과 얽혀 있어 **단독 수정 시
  겹침 위험** → 구흥모 쌤이 profile 쪽과 함께 처리하는 게 안전. 이번엔 손대지 않음.
- **[ ] 🟢 Whisper 환각 필터 화이트리스트** — `stt-transcribe` Edge Function 임계값 조정(서버).
- **[ ] 🟢 Supabase 어드바이저 2건** — 유출 비밀번호 차단 토글 + `touch_counselor_keys` search_path 고정(대시보드/DB).

### 🔸 클라 가능하지만 이번엔 보류(현장 데이터 필요)

- **[ ] 🟡 국적·거주형태 선택지 확충** — `<option>` 추가는 클라만으로 가능하나, 리뷰가 "**실사용 테스트에서
  나온 실제 사례 기준**으로" 추가하라고 명시. 임의로 지어 넣기보다 현장 피드백 모인 뒤 반영이 맞음.
  (추가 시 NATIONALITY_LIST_V4/LIVING_LIST_V4 추출 enum과 판정 매핑 동기화 주의 — 새 항목은 "기타"로 매핑.)

> a33은 `이어짐_v4.html` 내부 로직만으로 완결(Supabase/서버 변경 없음). 보류 항목은 하나도 안 건드렸습니다.
> 서버/DB/정책 항목은 구흥모 쌤이 착수 전 이 메모 "대기 중"에 한 줄 남겨주시면 겹침 없이 협업 가능합니다.

## 🔐 결정됨 — 비밀키(제로지식) → "조직도 접근 가능" 방식으로 (2026-09-07 sunny님 결정)

**배경**: 구흥모님이 설계·구현한 상담원 개인 비밀키 방식(로그인해도 별도 비밀키를 입력해야 상담 내용을
열람 가능, `보안_비밀키_복구_설계.md`) — 기능 자체(설정→저장→로그아웃→재로그인→잠금해제→이어하기,
복구코드 재설정까지 1~7번 전체)는 정상 작동 확인 완료. 그런데 구흥모님 본인이 "로그인만으로도 RLS로
보안이 되는데, 여기에 2차 보안(제로지식)까지 넣은 게 맞는 방향인가?"라는 의문 제기.

**정리한 내용(발표에 쓸 수 있는 트레이드오프 스토리)**:
- 로그인+RLS는 "앱을 통해 접근하는 다른 사람"(다른 상담원 계정, 외부 API 접근 시도)을 막아줌.
- 로그인만으로는 "DB에 직접 접근 가능한 사람"(service_role 키 유출, DB 백업 유출, 조직 관리자 본인)은
  못 막음 — 이건 기존에도 `encrypt_case_field`/`decrypt_case_field`(pgcrypto+Vault, 서버가 키 관리)로
  이미 어느 정도 막혀 있던 부분.
- 이번 제로지식 방식과 기존 서버키 방식의 진짜 차이는 "관리자(조직) 자신으로부터도 데이터를 지켜야
  하는가"임. 제로지식은 관리자도 상담원 비밀키 없이는 절대 못 봄 — 대신 **상담원이 비밀키+복구코드를
  둘 다 잃으면 그 상담 기록은 아무도(관리자도) 영원히 복구 불가**라는 실질적 리스크가 있음.
- 참고로 사유서 자체가 이름·주민번호·주소 등 진짜 식별정보는 애초에 `[___]`로 비워두고 저장 안 하는
  정책이라, DB에 남는 민감정보의 위험도 자체도 같이 고려해볼 지점.
**✅ 결정 (2026-09-07, sunny님)**: 위 질문("조직이 상담 기록에 접근할 수 있어야 하는가?")에 **"예, 조직도 접근해야 함"**으로 결론. → **순수 상담원 개인 제로지식(별도 비밀키 강제)은 기본값에서 제외.** 근거 3가지:
  1. **법적·관리 책임**: 사단법인이 개인정보 처리자(관리 주체)라, 정보주체 열람·정정·삭제 요청 대응, 케이스 연속성(직원 퇴사·부재 시 인계·수퍼비전)을 위해 조직 접근이 필요. (세부는 기관 개인정보 담당자 확인 권장)
  2. **한계효용 낮음**: 지킬 데이터가 이미 가명(실명·주민번호·주소는 `[___]`로 미저장)이라, 제로지식이 "관리자로부터 추가로" 지키는 건 가명화된 상담 메모뿐.
  3. **분실 리스크 큼**: 현장 비전문·고령 직원이 폰으로 사용 → 비밀키+복구코드 동시 분실 시 영구 손실 위험이 실질적.

**적용 방향 (구흥모 쌤 판단·구현 — 급하지 않음)**:
  - **유지**: RLS(로그인 본인만) + **서버키 암호화**(`encrypt_case_field`/`decrypt_case_field`, pgcrypto+Vault) — 조직/서버가 복호화 가능, DB백업·service_role 유출은 여전히 막음, 코치 요구("암호화 저장+담당자만 복호화") 충족.
  - **해제**: 상담원 개인 비밀키(제로지식) 강제 → **로그인만으로 본인 기록 열람**(별도 비밀키 입력 불필요). 즉 saveCaseProgress/resumeCase를 서버키 방식으로 되돌림(v0.48 이전 방식).
  - **(선택, 필수 아님)**: 더 강한 보호가 필요하면 "**기관 위탁키(escrow)**" — 조직이 복구키를 보유해 조직 접근을 유지하면서 추가 암호화. 여력될 때 검토.
  - **제로지식 코드(v0.48)는 삭제하지 말고 비활성/보류** — 정책이 바뀌면 재사용, 그리고 아래 발표 소재로 활용.
  - ⚠️ 이미 제로지식으로 저장된 fake data는 서버키 방식과 호환 안 됨 → 새 저장분부터(본선 fake data라 무방).

- 📣 **발표 소재로 유지**: 이 고민 자체("보안을 강화하려다 데이터 유실 리스크·조직의 법적 접근의무와 충돌 → 조직 접근 가능 쪽으로 결정")를 10/2 발표 "개발 과정에서 어려웠던 점" 파트에 넣으면 좋은 소재(`presentation-prep.md` 6번 섹션 참고).

## 🆕 신규 트랙 — 녹음 → AI 자동 항목 추출 → 사유서 원샷 생성 (2026-09-07 착수)

기존 v3(`이어짐_챗봇UI_시안_v3_모바일.html`)는 그대로 두고, **새 파일 `이어짐_v4.html`**에서 실험 중
(협업자와 같은 파일을 동시에 건드려서 git 충돌 나는 걸 피하려는 목적도 있음 — 이 파일이 안정화되면
그때 v3에 합칠지, 아니면 v4를 계속 별도 트랙으로 둘지 결정 필요).

**내용**: 녹음(또는 파일업로드) 텍스트가 나오면 사람이 "다음"을 누르지 않아도, Gemini가 위기사유·긴급성·
거주지역·거주형태·가구원수·연령대·성별·국적·소득·재산·건강상태를 JSON으로 자동 추출(확실하지 않으면
null) → 단계별로 항목이 전부 확실하면 자동으로 다음 단계로 넘어가고, 하나라도 불확실하면 그 단계에서
멈춰서 어떤 항목을 확인해야 하는지 안내 → 끝까지 자동으로 넘어가면 판정·사유서 생성까지 자동 완료,
숨겨져 있던 "AI 초안(비고)" 기능도 자동 실행. **1차 구현 완료, 아직 실사용 테스트 전 — 프롬프트·멈추는
기준 등은 써보면서 계속 다듬을 예정.**

## ✅ 결정됨 — 앞으로 v3는 업데이트 중단, v4로 통일 (2026-09-07, 구흥모님 확정)

지금 `이어짐_챗봇UI_시안_v3_모바일.html`과 `이어짐_v4.html` 두 파일이 동시에 살아있어서 git 충돌
위험·"뭐가 최신이냐" 헷갈림이 있었던 문제(아래 제안 참고) — 구흥모님이 **오늘부로 v3는 더 이상
건드리지 않고, 앞으로 모든 기능·버그수정은 `이어짐_v4.html` 한 파일에만 반영하기로 확정**했습니다.
v4는 v3보다 기능이 더 앞서 있는 상태(녹음/타이핑 → AI 자동 항목 추출·위저드 자동 진행, 화면 꺼짐
방지, STT 환각 필터 등)이고, 실제 배포 사이트(homelesshot.github.io)도 이미 v4 계열 코드로 보입니다.

**협업자님 Claude 세션께**: 혹시 v3 쪽에서 진행 중인 수정이 있다면 완료되는 대로 v4로 옮겨주시고,
이후 새 작업은 v3가 아니라 v4 기준으로 커밋 부탁드립니다. v3 파일은 삭제하지 않고 참고용으로만
남겨둡니다.

**✅ 답변 (2026-09-07, sunny님 + 협업자 Claude)**: **동의합니다** — v4로 통일 찬성. 단 **"안전하게 이전"** 조건으로:
  1. (참고) v3의 ②·④ 화면분할(v0.53)을 v4에 이식하려고 보니, **구흥모 쌤도 거의 같은 시각에 같은 분할을 v4에 구현("up" 커밋)** — 병렬로 겹쳤습니다. 구흥모 쌤 버전(fillHouseholdFieldsAV4/BV4로 분리)이 origin에 있고, 제가 그 버전을 검증했습니다(수동 흐름 5화면 모두 한 화면·판정·사유서 정상). **제 중복분은 버리고 구흥모 쌤 버전으로 통합.** ⇒ v4는 이제 분할 + AI 자동추출을 모두 가진 상태.
  2. **v4를 현장에서 한 번 실사용 테스트한 뒤 라이브 링크를 v4로 교체** — 현재 담당자가 v3 링크로 실사용 중이라, 테스트 안 된 v4로 바로 갈아타면 위험. 그 전까지 **v3가 현장·라이브 유지**(긴급 버그 외 손대지 않음).
  3. **배포 전환 시 할 일**: GitHub Pages 서빙 파일/담당자 링크를 v4로 변경(지금은 v3). v4 현장 검증 후.
  4. ⚠️ **이번에도 병렬 충돌이 났습니다**(비밀키 때, 이번 v4 분할 때 두 번). 통일만으로는 부족 — **큰 기능은 착수 전에 이 메모 "대기 중"에 "누가 맡음"을 한 줄 적고 시작**하면 겹침을 막을 수 있습니다. 제안드립니다.
  ⇒ **이제부터 새 기능 작업은 v4 기준**으로 하겠습니다.

## 반영 완료

- [x] 상단바(메뉴☰·버전기록📜 버튼을 상단바 바깥으로 빼서 떠있는 버튼으로, 상단바는 "이어짐 프로토타입"
      글자만 남기고 위아래 폭 최소화 / 진행단계 1~7 표시는 막대 배경·숫자 없애고 색깔 점만 남김)
      — v0.49 반영, 2026-09-04 (구흥모님 요청)
