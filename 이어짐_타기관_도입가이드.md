# 이어짐 — 다른 기관 도입 가이드 (오픈소스 확산용)

> 이어짐은 **파일 하나(HTML)로 동작**하고 **전국 공통 법령(긴급복지지원법)** 기반이라, 예산이 부족한 사회복지 기관도 장벽 없이 가져다 쓸 수 있게 설계했습니다. 이 문서는 **다른 기관이 자기 기관용으로 도입할 때 무엇을·어떻게 바꾸는지**를 안내합니다.

---

## 1. 왜 도입이 쉬운가
- **단일 HTML 파일** — 별도 앱 빌드·서버 프레임워크 없이 정적 호스팅에 올리면 끝.
- **전국 공통 기준** — 긴급복지지원법 소득·재산·위기사유 기준이라 지역 종속성이 낮음(지역 기준값만 확인).
- **비로그인 사용 가능** — 도입 초기 시연·현장 테스트를 계정 없이 바로 할 수 있음.

## 2. 도입 절차 (요약)
1. **파일 확보** — 저장소에서 `이어짐_v4.html`(+ `feedback.html`) 내려받기.
2. **Supabase 프로젝트 생성**(리전: **Seoul**) — 무료 플랜으로 시작 가능.
3. **Edge Function 배포** — `stt-transcribe`(음성인식), `ai-draft`(항목추출·초안). 대시보드 **Secrets에 OpenAI API 키** 등록(서버에만 보관).
4. **DB·RLS 설정** — 상담 저장 테이블 + RLS 활성화(본인만 접근), 민감값 암호화(pgcrypto).
5. **화면 연결값 교체** — `이어짐_v4.html` 상단의 `SUPABASE_URL`과 공개(publishable) 키를 본인 프로젝트 값으로.
6. **정적 배포** — GitHub Pages 등에 HTML 업로드 → 링크 배포.

## 3. 우리 기관에 맞게 "바꿔야 할 것" 체크리스트
| 바꿀 항목 | 어디서 | 설명 |
|---|---|---|
| **기관명** | 상담일지 양식·처리방침·화면 소속 | "사단법인 인천내일을여는집" → 우리 기관명(문서 위조 방지 위해 고정값 권장) |
| **상담일지 양식(.hwpx)** | `HWPX_TEMPLATE_B64`(v4.html 내) | 우리 기관/지자체 제출 양식으로 교체(토큰 매핑 유지) |
| **대상자 유형** | `targetType` 옵션 | 우리 기관 대상(예: 가정폭력·시니어·푸드뱅크 이용자 등)으로 조정 |
| **지역 기준** | 판정 관련 상수 | 소득·재산 기준(고시금액)·지역유형(농어촌 등) 확인·갱신 |
| **개인정보 처리방침** | 처리방침 문서·문의처 | 우리 기관 명의·연락처·보유기간으로 교체(개인정보 담당자 검토) |
| **Supabase/OpenAI 키** | 대시보드 Secrets·화면 상단 | 우리 프로젝트 값으로 — *실제 키는 서버에만, 저장소에 넣지 않음* |

## 4. 확산 가능 분야 (예시)
노숙인 아웃리치는 여러 적용처 중 하나입니다. 대상유형·양식·기준만 바꾸면 아래에도 응용 가능:
- **가정폭력 피해자 초기 인터뷰**(긴급 분리·임시주거 판단)
- **시니어 일자리·돌봄 대상자** 초기 상담
- **푸드뱅크·무료급식소** 이용자 위기 스크리닝
- **자활·쪽방 지원** 등 여러 취약계층 **초기 상담·서류화**

## 5. 도입 시 주의
- **개인정보**: 실명·주민번호·주소는 저장하지 않는 설계를 유지하세요(상담일지 `[___]`). 녹음 시 실명 금지 교육 필수.
- **국외 전송 고지**: 음성·텍스트가 OpenAI(국외)로 전송되므로, 처리방침에 국외이전 사실·목적·보유기간을 명확히 고지하세요.
- **API 비용**: STT·AI 호출은 OpenAI 사용량이 발생합니다. 호출 제한·모니터링을 함께 두세요.
- **판정은 참고용**: 예비판단이며 최종은 담당 기관·지자체 심사임을 화면에 항상 표시하세요.

## 6. 함께 보면 좋은 문서
- 사용법·운영: `이어짐_이용자·관리자_가이드.md`
- 개발·구조: `이어짐_발표대본_개발과정.md`, v4.html 내 `VERSION_LOG`
- 보안 점검: `이어짐_적대적리뷰_2026-09-09.md`, `이어짐_적대적리뷰_해결현황_2026-09-10.md`
- 효과 측정 도구: `이어짐_검증기록_템플릿.md`, `feedback.html`

---

*이어짐은 오픈소스 공개를 지향합니다 — 더 많은 현장이 "현장의 말을 실제 지원으로" 잇는 데 쓰이길 바랍니다.*
