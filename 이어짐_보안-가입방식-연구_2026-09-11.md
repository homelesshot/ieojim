---
title: 회원가입 검증 방식 연구 — 도메인 제한 폐기, 초대 전용 방식 검토 (2026-09-11, 기록용 — 아직 미구현)
description: "회원가입을 기관 이메일 도메인으로 제한" 방안을 폐기(소규모 기관은 네이버·지메일을 대표메일로 쓰는 경우가 많아 비현실적)한 뒤, 대신 검토한 방법들을 정리한 연구 메모. 이번엔 텍스트 기록만 하고 구현은 10월 이후로 미룸.
---

# 회원가입 검증 방식 연구 (2026-09-11, 기록용 — 아직 구현 안 함)

## 왜 이 문서가 필요한가

기존에 준비해뒀던 "회원가입을 `@homelesshot.or.kr` 도메인 이메일로만 제한"하는 SQL 방안을 **폐기**합니다.
이유(구흥모님 지적): 작은 기관은 정식 도메인 메일을 쓰는 게 생각보다 큰 비용·수고라서, 대표메일로
네이버·지메일 같은 무료 메일을 쓰는 경우가 흔합니다. "기관 도메인 메일이 있어야 가입 가능"이라는 조건은
우리 기관은 물론, 이 서비스를 나중에 가져다 쓸 다른 소규모 기관에도 비현실적인 전제입니다(오픈소스
평가기준인 "다른 기관이 그대로 가져다 쓸 수 있는가"에도 안 좋은 영향).

해결해야 할 문제는 두 가지입니다.
1. **문서 위조 위험**(적대적 리뷰 원본 지적) — 회원가입이 열려있어서, 아무나 가입해서 기관명·이름을
   자유롭게 입력하면 "공식처럼 보이는" 사유서를 만들어낼 수 있음.
2. **API 호출 한도 우회**(2026-09-11 대화 중 새로 확인) — API 시간당 호출 제한을 "로그인 여부"로
   나눠놨는데(비로그인 20~40회, 로그인 300회/시간), 지금처럼 회원가입이 열려있으면 누구나 계정을
   만들어 "로그인 상태"의 더 넉넉한 한도를 얻을 수 있고, 계정을 여러 개 만들면 한도도 그만큼 늘어남.

두 문제 모두 근본 원인이 같습니다 — **"진짜 우리 직원인지 확인하지 않고 아무나 계정을 만들 수 있다"**는
것. 도메인 제한 없이 이 문제를 풀 방법을 조사했습니다.

## 조사한 방법 2가지

### ① 초대 전용 방식 (권장)

Supabase Auth에는 "새 사용자가 스스로 회원가입하는 것"과 "관리자가 특정 이메일로 초대해서 계정을
만들어주는 것"을 분리하는 기능이 있습니다.

- Supabase 대시보드 → Authentication → Providers/Settings에서 **"Allow new users to sign up"을
  끄면**, 지금 있는 회원가입 화면으로는 더 이상 아무도 가입할 수 없게 됩니다.
- 대신 관리자(구흥모님)가 Supabase 대시보드의 **Authentication → Users → "Invite user"** 메뉴에서
  진짜 상담원의 이메일 주소(네이버·지메일 등 **어떤 메일이든 상관없음** — 도메인 제한이 전혀 필요
  없습니다)를 입력하면, 그 사람에게 초대 메일이 가고, 메일의 링크를 눌러 비밀번호만 정하면 바로
  이어짐을 쓸 수 있게 됩니다.
- 이 방식은 **"그 사람이 실제로 그 메일함의 주인"이라는 것 자체가 검증**이 됩니다(관리자가 실제
  상담원의 진짜 메일 주소로만 초대하니까). 이메일 인증(컨펌메일)을 별도로 켤 필요도 없습니다 — 초대
  자체가 인증 역할을 합니다.
- **장점**: 도메인 제한·별도 메일 서비스 없이 가장 확실하게 "관계없는 사람의 무단 가입"을 막음. 위
  두 문제(문서 위조, 호출 한도 우회) 모두 근본적으로 해결됨(회원가입 자체가 막혀 있으니까). 추가
  비용 없음.
- **단점**: 새 상담원이 들어올 때마다 관리자가 한 번씩 초대해줘야 함 — 다만 이어짐을 쓰는 상담원 수가
  많지 않은 조직 특성상 큰 부담은 아닐 것으로 보임. (오픈소스로 다른 기관이 가져다 쓸 때도 "관리자가
  초대" 방식은 똑같이 그 기관 안에서 문제 없이 재현 가능 — 오히려 "설치설명서만으로 다른 기관이 쓸 수
  있는가"라는 오픈소스 평가기준에도 자연스럽게 맞음.)

### ② 회원가입은 열어두고, 이메일 인증(컨펌메일)만 켜기

- Supabase가 기본 제공하는 내장 메일 발송은 **시간당 딱 2통까지만 무료**입니다(Supabase 공식 문서
  확인, 2024년 이후 기준). 상담원 여러 명이 한꺼번에 가입하면 바로 막혀서 실무적으로 쓰기 어렵습니다.
- 시간당 30명까지 늘리려면 SendGrid·AWS SES 같은 **별도 메일 발송 서비스(커스텀 SMTP)를 새로
  가입·연결**해야 합니다 — 별도 서비스 가입과 약간의 설정, 운영 부담이 추가로 필요합니다.
- 이 방식은 회원가입 자체를 막지는 않으므로, 계정을 여러 개 만드는 것 자체는 여전히 가능합니다(다만
  매번 실제 이메일 인증을 거쳐야 해서 이전보다는 훨씬 번거로워집니다) — 위 문제 ②(호출 한도 우회)를
  완전히 막지는 못하고 문턱만 높입니다.
- **결론**: "쉽게"라는 조건에는 맞지 않음 — 무료로 제대로 쓰려면 결국 커스텀 SMTP 설정이 필요해서
  ①번보다 손이 더 많이 가고, 문제 해결도 더 약함.

## 권장 결론 (10월 이후 구현 과제로 기록)

**①번(초대 전용 방식)을 10월 이후 구현 과제로 채택**하는 것을 권장합니다. 도메인 제한도, 별도 메일
서비스 비용도 없이 두 문제를 가장 확실하게 해결합니다.

구현 시 필요한 작업(지금은 실행하지 않음 — 기록만):
1. Supabase 대시보드 → Authentication 설정에서 "Allow new users to sign up" 끄기.
2. 이어짐 화면의 회원가입 폼을 숨기거나, "관리자에게 문의해 초대를 받아주세요" 안내로 교체(v4.html
   수정 필요).
3. 관리자가 상담원을 초대하는 절차 안내 — 별도 코드 개발 없이 **Supabase 대시보드의 "Invite user"
   메뉴를 바로 쓸 수 있음**(진짜 구현 착수 시 이 대시보드 기능부터 써보는 걸 권장, 그 다음 필요하면
   앱 안에 초대 버튼을 만드는 것도 검토 가능).

**참고로**, 지금(9/11) 적용한 "시간당 호출 횟수 제한"(로그인 300회/비로그인 20~40회, 4번 항목)은
①번을 적용하기 전까지도 여전히 유효한 1차 방어선입니다 — 지금 당장은 회원가입이 열려있어 여러 계정을
만들면 한도가 늘어나는 허점이 있지만, 계정을 여러 개 만드는 것 자체가 익명으로 무제한 호출하는 것보다는
훨씬 번거로우므로 당장 심각한 위험은 아닙니다. ①번은 이 허점을 근본적으로 막는 다음 단계로 남겨둡니다.

## 참고 자료 (WebSearch로 확인, 2026-09-11)

- [Send emails with custom SMTP | Supabase Docs](https://supabase.com/docs/guides/auth/auth-smtp) —
  Supabase 기본 내장 메일: 시간당 2통, 커스텀 SMTP 연결 시 시간당 30명까지.
- [Invite-Only Login in Supabase | Tutorial | RapidDev](https://www.rapidevelopers.com/supabase-tutorial/how-to-allow-login-only-for-invited-users-in-supabase) —
  "Allow new users to sign up" 끄기 + 관리자 `inviteUserByEmail`(또는 대시보드 Invite user)로 계정
  생성하는 초대 전용 패턴 설명.
