---
title: 코칭 후속 정리 — 9/2 코칭 이후 적용 내용 + 적대적 리뷰 해결 현황 + 주요 업데이트 (2026-09-10)
description: 2주차(9/2) 코칭에서 받은 조언별 적용 여부, 9/9 적대적 리뷰 해결/미해결 현황 요약, 9/2 이후 굵직한 업데이트 타임라인을 한 문서에 정리 — 3차(9/10) 코칭용.
---

# 코칭 후속 정리 (2026-09-10) — 3차 코칭 대비

🔗 웹에서 보기(가독성 좋은 페이지, 공유용): https://claude.ai/code/artifact/c15c65bf-5b6b-43c7-b363-ce58ae24a247

## 1. 지난주(9/2) 코칭에서 받은 내용 → 지금까지 적용한 것

(원본 전체는 `coach-meeting-2026-09-02.md` 참고)

| 코치 조언(9/2) | 적용 현황 |
|---|---|
| 스크롤형 랜딩 → 페이지별 이동(위저드) + 완료 시점 명확히 | ✅ 적용됨 — 단계별 화면 전환 구조로 이미 전환, 완료 시 안내(#doneBar) 존재 |
| 판정 배지를 신호등처럼 시각적으로 강조 | ✅ 적용됨 — 결과 화면 색상 배지(v-ok/v-warn/v-bad) 체계로 구현, 이후 9/9 리뷰로 예비판단 면책 문구까지 추가(a32) |
| 가명화 도우미 방향 최종 결정(A: 자동 내장 vs B: 폐지) | ✅ **오늘(9/10) 사실상 결론** — 개인정보보호법 확인 결과 "사전 고지만 되면 실명 언급도 적법"이라, 완전 익명화(B)는 법적 의무가 아님. "적법한 사전 고지(이미 완료, a36) + 구조화된 정보 자동 최소화(이미 완료, a33) + 선택적 가명화 도우미(현행 유지)"로 방향 정리. 자세한 내용은 `adversarial-review-2026-09-09.md`의 "개인정보보호법 근거 확인" 섹션 참고 |
| STT는 모델 교체보다 파라미터 조정이 현실적 | ✅ 1차로 `language:ko` 파라미터 추가(a1) → 실사용 재발 확인 후 결국 whisper-1로 모델 자체 교체(a5 계열, 화자분리는 포기) — "먼저 가볍게 시도 → 안 되면 모델 교체"라는 코치 조언의 순서 그대로 따름 |
| Supabase 서울 리전 + RLS + 암호화(2테이블 분리) | ✅ 완료·재확인됨 — pgcrypto+Vault 서버키 암호화, RLS 전 테이블 적용 |
| Supabase 유료 전환 불필요, AI 코딩툴 구독에 투자 | ✅ 반영 — 유료 전환 안 함(무료 티어 유지) |
| 기기별 버그(PC 다운로드 크래시, 갤럭시 hwpx 생성 실패) | ✅ **해결 완료 확인**(2026-09-10, 정확한 수정 시점은 특정 안 됨 — v4 트랙 다수 수정 중 하나로 해결된 것으로 추정) |

## 2. 적대적 리뷰(9/9) 해결 현황 요약

전체 문항(원본 그대로 보존)과 각 항목별 상세 해결 방법·코드는 `adversarial-review-2026-09-09.md`
전체를 참고하세요. 여기서는 요약만 정리합니다.

**✅ 완전 해결(v4.html 코드 반영·이미 커밋됨)**
- 예비판단 결과 화면 면책 문구 추가(a32)
- hwpx 토큰/따옴표 이스케이프 보완(a32)
- 소득·재산 AI 자동채움 값 사람 확인 강제(a32)
- 5인 이상 가구 소득기준 실제 고시금액 반영, 8인 이상만 추정치로 남음(a32 → a55, 오늘 근본 해결)
- 건강상태 화면 트라우마 배려 안내 문구(a33 → a49)
- 자동추출 경로 부분 가명화(구조화된 개인식별정보 스크럽, a33)
- 개인정보 처리방침 신설 + 국외이전(OpenAI) 고지(a36) — 오늘 확인된 법적 근거로 이게 "치명 2" 항목의
  실질적 해결책이었다는 게 재확인됨
- STT/AI 인증 헤더를 로그인 세션 토큰 우선으로(클라이언트 절반, a54, 오늘)
- 국적·거주형태 선택지 확충(a54, 오늘)
- 녹음결과 화면에 "가명화하기(수동)" 버튼 추가(a55, 오늘)

**⏳ 실행 가능한 SQL/Edge Function 코드는 준비 완료, Supabase 인프라라 아직 미적용**
(전부 `adversarial-review-2026-09-09.md`에 복사·붙여넣기 가능한 형태로 있음)
- STT·AI 호출 시간당 한도(서버 절반) — 카운터 테이블 + Edge Function 코드 준비됨
- 조직 접근 권한(관리자 역할, is_admin + RLS) — SQL 준비됨
- decrypt_case_field 소유권 검증(SPOF 완화) — SQL 준비됨
- 회원가입 기관 도메인 제한 — SQL 준비는 돼 있으나 **지금은 적용 불가(기관 정식 도메인 메일이 아직 없음)**.
  도메인 메일이 생기면 그때 적용 가능
- Whisper 환각 필터 AND→OR 조건 강화 — Edge Function 코드에 포함됨
- `touch_counselor_keys` search_path 고정 — SQL 준비됨

**⏳ 코드로 해결 불가, 대시보드 수동 작업**
- Supabase Auth "Leaked Password Protection" 토글(5분)

## 3. 지난주(9/2) 코칭 이후 굵직한 업데이트 타임라인

- **9/4**: 현장 첫 실사용 데드라인 대응 — 비로그인 저장 소실 방지(localStorage 안전망), STT 언어 혼입
  1차 완화, 모바일 글씨 확대, 녹음 결과 화면 재설계, 개인정보 비밀키(제로지식 암호화) 클라이언트 구현
- **9/7**: AI 공급자 전환(Gemini → OpenAI), STT 모델 확정(whisper-1, 화자분리 포기), 소득·재산 자동추출에
  OpenAI Structured Outputs 적용(필드 누락 방지), hwpx 사유서 양식을 협업자가 한글에서 완성한 새 버전으로
  전면 교체, **보안 철학 재검토** — 제로지식 암호화를 완성했다가 "조직 접근·법적 책임" 이유로 서버키
  암호화(조직 복호화 가능)로 되돌림, v3/v4 파일을 v4로 통일
- **9/8**: 현장 실사용 피드백 수렴(재상담 시 질문 축소 요청, 이름 저장 요청 — 후자는 본선 기간 보류로
  확정) → 재상담 테스트 파일 1차 구현
- **9/9**: 적대적 리뷰 실시 및 1·2차 대응(a32, a33) — 예비판단 면책 문구, hwpx 이스케이프, 소득·재산
  확인 강제, 5인 이상 배지, 트라우마 배려 안내, 자동추출 부분 가명화. 개인정보 처리방침 신설(a36).
  제출일 9/17 확정, 발표일 9/30 확정
- **9/10(오늘)**: "검정 그라데이션" 화면 버그 원인 규명·수정(숨겨진 모달 7개의 그림자 방향 문제, a53),
  적대적 리뷰 3차 대응(a54) — STT/AI 인증 헤더 개선, 국적·거주형태 선택지 확충, 개인정보보호법 근거
  확인으로 가명화 도우미 방향 사실상 정리. 이어서 "4인이 최대치"라는 기존 전제가 틀렸음을 확인 —
  보건복지부 기준중위소득 고시가 실제로는 7인까지 공표된다는 걸 찾아 5·6·7인 소득기준을 실제 고시
  금액으로 교체하고(8인 이상만 추정치로 남김), 녹음결과 화면에 "가명화하기(수동)" 버튼 추가(a55)

## 4. 실사용 테스트 피드백 — 수치화된 검증이 아니라는 한계

지금 가진 실사용 피드백은 9/8 현장에서 상담원 한 분에게 구두로 받은 의견 3가지가 전부입니다.
"몇 명이 써봤고 만족도가 몇 점이었다" 같은 정량적인 수치는 없고, 아래 원문 그대로의 정성적 피드백과
그에 대한 실현 가능성 판단만 있는 상태입니다.

**받은 피드백 원문(2026-09-08)**

1. **초기상담 vs 재상담 구분 없음** — 처음 만나는 사람 기준으로는 적절하지만, 이미 상담한 적 있는
   사람을 재상담할 때는 불필요한 질문이 많음.
2. **이름 입력 → 재상담 시 이전 기록 바로 확인** — 이름을 넣으면 재상담할 때 이전 기록을 바로
   찾아볼 수 있으면 좋겠다는 요청.
3. **입력 속도는 문제 없음** — 대화하면서 손으로 눌러 입력했는데, 입력 자체는 충분히 빨랐다(긍정
   피드백, 현재 UI 흐름/속도에 대한 검증).

**항목별 실현 가능성 판단**

- **1번(재상담 질문 축소)**: 기술적으로는 본선 기간 내 가능 — "초기상담/재상담" 선택 후 재상담이면
  일부 단계를 건너뛰거나 "이전과 동일" 확인만으로 넘어가는 방식. 실제로 `이어짐_v4_재상담테스트.html`
  (버전기록 a31, 로컬 테스트 전용 파일)로 1차 구현까지 완료 — 브라우저(기기)에 저장된 마지막 값을
  자동으로 미리 채워주는 방식이라 이름 기반 조회 정책과 무관하게 바로 시험해볼 수 있음. 다만 한 기기로
  여러 사람을 연달아 상담하면 "마지막 값"이 다른 사람 것일 수 있어, 실제로 써보면서 이게 실용적인지
  확인이 더 필요함.
- **2번(이름으로 이전 상담 찾기)**: 개인정보 정책(이름 암호화 저장 + 조직 검색 기능)이 걸려 있어
  본선 기간(~9/17)엔 보류로 확정. 발표에서는 "이 요청이 나왔다는 것 자체가 현장 검증의 결과"로 쓰고,
  정책 결정은 본선 이후 기관 내부 논의로 넘김.
- **3번(입력 속도)**: 추가 조치 불필요 — 지금의 위저드형 UI 방향이 실사용에서 검증됐다는 긍정 신호로
  발표 자료에 그대로 사용 가능.
