---
title: 이어짐 프로젝트 회고 — 처음 의도, 진행 과정, 변곡점 정리 (발표 준비용)
description: <AI 챌린지 해> 10/2 본선 발표 준비를 위해, 프로젝트를 시작한 최초 의도부터 지금까지의 진행 과정·주요 변곡점·코칭을 통한 변화·목적이 변질됐는지 여부를 정리한 회고 메모. 중간 과정의 구체적 시행착오(버그·회귀·재작업 사례) 포함.
---

# 이어짐 프로젝트 회고 — 처음 의도부터 지금까지 (2026-09-07 작성, 구흥모님 사실관계 정정 반영, 시행착오 보강)

이 문서는 `presentation-prep.md`(발표 슬라이드/Q&A 준비용)를 보완하는 성격의 문서입니다. 발표에서 "개발
과정" 파트를 이야기할 때, 단순 기능 나열이 아니라 "왜 시작했고, 그 의도가 지금까지 어떻게 이어졌는가"를
스토리로 말할 수 있도록 타임라인과 변곡점을 정리했습니다. **처음 버전이 최근 며칠(9/7)에 치우쳐 있다는
지적을 반영해, 1~3주차 사이 실제로 있었던 크고 작은 시행착오(버그·회귀·재작업)를 훨씬 촘촘하게 채웠습니다.**

**⚠️ 정정 이력**: 최초 버전에서 "코칭을 받기 전에 팀이 스스로 프로토타입을 만들었다"는 부분을 마치
프로그램과 무관한 독자적 시작인 것처럼 서술했으나, 이는 사실과 다릅니다. **예선에 제출한 프로토타입으로
본선에 진출했고, 본선에서 코칭을 받으며 그 프로토타입을 발전시켜온 것**이 정확한 구조입니다 — 이는
챌린지의 정상적인 진행 절차이지, 코칭과 무관하게 벌어진 예외적 사건이 아닙니다. 또한 "hwpx 문서를
템플릿 치환 방식으로 만든 것"은 팀이 시행착오 끝에 스스로 깨달은 것이 아니라, **1주차(8/28) 코칭에서
코치가 명시적으로 제안한 방법**이었습니다(코치 미팅 메모에 "원래 상담일지 양식(HWPX)에 각 항목이 들어갈
부분에 코드변수 입력하여 만들기. 기존 양식을 그대로 사용 가능"이라고 명시). 아래 내용은 이 두 가지를
바로잡아 다시 정리한 것입니다.

## 1. 예선 → 본선 — 어떻게 시작됐나

**예선 제출**: 이어짐은 예선에 제출한 프로토타입(`이어짐_prototype_artifact.html`, 최초 제목 "이어짐 —
위기가구 긴급복지 예비판단 도우미")으로 본선에 진출했습니다. 이 프로토타입에는 이미 핵심 뼈대 3단계
(①위기가구 정보 입력 → ②예비판단 결과 → ③사유서 초안 자동 생성)와, 개인정보를 가리는 "가명화 도우미",
그리고 AI(당시 Google Gemini 무료 API)가 사유서 초안을 작성해주는 기능까지 포함되어 있었습니다. 비슷한
시기에 "도입 전/후 효과를 숫자로 비교하겠다"는 목적의 baseline 측정 도구(`이어짐_1주차_baseline측정.html`
— 상담 1건 처리시간·공적 지원 연계율·체감 업무 부담 기록용)도 함께 준비했습니다.

**본선 코칭**: 본선 진출 이후, 이 예선 프로토타입을 실제 현장에서 신뢰할 수 있는 제품 수준으로
끌어올리는 것이 본선 코칭의 목표였습니다. 1주차(8/28) 코칭에서 보안 구조, 실제 문서 양식으로의 전환,
UI 정리 등 구체적인 방향을 제시받았고(2번 항목 참고), 이후 진행 과정은 이 조언을 실제로 구현해나가는
과정이었습니다.

## 2. 핵심 목적 — 무엇을 위해 시작했나 (지금까지 변하지 않은 한 문장)

**출발점 문제의식**: 아웃리치 상담원이 거리 노숙인·쪽방주민 등 위기가구를 현장에서 만났을 때, 긴급복지지원법상
예비판단(소득·재산 기준 충족 여부)과 지원요청 사유서 작성을 그 자리에서 정확하고 빠르게 해내기 어렵다는
현장 실무 경험이 출발점이었습니다. 기존 방식은 상담원이 수기로 정보를 정리해서 사무실에 돌아와 문서를
다시 작성해야 했고, 판단 기준(소득·재산 한도 등)을 매번 정확히 적용하기도 쉽지 않았습니다.

**최초 의도(변하지 않은 핵심 목적)**: "상담원이 현장에서 음성으로 말하거나 텍스트로 입력하면, AI가
실시간으로 긴급복지지원 자격을 예비판단하고, 그 판단 근거를 담은 지원요청 사유서 초안을 자동 생성해준다."
이 한 문장은 예선 프로토타입부터 지금(a30, 9/7)까지 한 번도 바뀐 적이 없습니다 — 아래에서 보듯 "어떻게"
구현할지는 코칭·실사용·협업을 거치며 수없이 바뀌었지만, "왜/무엇을 위해"는 시종일관 동일했습니다.

## 3. 진행 과정 타임라인 (버전 단위로 세분화)

### 1주차 (~8/28) — 1차 코칭: 예선 프로토타입을 발전시킬 방향 제시

- 코치 조언 3영역: ① **보안**(서울 리전 Supabase, RLS, 민감정보 암호화, LLM 입력 해시토큰화, 유료 API 사용
  — "본선은 안전한 구조를 fake data로 완성도 있게 보여주는 것"), ② **기능**(녹음+STT, 실제 서류 양식,
  UI를 메인 1창+사이드바로 정리), ③ **협업 방식**(기능명세서로 분담, QA 스프레드시트).
- 이 중 "실제 서류 양식으로" 항목의 구체적 방법으로, 코치가 "원래 상담일지 양식(hwpx)에 각 항목이 들어갈
  자리에 코드변수를 넣는 방식 — 기존 양식을 그대로 활용 가능"이라고 명시적으로 제안했습니다. 이는 이후
  ①번 변곡점(4번 항목)의 출발점이 됩니다.
- 이 시점부터 "코드가 다 알아서 하게 만들기"보다 "실제로 쓰일 인프라·양식·보안 기준에 맞춰가기"로 방향이
  구체화됨. Supabase 서울 프로젝트 생성, RLS ON, 메인 테이블(가명화된 값)과 실명 등 민감정보를 담는
  별도 테이블을 분리해 pgcrypto+Vault로 암호화하는 구조를 이 주에 실제로 구현.

### 2주차 (~9/2) — 2차 코칭 이전: 핵심기능 완성 + 첫 시행착오들

이 구간(1주차 중후반~2주차 코칭 직전)에 실제로 구현하면서 겪은 시행착오가 여러 건 있었습니다.

- **화자분리(diarization) 모델의 트레이드오프**: STT를 처음엔 OpenAI `gpt-4o-transcribe-diarize`(화자분리
  지원)로 결정했으나, "일단 이걸로 시작해보고 안 되면 바꾸자"는 전제로 2주차 코칭에 들어갔고, 실제로는
  3주차(9/4)에 언어 강제가 안 되는 문제로 결국 `whisper-1`로 교체하며 화자분리 기능 자체를 당분간
  포기하게 됩니다(아래 참고).
- **로그인/저장 관련 버그 다수**: 상담원 이름이 저장 후 새로고침하면 사라지는 문제(원인: `counselor_profiles`
  프로필 row가 없는 계정에서 `update()`가 0행에 적용되어 조용히 실패 — `upsert()`로 교체해 해결), "새 상담
  시작" 시 대상유형 선택 칩과 스킵 버튼이 사라지는 문제(웰컴 화면 재생성 시 마크업 누락), 상담 완료 후 다른
  상담을 봤다가 돌아오면 완료 버튼이 사라지는 문제(`#doneBar`가 상태 복원 영역 바깥에 있어 복원 안 됨) 등,
  로그인·자동저장·이어하기 기능을 실제로 완성해가는 과정에서 크고 작은 회귀가 반복적으로 발생하고
  수정됨.
- **가명화 도우미에 대한 첫 의문**: 이 시점까지는 "가명화 → AI 처리 → 사유서 작성"이 당연한 흐름이라고
  생각했지만, 2주차 코칭에서 이 전제 자체에 의문이 제기됩니다(바로 아래 참고).

### 2주차 (9/2) — 2차 코칭: 체크포인트 통과 + 방향 재검토

- 보안 인프라(RLS·암호화·Vault), STT 실연결(OpenAI), 로그인/자동저장/이어하기 구현 완료 — 코치가
  "이미 잘 되어 있다"고 확인.
- **UI 방향 전환**: 길게 스크롤하는 랜딩페이지 방식은 중요한 정보(예비판단 결과)를 놓치기 쉽다는 지적 →
  책장 넘기듯 한 화면에 한 단계씩 보이는 방식으로 전환 권고, 완료 시점을 명확히 알리는 UX 필요성도 지적됨.
- **가명화 도우미 방향에 대한 첫 의문 제기**: "산출물(사유서)엔 어차피 실명이 들어가야 하는데, 왜
  가명화가 필요한가?"라는 코치의 질문 — (A) 세션에 자동 내장 vs (B) 기능 폐지 중 택1 숙제로 남음. 이
  질문은 이후 9/7 "비밀키(제로지식) → 조직 접근 가능 방식"이라는 더 큰 보안 철학 재검토로 이어지는
  씨앗이 됩니다(5번 항목 참고).
- STT 정확도 개선은 "모델 교체/파인튜닝"이 아니라 "API 파라미터 조정(언어 고정, 저신뢰 구간 제거)"이
  가장 현실적이라는 실용적 조언 — 코치는 모델 파인튜닝을 "사실상 비현실적"이라고 명확히 선을 그음.
- 기타 발견된 버그: PC에서 사유서 추출 시 프로그램이 다운되는 문제, 갤럭시(안드로이드)에서 사유서 생성이
  안 되는 문제(아이폰에서는 정상) — 기기별 호환성 이슈로 이 시점엔 미해결 상태로 남음.
- 인프라 비용 조언: Supabase 유료 전환 불필요(무료 티어 단점은 "7일 무접속 시 자동 일시정지"뿐, 데이터
  삭제는 아님), 오히려 AI 코딩 도구 요금제에 투자하라는 권고.

### 3주차 (9/2~9/4) — 사유서 양식 전면 재작업 + 협업 중 발견된 버그들

- **hwpx 사유서 양식 개편**: 협업자가 승인한 새 디자인 시안에 맞춰 섹션 제목 문구 변경("생계·재산 곤란"→
  "생계·재산 상황", "5. 근거"→"5. 법적 근거"), 결과 요약 줄 추가, 기관명 부제·작성일/작성자 분리, 개인정보
  처리 안내 문구 추가, 기본정보 표를 11행×2열 → 6행×4열로 재구성 — `buildHwpxTokens()`와 `.txt`/`.rtf`
  내보내기 로직을 모두 갱신해야 하는 손이 많이 가는 작업이었음.
- **중요 버그(v0.34) — hwpx 다운로드 먹통**: 상담을 완료·저장했다가 다른 상담 화면에 갔다 돌아오면 한글
  다운로드 버튼이 "먼저 예비판단 실행을 눌러주세요" 오류를 내며 작동하지 않는 문제 발견. 원인은 판단
  결과의 구조화 데이터를 담은 전역변수 `lastRunData`가 상담 일시정지·재개(`snapshotCaseState`/
  `restoreCaseState`) 저장·복원 대상에서 누락되어 있었던 것 — 두 함수에 포함시켜 해결. 이후에도 한 번 더
  "안 눌린다"는 재보고가 있었으나 이건 수정 이전 캐시된 데이터 문제로 확인됨.
- **카카오톡 인앱브라우저 파일 저장 불가(v0.35)**: 안드로이드에서 카카오톡 링크로 접속하면 blob 파일
  저장이 구조적으로 막혀 있다는 걸 카카오 공식 문서로 확인 — User-Agent 감지 후 "다른 브라우저로 열기"
  안내 문구를 접속 직후와 저장 시도 시 모두 노출하도록 대응(구조적으로 완전히 해결할 수 없어 안내로
  우회한 사례).
- **협업 중 파일 분기 실험과 버그 발견**: 협업자가 단계형(위저드) UI 스킨을 별도 파일(`이어짐_단계형_통합본.html`)
  로 실험하던 중, 사이드바 "이어하기"로 진행중/완료 상담을 다시 열면 화면이 완전히 비어버리는 치명적
  버그를 발견(원인: 위저드 스킨은 `botCard()`를 통해 카드가 만들어질 때만 화면에 표시되는데,
  `restoreCaseState()`는 `chat.innerHTML`을 통째로 교체하는 방식이라 `botCard()`를 거치지 않음) — 이
  버그를 고쳐두긴 했지만, 이 파일 자체는 "당분간 보류, 메인 작업 파일은 기존 파일로 계속 간다"는 결정이
  내려지며 일단 미사용 상태로 남음(이후 v4 트랙으로 다시 이어짐 — 4번 항목의 파일 분기 스토리 참고).
- **hwpx 템플릿 교체(v0.36)**: 협업자가 한글 프로그램에서 직접 완성한 사유서 양식(기본정보 4열 표,
  섹션 2열 표, 원문자 ①~⑥, 작성일/작성자란)을 받아 그 파일의 샘플 값을 `{{토큰}}`으로 역변환해서
  템플릿을 통째로 교체. 기존 `buildHwpxTokens()` 로직은 변경 없이 그대로 재사용 가능함을 확인 — 이
  시점에서 "hwpx 비주얼 스타일(문단번호·정렬 어긋남)" 문제가 사실상 해결됨(4번 항목 참고).

### 3주차 (9/4) — 현장 실사용 데드라인 대응: 하루 동안 v0.41~v0.48까지 8단계 배포

9/4는 실제 현장 상담원이 처음 사용해보는 날이었고, 오전엔 "협업자 작업 중이니 건드리지 말자"는
방침이었지만 오후에 현장 사용 데드라인 때문에 예외적으로 실시간 대응에 들어갔습니다. **이 하루 동안의
연쇄적인 버그 발견→수정 과정이 발표에서 "현장 변화 대응"을 보여줄 수 있는 가장 구체적인 사례**입니다.

- **v0.41 — 비로그인 시 입력 소실**: 서버 자동저장(`saveCaseProgress`)이 로그인 상태에서만 동작했는데,
  현장에서는 대부분 로그인 없이 빠르게 쓰고 있었던 것으로 확인 — 녹음→변환 텍스트가 새로고침·브라우저
  재시작 시 통째로 사라지는 사고가 실제 발생(9/3 사고 신고). 로그인 여부와 무관하게 항상 동작하는
  `localStorage` 임시저장을 급히 추가.
- **v0.42 — 실사용 중 확인된 4가지 문제 동시 대응**: 녹음/파일업로드 후 확인 없이 바로 다음으로 넘어가던
  문제(1차 수정, 나중에 v0.43에서 재설계), STT 한국어/영어 혼입(`language:ko` 파라미터 추가로 1차 완화),
  모바일 글씨 크기 확대("가장 작은 라벨류는 2배, 본문류는 약 1.3배"라는 규칙 도입), 가로 스크롤 발생
  문제(전역 `box-sizing:border-box` 리셋).
- **v0.42 규칙이 만든 회귀 — 3연속**: "가장 작은 글씨 2배" 규칙을 기계적으로 적용한 여파로 이후 최소
  세 곳에서 레이아웃이 깨지는 회귀가 발생했습니다. (1) v0.44에서 사이드바 맨 아래 1회성 안내문이
  커진 글씨로 "최근" 목록을 가리는 문제, (2) v0.46에서 입력창 위 "대상자 유형을 선택하세요" 힌트가
  커진 글씨 때문에 좁은 화면에서 드롭다운·타이머와 같은 줄에 못 들어가고 밀려나는 문제, (3) 같은
  v0.46에서 첫 화면(웰컴) 제목·설명·유형칩 글씨가 커지면서 상단 여백과 겹쳐 화면 아래로 밀려나 스크롤
  해야 보이는 문제. 세 번 다 "이상한 부분 보이면 알려달라"는 안내를 반복하며 그때그때 화면 크기별
  예외 처리를 추가하는 방식으로 대응 — 하나의 CSS 규칙 변경이 여러 화면에 파급되는 걸 실사용 중에야
  발견하게 된 전형적 사례.
- **v0.43 — 녹음 결과 화면 재설계**: 카드를 하나만 만들고 상태(녹음중→변환중→결과/빈결과/오류)에 따라
  내용만 갱신하는 방식으로 재설계, "이전" 버튼이 "재녹음하기"로 동작하도록 변경. 안내문 접기(물음표
  버튼) 컴포넌트를 이때 처음 도입.
- **v0.45 — STT 모델 자체를 교체(근본 해결)**: v0.42의 `language:"ko"` 파라미터 추가에도 불구하고 영어
  혼입이 실사용에서 계속 재발 — 원인을 조사해보니 OpenAI 공식 문서와 달리 실제로는(OpenAI 커뮤니티·
  Microsoft Q&A 다수 보고) `gpt-4o-transcribe`/`gpt-4o-transcribe-diarize` 계열에서 `language`가 "강제"가
  아니라 "힌트"로만 취급되는 알려진 문제였음. 반면 구모델 `whisper-1`은 디코더 단계에서 언어가 실제로
  강제됨을 확인하고 교체 — **트레이드오프로 화자분리(diarization) 기능은 이 시점에 포기**(화면은
  화자분리 데이터가 없으면 해당 영역을 자동으로 숨기도록 이미 처리되어 있어 깨지지는 않음). 배포 직후
  "모델을 허용해줬는데도 오류"라는 재보고가 있었으나, 이는 OpenAI 프로젝트의 모델 허용 목록 반영
  지연 때문이었고 실제 코드/설정 문제는 아니었던 것으로 확인.
- **v0.46 — 위 회귀 두 건 수정** (앞서 설명).
- **v0.47 — 재녹음 중 표시가 너무 약함**: 단계형(위저드) 화면에서는 하단 입력창(마이크·타이머 포함)이
  숨겨져 있어, 녹음 결과 카드의 작은 빨간 글씨 하나("🔴 녹음 중…")만으로는 재녹음 시 실제로 녹음 중인지
  확인하기 어렵다는 재신고 — 빨간 배경 박스+펄스 애니메이션+카드 내 초단위 타이머로 교체, 재녹음 버튼을
  누르면 해당 카드로 자동 스크롤하도록 개선.
- **v0.48 — 개인정보 비밀키(제로지식 암호화) 클라이언트 구현**: 협업자가 저녁에 올린 설계 문서
  (`보안_비밀키_복구_설계.md`)를 바탕으로, PBKDF2(200,000회)로 비밀키/복구코드에서 래핑키를 유도하고
  AES-GCM 256으로 상담원별 DEK를 암호화해 서버에는 암호문만 저장하는 방식을 그날 밤 구현·배포. 서버
  테이블(`counselor_keys`) 생성은 역할 분담상 담당자 몫으로 남아, 클라이언트 코드는 먼저 배포됐지만
  테이블이 없는 동안은 "서버 설정이 끝나면 사용할 수 있어요(준비 중)"로 비활성 표시되도록 안전하게
  분기 처리 — 기존 방식(서버측 암호화 RPC)은 계속 정상 동작해 이 기능 때문에 실사용이 막히는 일은
  없도록 함.

### 4주차 (9/7) — 보안 철학 재검토 + 파일 통합 + AI 공급자 전환 + 세부 정합성 버그 수정

- v0.48로 완성한 제로지식 암호화 기능은 실제로 작동을 확인했지만, 구흥모님 스스로 "로그인만으로도 RLS
  보안이 되는데 2차 보안까지 필요한가"라는 의문을 제기하며 방향을 재검토(5번 항목 참고).
- v3/v4 파일 통일 결정(4번 항목 참고), AI 모델 전환(Gemini→OpenAI, 6번 항목 참고), 소득·재산 자동추출
  신뢰성 강화(Structured Outputs), hwpx 양식을 협업자가 한글에서 완성한 새 버전으로 전면 교체(a27),
  소득·재산 "미입력"과 "0원"을 구분하지 않아 잘못된 판정이 나올 수 있던 문제 발견·수정, 소속·작성자·메모
  등 세부 정합성 버그 다수 수정, 코드에 남아있던 오래된 Gemini 관련 문구·죽은 코드 정리.

## 4. 주요 변곡점 6가지

### ① hwpx(한글 문서) 생성 방식 — 코치의 제안을 실제로 구현하며 한 걸음 더 나아간 과정

앞서 말씀드린 대로, "기존 양식에 값만 채워 넣는 방식"은 1주차 코칭(8/28)에서 코치가 직접 제안한
방향이었습니다. 팀은 이 조언을 실제로 구현하는 과정에서, 처음엔 문서 내부의 표·문단 구조 자체를 코드로
만들면서 값을 채우려 했는데, 실제 한글 프로그램에서 열면 문단번호·정렬이 계속 어긋나는 문제가 반복됐습니다.
그래서 한 걸음 더 나아가, **표·문단 구조까지 전부 팀원이 실제 한글 프로그램에서 완성한 파일을 그대로
쓰고, 그 안의 텍스트 값만 `{{토큰}}` 치환으로 갈아 끼우는 방식**으로 정착시켰습니다(v0.36, 이후 9/7에
한 번 더 새 양식으로 교체). 이 전환 과정에서 사유서 양식 자체도 손이 많이 가는 재작업(섹션 재구성,
기본정보 표 구조 변경 등, 3번 항목 3주차 참고)을 거쳤고, hwpx 다운로드가 갑자기 먹통이 되는 버그
(`lastRunData` 누락, v0.34)도 이 과정에서 발견·수정됐습니다. 즉 코치가 제시한 "기존 양식 재사용"
원칙을, 구조 생성까지 사람에게 완전히 맡기는 형태로 더 철저하게 적용한 것입니다 — "사람이 잘하는
도구(문서 서식)는 사람이 만들고, AI는 그 위에서 값을 채우는 자동화만 담당한다"는 실전 교훈으로 발표에
쓸 수 있습니다.

### ② 음성인식(STT) — 파라미터 조정으로 안 되자 모델 자체를 교체, 화자분리 기능은 포기

처음엔 화자분리(diarization)를 지원하는 `gpt-4o-transcribe-diarize`로 시작했고, 2주차 코칭에서도
"모델 교체보다 파라미터 조정이 현실적"이라는 조언에 따라 `language:"ko"` 파라미터만 추가하는 선에서
1차 대응(v0.42)했습니다. 하지만 9/4 실사용에서 영어 혼입이 계속 재발했고, 조사 결과 이 모델 계열은
`language`가 "힌트"로만 취급되는 알려진 한계가 있다는 걸 확인했습니다. 결국 화자분리라는 기능 하나를
포기하는 대가를 치르고 언어가 실제로 강제되는 구모델 `whisper-1`로 교체(v0.45)해서 문제를 근본적으로
해결했습니다 — 처음 조언대로 가볍게 고치는 시도를 먼저 해보고, 그게 현장에서 안 통한다는 게 확인된
뒤에야 더 큰 대가(기능 하나를 포기)를 치르는 결정을 내린 사례입니다.

### ③ UI 세부 조정 — 하나의 규칙 변경이 여러 화면을 깨뜨린 반복적 회귀

9/4 실사용 대응 중 "글씨가 너무 작다"는 피드백에 "가장 작은 라벨류는 2배로 키운다"는 규칙(v0.42)을
적용했는데, 이 규칙이 이후 최소 세 차례(사이드바 안내문 v0.44, 입력창 유형힌트·첫 화면 v0.46) 다른
화면에서 예상치 못한 레이아웃 붕괴를 일으켰습니다. 매번 "이상한 부분 있으면 알려달라"고 안내하며
발견되는 대로 화면 크기별 예외 처리를 추가하는 식으로 대응했습니다 — 한 화면짜리 앱이 아니라 여러
화면이 CSS를 공유하는 구조에서 전역 규칙 하나를 바꾸는 게 얼마나 조심스러워야 하는지 실사용을 통해
체감한 사례입니다.

### ④ 파일 구조 — 위저드 UI 실험 → 보류 → v4로 재점화 → v3/v4 분기와 재통합

3주차(9/2~9/4)에 협업자가 단계형(위저드) UI를 별도 파일(`이어짐_단계형_통합본.html`)로 먼저 실험했고,
이 과정에서 "이어하기 시 화면이 완전히 비는" 치명적 버그를 발견·수정했지만, 실사용 데드라인 압박 속에
"당분간 보류, 메인 파일은 기존 파일로 유지"라는 결정이 내려지며 이 파일은 잠시 미사용 상태로 남았습니다.
이후 9/7에 협업자와 같은 메인 파일을 동시에 수정하면서 git 충돌이 반복되자, 새 실험 트랙(`이어짐_v4.html`)
을 다시 분기했습니다(녹음→AI 자동 항목 추출→위저드 자동 진행 기능을 여기서 먼저 실험). v4가 v3보다
기능적으로 앞서게 되자, 같은 날 "v3는 동결하고 v4로 통일"하는 결정이 내려졌습니다. 다만 실제 배포
링크는 "v4를 현장에서 한 번 실사용 테스트한 뒤" 전환하기로 해서, 안전장치를 두고 단계적으로 전환하는
방식을 택했습니다. 통일 과정에서도 두 세션이 거의 동시에 같은 기능(가구·건강 화면 분할)을 각자
구현하는 중복이 또 발생해, "큰 기능은 착수 전에 공유 메모에 담당자를 적어두자"는 협업 규칙이 새로
생겼습니다.

### ⑤ 보안 철학 — "상담원 개인 비밀키(제로지식)"를 완성했다가 "조직도 접근 가능한 서버키 암호화"로 되돌림

가장 의미 있는 정책적 변곡점입니다. 9/4 밤에 상담원 개인만 열람 가능한 제로지식 암호화(비밀키+복구코드
방식)를 실제로 완성했지만(v0.48, 설정→저장→로그아웃→재로그인→잠금해제→이어하기까지 정상 동작 확인),
9/7 구흥모님 본인이 "로그인만으로도 RLS 보안이 되는데 2차 보안까지 필요한가"라는 의문을 제기했고,
다음 근거로 방향을 바꿨습니다:
- 사단법인은 개인정보 처리자로서 열람·정정·삭제 요청 대응, 직원 퇴사·부재 시 케이스 인계를 위해 조직
  접근이 필요함(법적·관리 책임).
- 이름·주민번호·주소 등 진짜 식별정보는 애초에 저장하지 않는 정책이라, 제로지식이 추가로 지키는 범위가
  가명화된 메모 정도로 한계효용이 낮음.
- 현장 비전문·고령 직원이 스마트폰으로 씀 — 비밀키+복구코드를 동시에 잃으면 영구 손실되는 리스크가 실질적임.

결과적으로 "로그인(RLS) + 서버키 암호화(조직이 복호화 가능)"로 되돌아갔고, 제로지식 코드는 삭제하지
않고 보류 상태로 남겨뒀습니다. 완성까지 갔다가 정책적으로 되돌린 결정이라, **단순 버그 수정이 아니라
"완성한 기능을 스스로 재검토해서 되돌린" 드문 사례**이기도 합니다. **이 고민 자체("보안을 강화하려다
데이터 유실 리스크·조직의 법적 접근 의무와 충돌")가 발표 "어려웠던 점" 파트의 좋은 소재로 이미 지목되어
있습니다.**

### ⑥ AI 공급자 — Gemini에서 OpenAI로, 그리고 신뢰성 강화

AI 초안(비고 자동작성) 기능이 예선 프로토타입 때부터 Google Gemini(무료 API)를 호출하고 있었는데,
이후 API 키를 서버(Supabase Edge Function)로 옮겨 안전하게 감추는 과정을 거쳤고(v0.8), 9/7에는
프라이버시·키 관리를 STT에 이미 쓰던 OpenAI 계정으로 통합하기 위해 OpenAI(gpt-4o-mini)로 전환했습니다.
전환 직후 "재산·부채 필드가 자동으로 안 채워지는" 문제가 발생했는데, 원인은 Gemini가 "JSON만 출력하라"는
지시를 비교적 잘 따르던 것과 달리 gpt-4o-mini는 일반 텍스트 모드에서 형식을 놓치는 경우가 있었기
때문이었습니다. 이를 계기로 단순 JSON 모드 → **OpenAI Structured Outputs(JSON Schema strict 모드)**까지
단계적으로 신뢰성을 강화했습니다 — 단순히 "빠르게 고치기"가 아니라 "왜 안전한지"를 조사해서 근본적으로
해결한 사례입니다.

### (참고) UI 흐름 — 긴 스크롤 방식에서 위저드(단계별 화면 전환) 방식으로

예선 프로토타입은 한 화면에 0→1→2→3단계가 모두 세로로 이어지는 긴 스크롤 방식이었습니다. 2주차 코칭에서
지적된 "긴 스크롤 중 중요한 정보를 놓치기 쉽다"는 문제를 계기로, 한 화면에 한 단계만 보여주고 명시적으로
다음/이전을 눌러 넘어가는 구조로 전환했습니다(3주차에 협업자가 먼저 별도 파일로 실험 → ④번 항목 참고).
이 과정에서 화면이 좁아 한 단계 내용이 다 안 들어가는 문제가 발견되어 일부 단계를 다시 세부 화면으로
쪼개는 작업(가구·건강 화면 분할)이 뒤따랐고, 이 작업 역시 두 세션이 동시에 구현하는 중복이 발생했던
지점입니다(④번 항목 참고).

## 5. 목적이나 의도가 변질된 것이 있는가

**핵심 목적(왜)은 변하지 않았습니다.** "현장 상담원이 그 자리에서 빠르고 정확하게 예비판단과 문서 작성을
할 수 있게 돕는다"는 문제의식은 예선 프로토타입 단계부터 지금(a30)까지 한 번도 흔들리지 않았고, 오히려
실사용 피드백을 거치며 더 구체화됐습니다(예: 소득·재산 "미입력"과 "0원"을 구분하지 않으면 잘못된 판정이
나올 수 있다는 것을 9/7에 뒤늦게 발견하고 고친 것 — 이건 "정확한 예비판단"이라는 최초 목적에 오히려 더
충실해진 사례입니다).

**바뀐 것은 전부 "어떻게(수단)"입니다**: 어떤 AI 공급자를 쓸지(Gemini→OpenAI), 어떤 STT 모델을 쓸지
(화자분리 지원 모델→언어 강제가 되는 구모델), 문서 구조를 코드로 만들지 사람이 만든 것을 그대로 쓸지,
보안을 개인 비밀키로 할지 조직 접근 가능한 서버키로 할지, 화면을 스크롤로 보여줄지 단계별로 보여줄지,
파일을 하나로 유지할지 잠시 나눴다 합칠지 — 이런 결정들은 코칭·실사용 테스트·협업 과정에서 여러 번
뒤집혔고, 그 사이사이에 크고 작은 버그·회귀도 상당히 많았습니다(로그인 프로필 저장 실패, 완료 버튼
사라짐, hwpx 다운로드 먹통, CSS 규칙 하나가 세 화면을 깨뜨린 것 등). 이건 프로젝트가 방향을 잃은 게
아니라, **코칭에서 제시된 방향과 실제 구현·실사용 중 발견한 현실(코드로 만든 문서의 한계, 모델의 실제
동작, 사용자 반응, 협업 중 발견한 리스크)을 계속 맞춰가며 검증하고 고쳐온 정상적인 반복 과정**으로
해석하는 게 맞습니다. 특히 ⑤번(보안 철학) 변곡점은 "겉보기엔 후퇴(제로지식 암호화를 없앰)처럼 보이지만,
실제로는 법적 책임·운영 현실을 더 깊이 고려한 결과"라는 점을 발표에서 정확히 설명할 필요가 있습니다 —
심사위원이 "왜 더 강한 보안을 뺐냐"고 물을 수 있는 지점이기 때문입니다.

## 6. 코칭을 통해 생긴 구체적 변화 요약

| 시점 | 지적/조언 | 실제 변화 |
|---|---|---|
| 1주차(8/28) | 서울 리전·RLS·암호화·유료 API | Supabase 서울 프로젝트, RLS ON, pgcrypto+Vault 암호화, 유료 API 전환 |
| 1주차(8/28) | "기존 hwpx 양식에 코드변수를 넣는 방식"으로 실제 서류 양식 구현 | 처음엔 구조까지 코드로 시도 → 문제 발견 후 사람이 만든 파일에 값만 치환하는 방식으로 정착, 이 과정에서 사유서 양식 재작업·hwpx 다운로드 버그(v0.34)도 발생·해결(①번 변곡점) |
| 1주차(8/28) | UI 메인 1창+사이드바 | 사이드바 구조 도입, 나머지 기능 숨김 |
| 2주차(9/2) | 스크롤 방식 → 페이지 넘김 | 위저드(단계별 화면) 방식으로 전환, 협업자의 별도 파일 실험 중 "이어하기 빈 화면" 버그 발견·수정 거쳐 정착(④번 항목 참고) |
| 2주차(9/2) | 가명화 도우미 존재 이유 재검토 | 이후 9/7 "제로지식→조직접근" 보안 철학 재정립으로 확장(⑤번 변곡점) |
| 2주차(9/2) | STT는 모델 교체보다 파라미터 조정이 현실적 | 우선 언어 고정(ko) 파라미터만 추가(v0.42) → 실사용에서 재발 확인 후 결국 모델을 whisper-1로 교체, 화자분리 기능은 포기(②번 변곡점) |
| 2주차(9/2) | Supabase 유료 전환 불필요, 코딩 도구에 투자 | (팀 내부 결정 사항 — 발표에는 직접 노출 안 해도 됨) |
| 3주차(9/4 현장) | (코치 조언 아님 — 현장 실사용 피드백) | 하루 만에 v0.41~v0.48 8단계 배포: 비로그인 저장 소실, STT 언어 혼입, 모바일 글씨(3연속 회귀 포함), 녹음 결과 화면 재설계, 재녹음 표시 강화, 비밀키 암호화 클라이언트 구현까지 실시간 대응(③번 변곡점) |

## 7. 발표에 쓸 때 제안하는 프레이밍

- **예선 프로토타입 → 본선 코칭으로 발전시킨 흐름을 정확하게 말할 것** — "예선에 제출한 프로토타입으로
  본선에 진출했고, 본선 코칭을 통해 그 프로토타입을 실제 현장에서 신뢰할 수 있는 수준으로 발전시켰다"는
  게 정확한 구조입니다. 코칭이 준 구체적 방향(보안 구조, 문서 양식 방법, UI 구조 등)을 팀이 실제로
  구현하며 겪은 시행착오와 개선 — 이게 발표에서 말할 "개발 과정" 스토리의 핵심입니다.
- **"완벽하게 한 번에 되지 않았다"는 걸 숨기지 말 것** — 회고를 정리해보니 실제로는 로그인 프로필 저장
  실패, 완료 버튼 사라짐, hwpx 다운로드 먹통, CSS 규칙 하나가 세 화면을 깨뜨린 것처럼 자잘한 버그와
  회귀가 매우 많았습니다. 이걸 "매끄럽게 한 번에 완성했다"로 포장하기보다, "짧은 기간에 실제 사용자
  피드백을 받으며 이 정도로 촘촘하게 수정 사이클을 돌렸다"는 근거로 쓰는 게 더 설득력 있습니다(특히
  9/4 하루 동안의 v0.41~v0.48 8단계 배포는 그 자체로 강력한 증거).
- "처음부터 끝까지 문제의식은 하나였다 — 코칭에서 받은 구체적 방향을 실제로 구현하면서 계속 검증하고
  고쳤다"는 흐름으로 이야기하면, 심사기준 ①(완성도)·③(문제 해결)에 모두 자연스럽게 연결됩니다.
- ⑤번(보안 철학 변경) 변곡점은 반드시 "왜 되돌렸는지"까지 설명할 것 — 그냥 "뺐다"고만 말하면 후퇴로
  보일 수 있지만, 법적 책임·분실 리스크까지 고려한 결정이라는 걸 보여주면 오히려 성숙한 판단으로 읽힙니다.
- 9/4 오후 실사용 중 실시간 버그 대응(v0.41~v0.48)은 "현장 변화" 심사기준에 쓸 수 있는 가장 구체적인
  증거입니다 — 다만 `presentation-prep.md`에 표시된 대로, 아직 정량적인 Before/After 수치(작성 시간
  단축 등)와 상담원 인터뷰 코멘트는 비어 있는 상태라 9/18 제출 전 최우선으로 채워야 합니다. 예선 시기에
  준비해둔 baseline 측정 도구를 실제로 상담원에게 써보게 해서 "도입 전" 수치를 지금이라도 확보해 두면
  좋습니다.
