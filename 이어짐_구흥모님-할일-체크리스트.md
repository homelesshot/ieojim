---
title: 구흥모님이 직접 하셔야 할 일 (계속 갱신)
description: Claude가 코드로 대신 못 하고 구흥모님이 직접 눌러야 하는 일만 모아둔 체크리스트. SQL/DB 관련 항목은 특히 여기서 놓치지 않게 계속 챙김. 새 항목 생기면 여기 추가.
sources: cowork
---

# 구흥모님이 직접 하셔야 할 일

여기 있는 건 전부 "Claude가 코드는 다 준비해뒀지만, 계정 보안·비용·정책 문제 때문에 본인이 직접
눌러야 끝나는" 일입니다. 새로운 항목이 생기면 이 문서에 계속 추가해서 안 잊어버리게 관리합니다.

## ⬜ 미완료

### 1. (선택) 본인 계정을 "관리자"로 지정하기 — SQL, 급하지 않음

**언제 필요한가**: 지금은 안 하셔도 앱은 100% 정상 동작합니다. 나중에 "담당 상담원이 퇴사하거나
자리를 비웠을 때, 다른 사람(관리자)이 그 사람 상담 기록을 대신 봐야 하는 상황"이 생기면 그때 필요한
설정입니다. 지금 당장 그런 상황이 없다면 미뤄도 됩니다.

**무슨 뜻인가(쉽게 풀면)**: 지난 세션에서 "이 사람은 관리자다"라고 표시할 수 있는 자리(is_admin)를
DB에 새로 만들어뒀는데, 아직 그 자리에 체크된 사람이 아무도 없습니다. 본인 계정을 그 자리에 체크해야
실제로 관리자 권한이 작동합니다.

**어떻게 하나** (Claude는 계정 정보를 직접 조회하지 않으므로 아래 둘 중 하나를 골라주세요):
- **(A) 저한테 요청하기**: "내 uid로 관리자 설정해줘"라고 말씀해주시고, Supabase 대시보드
  (supabase.com 로그인 → `ieojim-stt-proxy` 프로젝트 → 왼쪽 메뉴 Authentication → Users)에서 본인
  이메일 옆에 있는 긴 영문/숫자 코드(UID)를 복사해서 알려주시면 제가 대신 실행해드립니다.
- **(B) 직접 실행하기**: 같은 대시보드에서 왼쪽 메뉴 **SQL Editor** → 아래 문장에서 `<본인 UID>`
  부분만 방금 복사한 값으로 바꿔서 붙여넣고 실행(Run) 버튼만 누르면 됩니다.
  ```sql
  update public.counselor_profiles set is_admin = true where id = '<본인 UID>';
  ```

### 2. "다른 기관 적용 가이드" 문서 — 아직 안 씀 (임팩트 20점 항목)

다른 기관이 이 코드를 가져다 쓰려면 뭘 바꿔야 하는지 설명하는 짧은 안내문입니다(Supabase 새 프로젝트
만들기, API 키 발급, 기관명 상수 하나 바꾸기 정도). 분량이 크지 않아서 요청하시면 제가 초안을 바로
만들어드릴 수 있습니다 — 아직 "만들어줘" 요청은 없었습니다.

### 3. 실사용 데이터 확보 — 코드가 아니라 현장에서 하셔야 하는 일 (가장 배점 큰 항목)

- baseline 측정 도구(`이어짐_1주차_baseline측정.html`)로 도입 전/후 소요시간 비교 최소 1건
- 실제로 만들어진 사유서 샘플(가명 처리) 캡처 1~2건
- 추가 정성 피드백(상담원 코멘트) — sunny님이 9/14에 가상 인터뷰 2건은 이미 채웠고, 5~10건까지
  확대하는 게 다음 목표로 잡혀있음(`이어짐_작업기록_2026-09-14.md` 참고)

## ✅ 완료된 것 (참고용, 더 이상 신경 안 쓰셔도 됨)

- decrypt_case_field 소유권 검증, touch_counselor_keys search_path 고정, cases 테이블 관리자 조회
  RLS 정책 — 2026-09-15 Claude가 Supabase에 직접 실행 완료(`심사기준-갭분석-2026-09-11.md` 참고)
- stt-transcribe.ts Whisper 환각 필터 개선판 — 이미 배포되어 있음 확인(2026-09-15)
- org(소속) 필드 고정값 전환 — 2026-09-15 완료(a71)
- presentation-prep.md 완성도 섹션 최신화 — 2026-09-11 완료
- Leaked Password Protection, 초대 전용 회원가입 — 제출 범위 밖으로 확정, 조치 불필요
