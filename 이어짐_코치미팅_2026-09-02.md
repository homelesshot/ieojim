---
title: 코치 미팅 정리 & 실행 계획 (2026-09-02, 2주차)
description: 2주차 코칭(9/2) 줌 녹취록 + 사용자 정리 대조·보완한 최종본. UI/UX 피드백, STT 개선 방법, 가명화 필요성 논의, 요금제/인프라 조언, 3~4주차 일정 포함.
---

# 코치 미팅 정리 & 실행 계획 (2026-09-02, 2주차)

## 1. UI/UX 피드백

1. **긴 랜딩페이지 방식 → 페이지 넘김 방식으로 변경**. 지금은 다음 버튼 누르면 아래로 쭉 이어지는 구조라 중간 내용(특히 예비판단 결과처럼 중요한 정보)을 스크롤하다 놓치기 쉬움. 책장 넘기듯 한 화면에 한 단계만 보이고, 이전 화면을 보려면 명시적으로 뒤로가기를 누르는 구조가 더 일반적인 UX.
2. **화면 하단에 이전/다음 버튼 고정 표시**.
3. **중요한 정보(특히 예비판단 결과)는 진한 색 + 동그라미/체크 이모지 등으로 가시성 강화**. 지금은 녹색 계열인데 더 청록색 계열로, 신호등처럼 명확한 긍정 표시를 주면 판단 근거를 굳이 다 읽지 않고도 "이 사람은 지원 대상이 되는구나"를 바로 알 수 있음.
4. **완료 시점을 명확히 인지시킬 것**: "＋ 새 상담 시작" 버튼만으로는 지금이 마지막 화면인지 알기 어려움. "완료 → 홈 화면으로 나가기" 또는 "상담 마치기"처럼 세션이 끝났다는 걸 명확히 알려주는 버튼/문구가 필요.

## 2. 가명화 도우미 — 필요성 재검토

코치님이 짚은 핵심 질문: "결국 사유서(산출물)에는 실명이 들어가야 하는데, 가명화 도우미가 왜 필요한가?"

- 정부 제출용 사유서 자체는 실명이 들어가야 정상적으로 작동 — 즉 최종 산출물 단계에서 가명화는 오히려 의미가 없어짐.
- 다만 예외적으로 필요할 수 있는 경우: (1) 녹음 원본이 기기에 남아있다가 분실·유출됐을 때의 대비, (2) 가정폭력 피해자처럼 실명이 노출되면 안전에 위협이 되는 특정 케이스(쉼터 위치 등)의 보호.
- 음성이든 텍스트든 이미 STT/AI 처리를 위해 서버(OpenAI 등)로 넘어가는 것은 가명화 여부와 무관하게 동일함 — 다만 API/엔터프라이즈 계약 시 "학습에는 사용하지 않는다"는 게 각 업체의 공식 고지사항.
- **코치님 제안(택1)**: (A) 가명화를 "새 상담 시작" 세션 안에 자동으로 내장(임베딩)해서 사용자가 의식하지 않아도 자동 처리되게 하거나, (B) 산출물에 어차피 실명이 필요하다면 별도의 "가명화 도우미" 기능 자체를 없애기.
- → 다음 코칭 전까지 어느 방향으로 갈지 결정 필요.

## 3. 음성인식(STT) 정확도 개선 방법 — 3가지, 현실성 순위 있음

1. **모델 교체**: 다른 STT API로 갈아타기. 비용 부담(개발 단가) 때문에 신중해야 함.
2. **모델 파인튜닝**: 직접 음성인식 모델을 학습시키는 것 — 매우 어려움, 사실상 비현실적(코치님도 "이건 어렵다"고 명시).
3. **API 통신 호출값(파라미터) 조정** — **코치님이 명시적으로 "가장 현실적"이라고 추천한 방법**:
   - 언어를 한국어(`ko`)로 고정 → 언어 자동판단으로 인한 환각(엉뚱한 언어로 오인식) 제거
   - 너무 작거나(웅얼거림) 너무 시끄러운(배경소음) 구간은 무시하고 빈 값으로 처리 → 잘못된 텍스트를 만들어내는 것보다 "인식 안 됨" 처리가 나음
   - 클로드에게 "STT 호출 코드에서 언어를 한국어로 고정하고, 너무 작은/큰 소리 구간은 무시하도록 설정 바꿔줘, 그 외에 조정 가능한 다른 옵션도 알려줘"라고 요청하면 나머지 옵션도 알려주고 구현 가능.
   - 시간이 남으면 시도해볼 만한 것 — 필수 우선순위는 아님.

(참고: 이미 오픈위스퍼 대신 OpenAI `gpt-4o-transcribe-diarize`로 결정한 상태를 코치님과 공유함 — 화자분리 지원, 비용은 오픈위스퍼보다 다소 높음, "일단 이걸로 시작해보고 안 되면 바꾸자"로 확인됨.)

## 4. 보안(Supabase) — 이미 설계한 구조 확인

- 테이블을 두 개로 분리: 메인 테이블(누구나 열람 가능한 범위)은 가명화된 값으로 저장, 실명 등 실제 정보는 별도 테이블에 두고 시크릿 키로만 열람 가능하게 하는 구조 — 이미 이렇게 설계되어 있음(pgcrypto+Vault로 구현 완료, 이번 코칭에서 재확인받음).
- RLS(행 수준 보안)는 버튼 켜듯이 설정하는 거라 이미 완료.
- 안 되면 fake data로 하거나 DB 연동 자체를 생략해도 괜찮다는 여지도 코치님이 남겨줌(최종 발표 때 시도는 해보되, 안 되면 "저희는 저장하지 않습니다"로 커버 가능).

## 5. 인프라/요금제 조언

- **Supabase 유료 전환은 불필요**: 무료 티어의 단점은 "7일간 트래픽 없으면 자동 일시정지"뿐이고, 데이터가 삭제되는 건 아니며 다시 켜면 됨. 코치님은 Supabase에 돈 쓰기보다 다른 곳(코딩 도구)에 투자하라고 조언.
- **AI 코딩 도구 요금제 업그레이드 고려**: 클로드 사용량이 홈페이지 프로젝트와 병행하다 보니 자주 부족해서 ChatGPT로 전환하는 상황이 발생 중. 코치님은 클로드 상위 요금제나 Codex(약 30만원) 같은 유료 코딩 도구 결제를 추천함 — "수상 가능성이 있는 팀이니 구독료는 크게 부담되지 않는 비용"이라는 취지.

## 6. 핵심기능 우선 구현 목록 (다음 코칭 전까지)

- 상담원 로그인 기능
- 최근 상담내역 저장(사이드바에 "최근" 목록으로 계속 쌓이도록)
- 사유서 양식 확정
- 음성 인식 결과가 사유서 양식 칸에 정확히 들어가도록 연결
- 가명화 기능을 어떻게 할지 최종 결정(위 2번 참고 — 임베딩 vs 삭제)

## 7. 기타 발견된 버그

- **기기별 사유서 다운로드 오류**: PC에서 추출 시 프로그램이 다운됨. 아이폰에서는 초안 보고서가 정상 작성되는데, 갤럭시(안드로이드)에서는 안 됨. (참고: 이건 어제 로그한 "아이폰 직접 녹음 오류"와는 별개의 문제 — 다운로드/생성 단계의 기기별 호환성 이슈.)

## 8. 남은 일정 (프로젝트 4주 구조)

- **2주차(이번 주, 오늘 코칭)**: 핵심기능 구현 체크포인트 — 코치님 확인 결과 이미 잘 되어 있다고 평가받음.
- **3주차(다음 코칭)**: 실사용 점검 — 실제 현장 사용자(아웃리치 상담원)가 직접 써보고 음성인식 정확도, 사용 편의성, 오류/버그에 대한 피드백을 받는 단계. (핵심기능 구현이 아니라 "실사용 테스트"가 3주차의 목표임 — 핵심기능은 이번 주 안에 마무리)
- **4주차**: 최종 제출 정리 — 이 시점부터는 새 기능 개발을 하면 안 되는 시점. (참고: 최종 제출 때 "깃허브 리드미로 정리"해야 하는 건 깃허브를 안 쓰는 다른 팀들 얘기였고, 저희 팀은 이미 깃허브로 계속 커밋해왔기 때문에 그 자체로 제출 준비가 되어 있음 — 별도 리드미 필수 아님)
- 다음 코칭 미팅은 다음 주(월/화 예정)로 잡을 것.
