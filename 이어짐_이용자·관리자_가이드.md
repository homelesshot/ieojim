# 이어짐 — 이용자·관리자 가이드

> 위기가구 긴급복지 상담 도우미 "이어짐"의 **이용자(현장 상담원)** 사용법과 **관리자(운영자)** 설치·운영 안내입니다.
> 라이브: https://homelesshot.github.io/ieojim/이어짐_v4.html

---

## A. 이용자(현장 상담원) 가이드

### A-1. 준비물
- 스마트폰 또는 PC 브라우저(모바일 크롬/사파리/삼성인터넷, PC 크롬/엣지), 인터넷 연결
- **설치·로그인 불필요** — 링크만 열면 바로 사용(저장·이어하기만 로그인 필요)

### A-2. 사용 순서 (3분)
1. **링크를 연다** → 상단에서 **대상자 유형**(거리 노숙인·쪽방 주민·가정폭력 피해자·취약노인·외국인노동자·위기가구 등)을 고른다.
2. **상황을 입력한다** — 세 가지 방법 중 하나:
   - ① 가운데 **🎤 녹음 버튼**을 눌러 말로 녹음
   - ② 아래 **입력창에 직접 타이핑**
   - ③ **"상황 입력 없이 바로 진행"** (수동으로 하나씩 선택)
   → 녹음/타이핑하면 **AI가 내용을 보고 위기사유·거주·소득·재산·건강 등 항목을 자동으로 채워줍니다.**
3. **단계별로 확인한다** — 확실한 항목은 자동으로 넘어가고, AI가 불확실한 항목은 **노란 "확인 필요"** 안내로 표시됩니다. 특히 **소득·재산은 반드시 눈으로 확인**한 뒤 "다음"을 누르세요.
4. **결과 3종이 자동 생성**됩니다: **① 긴급복지 예비판단 → ② 지원·연계 안내 → ③ 상담일지 초안(한글 .hwpx)**
5. **상담일지(.hwpx)를 저장**해 관공서 제출·기록에 사용합니다.

### A-3. 개인정보 — 꼭 지킬 것
- **녹음할 때 대상자의 실명·주민번호·주소·연락처를 말하지 마세요.** 음성은 텍스트 변환 전에 서버로 전송되어 가려지지 않습니다. 이름이 필요하면 **"어르신"·"A씨"**처럼 부르세요.
- 입력·문서에도 **실명 대신 가명·이니셜**을 쓰세요. 상담일지의 성명·주민번호·주소는 `[___]`로 비워지며, **실제 기입은 담당자가 오프라인에서** 합니다.
- 결과는 **예비판단(참고용)**이며, 최종 지원 여부는 담당 기관·지자체 심사가 결정합니다.

### A-4. 자주 묻는 상황
- **카카오톡 링크로 열었는데 저장이 안 돼요(안드로이드)** → 상단 **⋮ → "다른 브라우저로 열기"**(크롬)로 여세요.
- **로그인이 안 돼요(휴대폰)** → 시크릿 모드 끄기 + 쿠키·사이트데이터 허용 + 카톡 인앱 말고 크롬/사파리로 열기. (로그인 없이도 판단·상담일지는 됩니다 — 저장·이어하기만 로그인 필요)
- **녹음 중 화면이 꺼지면 녹음이 멈춰요** → 안전을 위한 의도된 동작입니다(꺼진 화면에선 소리가 깨져 엉뚱한 문장이 생기므로). 녹음 전 **화면 자동 꺼짐 시간을 길게/안 함**으로 설정하세요. 멈추기 전까지 녹음분은 저장·변환됩니다.
- **타이핑했더니 뒤 항목이 안 채워져요** → 긴급성 단서를 한 마디 넣어보세요(예: "오늘 당장", "이번 주 안에"). 그러면 끝까지 자동으로 채워집니다.

---

## B. 관리자(운영자) 가이드

### B-1. 구조
- **프론트**: 단일 HTML 파일(`이어짐_v4.html`) — 정적 호스팅(GitHub Pages 등)에 올리면 끝.
- **백엔드**: Supabase(서울 리전) — 로그인(Auth)·상담 저장(Postgres)·AI 호출 프록시(Edge Function).
- 화면에서 녹음/입력 → **Edge Function**(`stt-transcribe`, `ai-draft`)이 OpenAI를 호출(실제 키는 서버에만 보관) → 결과를 화면 판정엔진에 태워 상담일지 생성.
- **비로그인 상태에서도 판단·문서 생성은 100% 동작**(저장·이어하기·최근목록만 로그인 시 활성).

### B-2. 설치·배포
1. **정적 호스팅**: `이어짐_v4.html`을 GitHub Pages(또는 임의 정적 호스팅)에 업로드 → 링크 배포.
2. **Supabase 프로젝트 생성**(리전: **Seoul**).
3. **Edge Function 배포**: `stt-transcribe`(Whisper STT), `ai-draft`(항목추출·초안). 대시보드 **Secrets에 OpenAI API 키** 설정 — *키는 서버 환경변수로만, 저장소·화면 코드에 절대 넣지 않음.*
4. **DB·RLS**: 상담 저장 테이블(`cases` 등) + **RLS 활성화**(본인 상담원만 접근). 민감 스냅샷은 서버키 암호화(pgcrypto).
5. **화면 연결값**: `이어짐_v4.html` 상단의 `SUPABASE_URL`·공개(publishable) 키를 본인 프로젝트 값으로 교체. (publishable 키는 공개돼도 안전한 공개키)

### B-3. 운영 유의
- **개인정보**: 가명 입력 원칙, 상담일지 성명·주민번호·주소는 `[___]`. 상담원에게 "녹음 시 실명 금지" 교육.
- **접근 통제**: RLS로 상담원 본인 것만 열람. 조직 인계가 필요하면 관리자(admin) 접근 정책 별도 적용.
- **비용 관리**: STT·AI 호출은 OpenAI 사용량이 발생 → 시간당 호출 제한 등 남용 방지 검토(진행 중).
- **개인정보 처리방침**: 서비스 내 상시 접근 문서 + 최초 1회 안내. 문구·문의처는 기관 개인정보 담당자 검토.

### B-4. 개발·이력 참고
- 개발 전체 과정: `이어짐_발표대본_개발과정.md`, `이어짐_v4.html` 내 `VERSION_LOG`
- 보안 자체 점검: `이어짐_적대적리뷰_2026-09-09.md`, `이어짐_적대적리뷰_해결현황_2026-09-10.md`
