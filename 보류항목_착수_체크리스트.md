# 이어짐 — 적대적 리뷰 보류 항목 착수 체크리스트 (2026-09-09)

> 원본 리뷰: [`이어짐_적대적리뷰_2026-09-09.md`](이어짐_적대적리뷰_2026-09-09.md) (전체 13항목)
> 이미 반영: **a32**(4건: 면책문구·hwpx 이스케이프·소득재산 확인강제·5인 배지) / **a33**(2건: AI추출 전 PII 스크럽·건강화면 트라우마 안내)
> 이 문서 = 남은 **보류(서버/DB/정책) 항목**을 구흥모 쌤이 바로 착수할 수 있게 "어디를·무엇을·어떻게"로 풀어놓은 것.
> 착수하는 사람은 `담당`/`상태` 칸을 채워주세요. 겹침 방지 위해 시작 전 [아이디어_메모.md](아이디어_메모.md) "대기 중"에도 한 줄 남기기.

---

## 우선순위 한눈에 (리뷰 "제출 전 우선순위" 기준)

| # | 항목 | 심각도 | 작업 위치 | 클라만으론? | 상태 | 담당 |
|---|------|--------|-----------|-------------|------|------|
| 1 | STT·AI초안 인증 토큰 구조 | 🔴 치명 | Edge Function + 클라 + 정책 | ✗ | ☐ 미착수 | |
| 2 | 실명/지명 자동 가명화 (치명2 나머지) | 🔴 치명 | 클라 + 기관 정책 | △ 정책 먼저 | ☐ 미착수 | |
| 3 | 조직 접근 권한(admin) 구현 | 🟠 높음 | DB(SQL) | ✗ | ☐ 미착수 | |
| 4 | 암호화 단일키 SPOF | 🟡 중간 | DB + Edge Function | ✗ | ☐ 미착수 | |
| 5 | 기관/작성자 이름 위조 방지 | 🟡 중간 | Auth 설정 + 클라 | △ | ☐ 미착수 | |
| 6 | 국적·거주형태 선택지 확충 | 🟡 중간 | 클라(v4.html) | ○ (현장데이터 후) | ☐ 보류 | |
| 7 | Whisper 환각 필터 개선 | 🟢 낮음 | Edge Function | ✗ | ☐ 미착수 | |
| 8 | Supabase 어드바이저 2건 | 🟢 낮음 | 대시보드 + DB | ✗ | ☐ 미착수 | |

DB 작업(3·4·8 일부)의 실행 SQL은 [`db/2026-09-09_보류항목_DB작업.sql`](db/2026-09-09_보류항목_DB작업.sql)에 구역별로 정리해둠.

---

## 1. 🔴 STT·AI초안 인증 토큰 구조 (비용 폭탄·오남용 차단)

- **현황**: `stt-transcribe`·`ai-draft`가 공개 `sb_publishable_…` 키를 정적 Bearer로 사용 → 소스만 보면 누구나 무제한 호출.
- **먼저 결정할 정책 질문**: 이 앱을 **비로그인(익명)으로도 쓸 수 있게 둘 것인가?**
  - 익명 허용 유지 → 세션 토큰 교체는 불가. 대신 **(임시방편) 함수별 시간당 호출 카운터**로 비용 폭탄만 차단.
  - 로그인 필수로 전환 → 아래 정식 해결로.
- **정식 해결 (로그인 필수 시)**:
  1. 클라: `'Bearer '+STT_ANON_KEY` → 로그인 세션의 `access_token`으로 교체.
     `const { data:{ session } } = await sb.auth.getSession(); headers.Authorization = 'Bearer '+session.access_token;`
     (두 곳: `stt-transcribe` 호출 ~716행, `ai-draft` 호출 ~949·2809행. 비로그인 시 호출 막고 로그인 유도.)
  2. Edge Function: 요청 JWT를 디코드해 `role !== 'authenticated'`(=anon)면 401 거부.
  3. `ai-draft`는 클라가 보낸 `prompt`를 그대로 OpenAI에 넘김 → **고정 시스템 프롬프트로 감싸서** 이어짐 무관 용도 오남용 차단.
- **검증**: 로그아웃 상태로 `curl`에 publishable 키만 넣어 호출 → 401 나오면 성공.

## 2. 🔴 실명/지명 자동 가명화 (a33에서 형식PII만 처리, 이름은 남김)

- **현황**: a33이 이메일·주민번호·카드·전화만 자동 마스킹. **이름·지명은 상담원이 가명화 도우미에 목록 입력해야** 치환 → 녹음 직후엔 목록이 비어 자동 불가.
- **먼저 결정할 정책 질문(기관 개인정보 담당자)**: 이름을 어느 시점에·어떻게 식별할 것인가?
  - (a) 상담 시작 전 상담원이 대상자 이름을 한 번 입력받아 그 세션 내내 자동 치환, 또는
  - (b) NER(개체명 인식) 도입(오탐·비용 트레이드오프), 또는
  - (c) "이름은 처음부터 말하지 않기"를 운영 수칙으로.
- **주의**: 리뷰 지적대로 이름을 지우면 성별·관계 추론 정확도가 떨어질 수 있음 → 결정 후 추출 정확도 재검증 필요.

## 3. 🟠 조직 접근 권한(admin) 구현 — **DB SQL 준비됨**

- **현황**: 9/7에 "조직도 접근 가능"으로 정책 정했으나 RLS는 아직 "본인만"(admin 역할 자체가 DB에 없음).
- **작업**: [`db/2026-09-09_보류항목_DB작업.sql`](db/2026-09-09_보류항목_DB작업.sql) **[B] 구역** 실행 —
  `counselor_profiles.is_admin` 추가 + `cases`에 "admin이면 조회" SELECT 정책 **추가(비파괴적)** + 관리자 uuid 지정.
- **검증**: admin 계정으로 로그인해 남의 사례가 조회되는지 / 일반 계정은 여전히 본인 것만 보이는지.
- **시간 없으면**: 발표에서 "정책 방향은 정했고 기술 구현은 다음 단계"로 스코프 축소 설명(리뷰 권고).

## 4. 🟡 암호화 단일키 SPOF — **⚠️ 라이브 깨짐 주의**

- **현황**: `decrypt_case_field`가 전역 단일키 + `authenticated` 전체 실행권 + 소유권 미검증(어드바이저 경고).
- **⚠️ 함정**: 클라가 이어하기 시 `decrypt_case_field`를 **직접 RPC 호출**(v4.html ~1714행). 리뷰의 `REVOKE EXECUTE`를 지금 그대로 하면 **현장 이어하기가 즉시 깨짐**.
- **안전한 순서**(둘 중 하나 먼저):
  - (권장) 복호화를 Edge Function(service_role)으로 옮기고 클라는 그 함수를 호출 → 그 다음 REVOKE.
  - (대안) `decrypt_case_field` 내부에서 넘어온 `enc`가 `auth.uid()=counselor_id`인 `cases` 행의 `state_json_enc`와 일치하는지 검증하도록 함수 수정 → REVOKE 없이도 오남용 차단.
- SQL 스텁·경고는 위 SQL 파일 **[C] 구역**(지금 실행 금지 블록)에.

## 5. 🟡 기관/작성자 이름 위조 방지

- **현황**: `counselor_profiles.org`/`display_name`을 본인이 검증 없이 수정 → 가입만 하면 "사단법인 인천내일을여는집" 사칭 문서 생성 가능.
- **작업**:
  - (근본) 가입을 기관 도메인 `@homelesshot.or.kr`로 제한 — Supabase Auth 설정(또는 가입 직후 도메인 검증 후 미승인 비활성화).
  - (보강, 클라) hwpx의 기관명 `org`을 자유입력 대신 **고정값(기관명 하드코딩)**으로.
    ※ 단, 회원가입 `org` upsert(v4.html ~1194행)·프로필 표시 로직과 얽혀 있어 **구흥모 쌤이 profile 쪽과 함께** 수정하는 게 안전(단독 수정 시 겹침 위험 → 이번 a33에선 손대지 않음).

## 6. 🟡 국적·거주형태 선택지 확충 — (현장 데이터 모인 뒤)

- `<option>` 추가는 클라만으로 가능하나, 리뷰가 "**실사용 테스트에서 나온 실제 사례 기준**"으로 넣으라 명시 → 임의로 지어 넣지 않고 현장 피드백 후 반영.
- 추가 시: `NATIONALITY_LIST_V4`/`LIVING_LIST_V4`(추출 enum)와 판정 매핑(`ASSET_LIMIT`·`LIVING_PRESET`) 동기화, 새 항목은 기존 "기타" 계열로 매핑해 판정 로직 영향 없게.

## 7. 🟢 Whisper 환각 필터 (Edge Function)

- `stt-transcribe`의 화이트리스트 필터는 처음 보는 환각 문장을 못 거름 → `no_speech_prob`/`avg_logprob` 임계값을 보수적으로(AND→OR 검토) 조정. 근본 해결은 모델 한계상 어려움.

## 8. 🟢 Supabase 어드바이저 2건

- **유출된 비밀번호 차단 OFF** → 대시보드 Authentication에서 "Leaked password protection" ON (토글 하나, 5분).
- **`touch_counselor_keys` search_path 미고정** → SQL 파일 **[A] 구역** 실행(안전, 즉시 가능).

---

## 🔎 리뷰 보완 — 추가로 발견한 항목 (참고 수준 · sunny님/협업자 Claude 검토)

> 구흥모 쌤 리뷰 13항목 원문은 그대로 두고, **후속 검토에서 새로 눈에 띈 것**만 아래에 참고로 적습니다.
> **판단·우선순위는 구흥모 쌤과 기관 몫**입니다 — 특히 아래 ②·③은 구흥모 쌤 영역(재상담 기능·정책)과
> 겹쳐서 **제 의견은 참고 수준**이니, 실제 반영 여부·방식은 구흥모 쌤이 정해주세요.

**① 🔴 음성 원본이 가명화를 우회함 (리뷰 치명2의 사각지대) — a34에서 1차 완화함 [x]**
- STT는 텍스트가 아니라 **녹음 원본(audio)**을 그대로 OpenAI로 보냄(`transcribeAudio`). 대상자가 말로 뱉은
  실명·주민번호는 a33의 텍스트 스크럽이 개입하기 전에 이미 국외로 넘어감.
- **a34(반영)**: 녹음 화면에 "실명·주민번호는 말로 하지 마세요 — 이름은 어르신·A씨로" 안내 추가(즉시 완화).
- **근본 대응(보류)**: 온디바이스/국내 STT 검토, 또는 운영수칙으로 이름 미언급 강제. → 기술·비용·정책 결정.

**② 🟠 민감정보 국외이전 + 정보주체(대상자) 동의 절차 부재 — 참고**
- 건강·가정폭력 정황 = 개인정보보호법 **민감정보(§23)**, OpenAI 전송 = **국외이전(§28-8)**. 현재 대상자
  **동의 캡처가 없음**(상담원용 경고 한 줄뿐, 화면 2148행).
- (참고 의견) "대상자에게 녹음·AI 사용을 안내하고 동의를 받았음"을 상담원이 체크하는 최소 확인칸,
  또는 기관 표준 동의문구 도입 검토. **다만 어떤 문구가 법적으로 유효한지·동의를 어떻게 받을지는
  기관 개인정보 담당자 결정 사항** → 제 판단으로 화면에 넣지 않고 참고로만 남김.

**③ 🟡 기기 잔존 데이터 (재상담 모드) — 구흥모 쌤 영역, 참고**
- 재상담 모드가 건강·지원이력을 localStorage(`ieojim_last_profile_v1`)에 **평문 저장** → 공용/분실 기기 시 노출.
- (참고 의견) 저장 항목 최소화, 일정 기간 후 자동 삭제, 또는 로그아웃 시 삭제 검토.
  **이 기능은 구흥모 쌤이 만든 재상담(a31 테스트) 트랙**이라, 반영 여부·방식은 구흥모 쌤 판단에 맡깁니다(참고).

**④ 🟡 접근 감사로그·삭제요청 대응 부재 — 참고**
- 누가 언제 어느 사례를 열람했는지 기록 없음 + 정보주체 삭제요청 처리 경로 없음. **조직 admin 접근(3번)을
  열면 더 필요해짐** → admin 조회 정책과 함께 감사로그 테이블 검토 권장(참고).

**⑤ ⚪ XSS 미감사 — 참고(취약점 확인이 아니라 "점검한 적 없음")**
- 세션 토큰이 localStorage에 있어, 자유입력이 `innerHTML`로 안 걸러져 들어가는 곳이 하나라도 있으면
  토큰 탈취로 이어질 수 있음. **아직 감사하지 않았습니다** — 취약점을 발견했다는 게 아니라 점검 필요.
- (참고) 자유입력→DOM 삽입 지점 일괄 점검(가명화 도우미 출력은 escHtml 사용 확인, 나머지 확인 필요).

---

### 이번 세션이 손댄 것 / 안 댄 것
- **손댐(클라만·안전)**: a33(전송 전 PII 스크럽·트라우마 안내), **a34(녹음 화면 이름 회피 안내)**.
- **안 댐**: 위 1~8번 보류 항목 전부(서버/DB/정책 결정 필요) + 보완 ②③④⑤(참고 수준·구흥모 쌤/기관 판단).
  a33·a34는 `이어짐_v4.html` 내부 로직만으로 완결(Supabase/서버 변경 없음).
