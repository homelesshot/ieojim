# 이어짐 작업기록 — 2026-09-14 (sunny님 세션)

> 오늘 한 일: (1) 적대적 리뷰 후속 마무리 점검, (2) 실행 대기 보안 SQL 점검(지뢰 발견),
> (3) **실사용·검증 데이터 수집 시스템 구축**(이어짐 사용 피드백 폼 + 구글시트 자동저장 + 케이스기록 집계),
> (4) 첫 가상 인터뷰 2건 데이터화. 코드·문서는 아래 커밋들로 이미 push됨.

---

## 1. 적대적 리뷰 후속 (앞 세션 a33~a35, push 완료)
- **a33** AI 추출 전 PII 사전 스크럽 + 건강화면 트라우마 안내
- **a34** 녹음 화면 "실명·주민번호는 말로 하지 마세요"(음성 우회 완화)
- **a35** XSS 감사 완료(악용 취약점 없음) + 심층방어 4건(escHtml 5종 통일 등)
- 상세: `보류항목_착수_체크리스트.md`(⑤ XSS 항목 등), v4 VERSION_LOG.

## 2. ⚠️ 실행 대기 보안 SQL 점검 결과 (중요 — 실행 전 확인 필요)
`이어짐_적대적리뷰_해결현황_2026-09-10.md`의 3개 SQL을 현재 코드와 대조 점검함:

- **[A] `touch_counselor_keys` search_path 고정** → 안전, 그대로 실행 가능
  (`alter function public.touch_counselor_keys() set search_path = public;`)
- **[B] 조직 admin 접근(is_admin + cases RLS)** → 스키마 일치(counselor_profiles.id, cases.counselor_id)
  확인, 비파괴적(본인 접근 유지+admin 추가). 실행 가능. 단 현재 사이드바 쿼리가 `counselor_id`로 필터라
  admin이 화면에서 남 사례를 바로 보진 못함(DB 권한만 열림).
- **[C] `decrypt_case_field` 소유권 검증 → 🚨 그대로 실행하면 저장·이어하기 깨질 위험**
  - 해결현황 doc의 함수가 `c.name_enc·address_enc·memo_enc·raw_transcript_enc·doc_text_enc` 5개 컬럼을
    참조하는데, **이 컬럼들은 클라이언트 코드 어디에도 없고 그 SQL 블록에만 등장**함. 클라는 `state_json_enc`만 사용.
  - 없는 컬럼이면 첫 복호화 호출에서 예외 → 서버키 폴백(v4.html ~1961행 decrypt RPC) 사례의 이어하기가 전부 실패.
  - **실행 전 반드시 컬럼 확인**: `select column_name from information_schema.columns where table_schema='public' and table_name='cases' and column_name like '%_enc';`
  - `state_json_enc` 하나뿐이면 함수의 OR 목록을 그 컬럼만 남기고 실행(안전 버전 제공함).
- **실행 순서**: [B] → [C] → [A]. (기존 `db/2026-09-09_보류항목_DB작업.sql` [C]의 REVOKE 버전은 위험하니 쓰지 말 것.)

## 3. 실사용·검증 데이터 수집 시스템 구축 (오늘 신규, push 완료)
심사 갭분석(`이어짐_심사기준_갭분석_2026-09-11.md`)이 지목한 "가장 비어있는 영역 = ②현장변화+③문제해결(70점)
= 실사용 데이터"를 채우기 위한 수집 파이프라인을 만듦.

- **`이어짐_검증기록_템플릿.md`** — 데이터화 마스터 문서
  - 표 A(케이스 기록), 양식 B(이어짐 사용 피드백), 집계 C, 결과물 위치 D
  - 부록 E(구글폼 문항 사양 + Apps Script 생성), 부록 F(HTML 양식→구글시트 자동저장)
  - **원칙: [가상](기능검증·①완성도) / [실사용](②③④ 근거) 라벨 분리, 숫자 합치지 않음**
- **`이어짐_피드백양식.html` / `feedback.html`** — 타기관용 모바일 피드백 폼(16문항)
  - 제출 → Apps Script 웹앱(doPost) → 구글 시트 "응답" 탭에 자동 누적(타기관 계정 불필요)
  - no-cors 전송, 실패 시 "복사해서 회신" 폴백으로 데이터 유실 방지
  - `feedback.html`은 한글 없는 ASCII 파일명 복사본(카톡 등 링크 깨짐 방지) — **배포는 이걸로**
- **케이스기록(sunny님 구글 시트 탭)** — sunny님 가상 인터뷰 객관 지표 기록
  - 헤더: 구분·날짜·시나리오·입력방식·이어짐소요(분)·AI자동_채운수·AI자동_전체·사람수정건수·확인필요건수·판정결과·완성섹션(/5)·가명화확인·비고
  - 집계 수식(자동 갱신): 케이스수 `=COUNT(G2:G)`, 자동화율 `=SUM(F2:F)/SUM(G2:G)`,
    정확도 `=1-SUM(H2:H)/SUM(F2:F)`, 평균소요 `=AVERAGE(E2:E)`, 대상가능비율 `=COUNTIF(J2:J,"대상가능")/COUNTA(J2:J)`
- **이어짐이 자동 판단하는 항목 수 = 17개**(AI 추출 스키마 기준: targetType·urgency·crisis·region·rural·
  livingType·size·children·ageGroup·gender·nationality·income·deposit·fin·debt·health·providedItems).
  케이스기록의 `AI자동_전체`는 17로 고정.

## 4. 첫 가상 인터뷰 데이터 (2건, [가상])
| 구분 | 시나리오 | 입력 | 소요(분) | AI채운/전체 | 사람수정 | 확인필요 | 판정 | 완성(/5) | 가명화 |
|---|---|---|---|---|---|---|---|---|---|
| [가상] | 거리노숙인 | 타이핑 | (모름) | 13/17 | 3 | 2 | 대상가능 | 4 | O |
| [가상] | 거리노숙인 | 녹음 | 1.5 | 11/17 | 6 | 0 | 대상가능 | 5 | O |

- 초기 집계: 자동화율 **70.6%**, 정확도 **62.5%**, 완성섹션 평균 **4.5/5**, 대상가능 **100%**, 가명화 **2/2 O**
- 관찰(n=2, 잠정): 타이핑(13/17·수정3)이 녹음(11/17·수정6)보다 자동채움 정확도가 높게 나옴 — STT 오차 가능성. 표본 더 필요.

## 5. 배포 링크
- 이어짐 프로토타입(최신 v4, a68): `https://homelesshot.github.io/ieojim/이어짐_v4.html`
- 이어짐 사용 피드백 폼(배포용): `https://homelesshot.github.io/ieojim/feedback.html`

## 6. 오후 작업 — 앱 수정·현장 이슈 진단 (2026-09-14)

**(A) 이어짐 사용 피드백 폼 → 구글시트 자동저장 최종 완성**
- 구글폼/스크립트가 안 돼서, 이어짐 스타일 HTML 폼(`feedback.html`)을 만들어 배포. 제출 시 시트 소유자(sunny님)의
  Apps Script 웹앱(doPost)으로 전송 → 구글 시트 "응답" 탭에 자동 누적(타기관 계정 불필요, no-cors).
- 한글 파일명이 카톡에서 링크 깨져서 ASCII 파일명 `feedback.html`로 배포. 브라우저 테스트로 실제 저장 확인 완료.
- `이어짐_검증기록_템플릿.md` 부록 F에 doPost 코드·배포 절차 정리.

**(B) 케이스기록 시트 집계 수식 세팅** — 케이스수 `=COUNT(G2:G)`, 자동화율 `=SUM(F)/SUM(G)`, 정확도
  `=1-SUM(H)/SUM(F)`, 평균소요 `=AVERAGE(E)`, 대상가능비율. (소요시간은 숫자(분)로 입력, "1분30초"→1.5)

**(C) 모바일 로그인 진단** — 시뮬레이션 폰으로 확인 결과 **앱은 정상**(Supabase 로드·로그인·localStorage
  모두 정상). 데스크탑은 되고 폰만 안 되는 건 **그 폰 브라우저 설정**(시크릿 모드/쿠키·사이트데이터 차단/카톡
  인앱 브라우저) 문제 → 일반창+쿠키허용+크롬·사파리 직접 열기로 해결. (이메일 인증 이슈 아님.)

**(D) 녹음 중 화면 꺼짐 → 자동 중단 = 의도된 안전장치(v0.50)** — 버그 아님. 화면이 백그라운드로 가면
  (visibilitychange) 무음·환각 방지를 위해 일부러 녹음을 멈춤. "재녹음 버튼"이 뜬 건 그 결과.
  진짜 문제는 "화면이 꺼진 것"(Wake Lock을 그 기기가 안 지킴) → 워크어라운드: **녹음 전 화면 자동꺼짐
  시간을 길게/안 함**. 중지 전까지 녹음분은 저장·변환됨. (녹음 트랙은 구흥모 쌤 영역 — 현장 피드백으로 공유.)

**(E) ✅ a67 — 타이핑 자동기입이 앞 단계에서 멈추면 뒤 단계에서 버려지던 문제 수정**
- 원인: runAutoFillStepsV4가 앞 단계에서 항목 null이면 return → 사람이 다음 누르면 go…()가 ex 없이 렌더 →
  AI가 뽑아둔 뒤 단계 값(region 인천 등)이 기본값(서울)으로 버려짐. 타이핑은 긴급성 자주 빠져 ①에서 자주 멈춤
  (녹음/타이핑 코드 경로는 동일 — 입력이 짧아 생긴 착시).
- 수정: `ex`를 `lastExtractionV4`에 보관 후 goHousehold/goHousehold2/goIncome/goHealth에서 이어 채움.
  로컬 재현 검증: 거주지역 인천·거주형태 거리노숙·가구1·60-69·남성·소득재산 모두 유지(수정 전엔 서울·빈값).

**(F) ✅ a68 — 밀린 월세·공과금·전기·수도세 등 연체액을 모두 부채로 합산**
- 현장(sunny님) 판단: 갚아야 주거·생계 유지되는 연체액은 채무. buildExtractionPrompt debt 설명 보강
  (연체 월세·공과금·전기/수도/가스 합산, "세 달치"처럼 기간 나오면 월액×개월수). 재산 산정 인정·최종 판정은
  기관 심사라는 단서 병기(면책 유지).
- 로컬 검증: "월29만 3달 밀림 + 전기 11만" → 부채 290,000 → **980,000** 정확 합산.

**(G) 단전·단수(공급 중단) 처리 — 결정: 현행 유지(option 3)** — 위기사유 목록은 그대로 두고, 공급 중단
  상황은 긴급성=즉시로 자동 반영 + 위기사유는 상담원이 직접 선택. (판정 목록 CRISIS_LIST 변경 안 함.)

## 7. 다음 할 일
- [ ] 가상 케이스 **5~10건**으로 확대(시나리오 다양화) — 소요시간 꼭 숫자(분)로
- [ ] 타기관에 `feedback.html` 배포 → `응답` 탭 실사용 데이터 확보(2~3곳 목표)
- [ ] 보안 SQL: `cases`의 `%_enc` 컬럼 확인 후 [C] 확정 → [B]/[C]/[A] 실행(구흥모 쌤)
- [ ] 데이터 몇 건 쌓이면 → 발표용 집계표 + Before/After 막대로 정리
- [ ] (구흥모 쌤 영역) 녹음 화면꺼짐 시 Wake Lock이 일부 기기에서 안 먹히는 현장 피드백 검토
