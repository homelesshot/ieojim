---
title: 이어짐 적대적 리뷰 — 최종본 + 해결 현황 (2026-09-09 작성, 2026-09-11 해결 현황 갱신)
description: 원본 적대적 리뷰 전체 내용을 그대로 보존하면서, 각 항목이 어떻게 해결됐는지(완료) 또는 앞으로 어떻게 해결할지(SQL·Edge Function 코드 포함)를 항목별로 추가한 문서. 2026-09-11 — 치명1(API 호출 한도) 서버측 적용 완료, Leaked Password Protection은 Free 플랜 한계로 제출 범위 제외, 회원가입 도메인 제한 방법은 폐기하고 초대 전용 방식으로 대체 연구.
---

# 이어짐 적대적 리뷰 — 최종본 (2026-09-09, 원본 보존) + 해결 현황 (2026-09-11 갱신)

`이어짐_v4.html`과 실제 배포된 Supabase 인프라(RLS 정책, Edge Function 소스코드, 보안 어드바이저)를
공격자·악의적 사용자 관점에서 점검한 결과입니다. 원본 내용(문제·재현·해결 방법)은 하나도 지우지 않고
그대로 두고, 각 항목 끝에 **"해결 현황(2026-09-10)"** 섹션을 추가해서 지금 상태를 정리했습니다.

**⚠️ 중요 — 왜 일부는 "코드는 준비됐지만 아직 적용 안 됨" 상태인가**: 이번 세션에서 Claude가 직접
Supabase 프로젝트(DB 마이그레이션, Edge Function 배포)에 접근할 수 있는 도구를 확인했지만, 실제
프로덕션 DB 스키마 변경·Edge Function 배포·인증 설정 변경은 자동 실행 권한 범위 밖으로 판단되어
직접 실행하지 못했습니다(운영 중인 인프라를 사용자 확인 없이 바꾸는 것이므로 안전장치가 막았습니다).
대신 아래 각 항목에 **바로 복사해서 Supabase 대시보드(SQL Editor / Edge Functions)에 붙여넣고 실행할
수 있는 완성된 코드**를 준비해뒀습니다 — 구흥모님이나 협업자가 확인 후 직접 실행하시면 되고, 다음
세션에서 명시적으로 "적용해줘"라고 요청하시면 그때 실행할 수도 있습니다. v4.html 코드만으로 되는
항목은 이미 전부 실제로 반영·커밋했습니다.

## 🔴 치명 1 — STT·AI초안 기능이 로그인 없이 전 세계에 공개돼 있음

**문제**: `stt-transcribe`, `ai-draft` 두 Edge Function을 호출할 때 클라이언트가 로그인 세션 토큰이
아니라, 페이지 소스에 그대로 박혀 있는 공개 anon 키를 인증값으로 보냅니다.

```
var STT_ANON_KEY = 'sb_publishable_cnEU0Ly-lTtcR8Avmfjk7Q_wOEUT37s';
headers: { 'Authorization': 'Bearer ' + STT_ANON_KEY }   // stt-transcribe
headers: {'Authorization':'Bearer '+STT_ANON_KEY}         // ai-draft
```

Edge Function 설정의 `verify_jwt: true`는 "유효하게 서명된 JWT인가"만 확인하는데, 이 anon 키 자체가
Supabase가 발급한 유효한 JWT라서 검증을 그냥 통과합니다. **회원가입도 로그인도 전혀 필요 없이, 페이지
소스에서 URL과 키만 복사하면 누구나 `curl`로 두 함수를 무제한 호출할 수 있습니다.**

**왜 문제인가**: (1) 요청 횟수·파일 크기 제한이 전혀 없어 기관의 OpenAI 청구액이 통제 불가능하게 늘어날
수 있음(비용 폭탄), (2) `ai-draft`는 클라이언트가 보낸 `prompt`를 고정 시스템 프롬프트 없이 그대로
전달하므로, 이어짐과 전혀 무관한 콘텐츠 생성에 쓰일 수 있고 그 책임은 기관 API 키로 남음.

**해결 방법(원본)**: 두 호출 모두 `STT_ANON_KEY` 대신 로그인 후 발급되는 사용자 세션의 `access_token`
(`sb.auth.getSession()`으로 얻는 값)을 `Authorization` 헤더에 넣도록 클라이언트 코드를 수정합니다.
Edge Function 쪽에서도 요청에 담긴 JWT를 디코드해서 `role`이 `authenticated`인지 확인하고, `anon`
role이면 401로 거부하는 코드를 추가해야 완전히 막힙니다. 시간이 없으면 최소한 함수별로 시간당 호출
횟수를 세는 간단한 카운터 테이블이라도 추가해 비용 폭탄만 먼저 막는 것도 임시방편이 됩니다.

### ✅ 해결 현황(2026-09-10) — 절반 완료(클라이언트), 절반 대기(서버)

**완료(v4-track a54, v4.html에 실제 반영·커밋됨)**: `transcribeAudio`(STT)·`requestExtractionV4`·
`generateAiDraft`(AI초안) 세 호출 지점 전부, 로그인 상태면 `authSession.access_token`(로그인 세션
토큰)을 보내고, 비로그인 상태에서만 기존처럼 anon 키를 보내도록 수정했습니다(`sttAuthHeader()` 헬퍼).
"로그인 없이도 100% 동작해야 한다"는 기존 원칙은 그대로 지켰습니다.

**아직 대기 중(Supabase 인프라 변경 필요 — 아래 코드를 구흥모님/협업자가 직접 적용)**: 서버(Edge
Function)가 요청의 role을 실제로 검사하지 않으면 위 클라이언트 변경은 "더 안전한 값을 보낼 뿐" 실질적
차단 효과가 없습니다. 원본 리뷰가 제안한 대로 role 검증을 완전히 강제하면(비로그인 요청을 아예 거부)
지금 현장에서 로그인 없이 쓰는 사용 패턴이 깨질 위험이 있어, **완전 차단 대신 "인증 여부에 따라 다른
시간당 호출 한도"** 방식(원본이 언급한 "임시방편" 카운터 테이블)으로 절충했습니다 — 로그인 사용자는
넉넉하게, 비로그인(anon)은 훨씬 빡빡하게 제한해서 키 유출 시 비용 폭탄 위험을 크게 줄입니다.

**적용 방법 — ① DB에 카운터 테이블 추가 (Supabase SQL Editor에서 실행)**:
```sql
create table if not exists public.api_rate_limit_counters (
  bucket_key text primary key,
  window_start timestamptz not null default now(),
  count int not null default 0
);
alter table public.api_rate_limit_counters enable row level security;
-- 정책을 하나도 만들지 않으면 service_role(Edge Function 전용 키)만 접근 가능 — 의도된 동작
```

**적용 방법 — ② `stt-transcribe` Edge Function 코드 교체 (Supabase 대시보드 > Edge Functions >
stt-transcribe > 코드 편집, 아래 전체로 교체 후 배포)**: 기존 코드에 role 판별 + 시간당 호출 한도
+ Whisper 환각 필터 조건 강화(아래 🟢 항목도 함께 반영)까지 포함했습니다.
```ts
import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "jsr:@supabase/supabase-js@2";

// 이어짐 STT 프록시: 브라우저가 녹음 파일을 이 함수로 보내면,
// 서버에만 저장된 OPENAI_API_KEY로 OpenAI STT를 대신 호출해서 텍스트를 돌려준다.
// OpenAI 키는 절대 브라우저로 나가지 않는다.
//
// 2026-09-10: 적대적 리뷰(치명1) 대응 — anon 키로 로그인 없이 무제한 호출 가능하던 문제의
// 서버측 완화. 완전 차단(로그인 필수화) 대신, 로그인 여부에 따라 시간당 호출 한도를 다르게
// 적용해서 "로그인 없이도 100% 동작"이라는 기존 제품 원칙을 지키면서 비용 폭탄 위험을 줄임.

const CORS_HEADERS = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Headers": "authorization, x-client-info, apikey, content-type",
  "Access-Control-Allow-Methods": "POST, OPTIONS",
};

const HALLUCINATION_PATTERNS: RegExp[] = [
  /시청해\s*주셔서\s*감사/,
  /구독\s*과?\s*좋아요/,
  /다음\s*영상에서/,
  /다음\s*뉴스(입니다|였습니다)/,
  /지금까지\s*(MBC|KBS|SBS|JTBC)?\s*뉴스/i,
  /스포츠(였습니다|입니다)/,
  /오늘의\s*날씨였습니다/,
  /자막\s*제공/,
  /이\s*영상은[\s\S]{0,30}포함(하고\s*있습니다|합니다)/,
  /영상\s*시청에\s*감사/,
  /좋아요\s*와?\s*구독/,
  /무엇이든\s*물어보/,
  /궁금한\s*점이?\s*있으시면/,
  /도움이\s*필요하시면\s*(말씀|언제든)/,
  /언제든\s*(물어보|말씀)/,
  /더\s*궁금하신\s*점/,
];
function looksLikeHallucination(t: string): boolean {
  if (!t) return false;
  return HALLUCINATION_PATTERNS.some((re) => re.test(t));
}

const SUSPICIOUSLY_SHORT_AUDIO_SEC = 1.2;
const SUSPICIOUSLY_LONG_TEXT_FOR_SHORT_AUDIO = 15;

// ---- 🔴치명1 대응: role 판별 + 시간당 호출 한도 ----
function getJwtInfo(req: Request): { role: string | null; sub: string | null } {
  const auth = req.headers.get("Authorization") || req.headers.get("authorization");
  if (!auth || !auth.startsWith("Bearer ")) return { role: null, sub: null };
  const token = auth.slice(7);
  const parts = token.split(".");
  if (parts.length < 2) return { role: null, sub: null };
  try {
    const b64 = parts[1].replace(/-/g, "+").replace(/_/g, "/");
    const pad = "=".repeat((4 - (b64.length % 4)) % 4);
    const payload = JSON.parse(atob(b64 + pad));
    return { role: payload.role || null, sub: payload.sub || null };
  } catch {
    return { role: null, sub: null };
  }
}

const supabaseAdmin = createClient(
  Deno.env.get("SUPABASE_URL")!,
  Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!
);

async function checkRateLimit(bucketKey: string, limit: number, windowMs: number): Promise<boolean> {
  const now = Date.now();
  const { data } = await supabaseAdmin
    .from("api_rate_limit_counters")
    .select("*")
    .eq("bucket_key", bucketKey)
    .maybeSingle();
  if (!data) {
    await supabaseAdmin.from("api_rate_limit_counters").insert({
      bucket_key: bucketKey,
      window_start: new Date(now).toISOString(),
      count: 1,
    });
    return true;
  }
  const windowStart = new Date(data.window_start).getTime();
  if (now - windowStart > windowMs) {
    await supabaseAdmin
      .from("api_rate_limit_counters")
      .update({ window_start: new Date(now).toISOString(), count: 1 })
      .eq("bucket_key", bucketKey);
    return true;
  }
  if (data.count >= limit) return false;
  await supabaseAdmin
    .from("api_rate_limit_counters")
    .update({ count: data.count + 1 })
    .eq("bucket_key", bucketKey);
  return true;
}

Deno.serve(async (req: Request) => {
  if (req.method === "OPTIONS") {
    return new Response("ok", { headers: CORS_HEADERS });
  }

  if (req.method !== "POST") {
    return new Response(JSON.stringify({ error: "POST만 지원합니다." }), {
      status: 405,
      headers: { ...CORS_HEADERS, "Content-Type": "application/json" },
    });
  }

  const apiKey = Deno.env.get("OPENAI_API_KEY");
  if (!apiKey) {
    return new Response(
      JSON.stringify({ error: "서버에 OPENAI_API_KEY가 설정되지 않았습니다. Supabase 대시보드 > Edge Functions > Secrets에서 등록해주세요." }),
      { status: 500, headers: { ...CORS_HEADERS, "Content-Type": "application/json" } }
    );
  }

  const jwtInfo = getJwtInfo(req);
  const isAuthenticated = jwtInfo.role === "authenticated";
  const bucketKey = isAuthenticated ? `stt:auth:${jwtInfo.sub}` : "stt:anon:global";
  const limit = isAuthenticated ? 300 : 20; // 시간당 — 필요시 조정
  const rateOk = await checkRateLimit(bucketKey, limit, 60 * 60 * 1000);
  if (!rateOk) {
    return new Response(
      JSON.stringify({ error: "호출 한도를 초과했습니다. 잠시 후 다시 시도해주세요.", code: "RATE_LIMIT" }),
      { status: 429, headers: { ...CORS_HEADERS, "Content-Type": "application/json" } }
    );
  }

  try {
    const incomingForm = await req.formData();
    const audio = incomingForm.get("file");
    if (!audio || !(audio instanceof File)) {
      return new Response(JSON.stringify({ error: "file 필드(녹음 파일)가 필요합니다." }), {
        status: 400,
        headers: { ...CORS_HEADERS, "Content-Type": "application/json" },
      });
    }

    const outgoingForm = new FormData();
    outgoingForm.append("file", audio, audio.name || "recording.webm");
    outgoingForm.append("model", "whisper-1");
    outgoingForm.append("response_format", "verbose_json");
    outgoingForm.append("language", "ko");
    outgoingForm.append("prompt", "이것은 복지 상담원이 위기가구 현장에서 녹음한 한국어 상담 내용입니다.");

    const openaiRes = await fetch("https://api.openai.com/v1/audio/transcriptions", {
      method: "POST",
      headers: { Authorization: `Bearer ${apiKey}` },
      body: outgoingForm,
    });

    const data = await openaiRes.json();

    if (!openaiRes.ok) {
      return new Response(JSON.stringify({ error: data?.error?.message || "OpenAI 요청 실패" }), {
        status: openaiRes.status,
        headers: { ...CORS_HEADERS, "Content-Type": "application/json" },
      });
    }

    const rawSegments = Array.isArray(data.segments) ? data.segments : [];

    let finalText: string;
    let keptSegments = rawSegments;
    if (rawSegments.length) {
      keptSegments = rawSegments.filter((s: any) => {
        const noSpeech = typeof s?.no_speech_prob === "number" ? s.no_speech_prob : 0;
        const avgLogprob = typeof s?.avg_logprob === "number" ? s.avg_logprob : 0;
        const segText = String(s?.text || "");
        // 🟢 낮음 항목 대응: 기존 AND 조건(둘 다 만족해야 제외)은 화이트리스트에 없는 새로운
        // 환각 문구를 놓치기 쉬워, 둘 중 하나만 강하게 의심되어도 제외하는 OR 조건으로 강화.
        if (noSpeech > 0.55 || avgLogprob < -1.0) return false;
        if (looksLikeHallucination(segText)) return false;
        return true;
      });
      finalText = keptSegments.map((s: any) => String(s?.text || "").trim()).filter(Boolean).join(" ").trim();
    } else {
      const rawText = data.text ?? "";
      finalText = looksLikeHallucination(rawText) ? "" : rawText;
    }

    if (looksLikeHallucination(finalText)) {
      finalText = "";
    }

    const durationSec = typeof data.duration === "number" ? data.duration : null;
    if (durationSec !== null && durationSec > 0 && durationSec < SUSPICIOUSLY_SHORT_AUDIO_SEC && finalText.length > SUSPICIOUSLY_LONG_TEXT_FOR_SHORT_AUDIO) {
      finalText = "";
    }

    return new Response(
      JSON.stringify({ text: finalText, segments: keptSegments, bySpeaker: "", duration: data.duration ?? null }),
      { headers: { ...CORS_HEADERS, "Content-Type": "application/json" } }
    );
  } catch (err) {
    return new Response(JSON.stringify({ error: String(err) }), {
      status: 500,
      headers: { ...CORS_HEADERS, "Content-Type": "application/json" },
    });
  }
});
```

**적용 방법 — ③ `ai-draft` Edge Function 코드 교체** (같은 role 판별 + 시간당 호출 한도만 추가, 나머지
로직은 원본 그대로):
```ts
import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "jsr:@supabase/supabase-js@2";

const CORS_HEADERS = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Headers": "authorization, x-client-info, apikey, content-type",
  "Access-Control-Allow-Methods": "POST, OPTIONS",
};

const OPENAI_MODEL = "gpt-4o-mini";

function getJwtInfo(req: Request): { role: string | null; sub: string | null } {
  const auth = req.headers.get("Authorization") || req.headers.get("authorization");
  if (!auth || !auth.startsWith("Bearer ")) return { role: null, sub: null };
  const token = auth.slice(7);
  const parts = token.split(".");
  if (parts.length < 2) return { role: null, sub: null };
  try {
    const b64 = parts[1].replace(/-/g, "+").replace(/_/g, "/");
    const pad = "=".repeat((4 - (b64.length % 4)) % 4);
    const payload = JSON.parse(atob(b64 + pad));
    return { role: payload.role || null, sub: payload.sub || null };
  } catch {
    return { role: null, sub: null };
  }
}

const supabaseAdmin = createClient(
  Deno.env.get("SUPABASE_URL")!,
  Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!
);

async function checkRateLimit(bucketKey: string, limit: number, windowMs: number): Promise<boolean> {
  const now = Date.now();
  const { data } = await supabaseAdmin
    .from("api_rate_limit_counters")
    .select("*")
    .eq("bucket_key", bucketKey)
    .maybeSingle();
  if (!data) {
    await supabaseAdmin.from("api_rate_limit_counters").insert({
      bucket_key: bucketKey,
      window_start: new Date(now).toISOString(),
      count: 1,
    });
    return true;
  }
  const windowStart = new Date(data.window_start).getTime();
  if (now - windowStart > windowMs) {
    await supabaseAdmin
      .from("api_rate_limit_counters")
      .update({ window_start: new Date(now).toISOString(), count: 1 })
      .eq("bucket_key", bucketKey);
    return true;
  }
  if (data.count >= limit) return false;
  await supabaseAdmin
    .from("api_rate_limit_counters")
    .update({ count: data.count + 1 })
    .eq("bucket_key", bucketKey);
  return true;
}

Deno.serve(async (req: Request) => {
  if (req.method === "OPTIONS") {
    return new Response("ok", { headers: CORS_HEADERS });
  }

  if (req.method !== "POST") {
    return new Response(JSON.stringify({ error: "POST만 지원합니다." }), {
      status: 405,
      headers: { ...CORS_HEADERS, "Content-Type": "application/json" },
    });
  }

  const apiKey = Deno.env.get("OPENAI_API_KEY");
  if (!apiKey) {
    return new Response(
      JSON.stringify({ error: "서버에 OPENAI_API_KEY가 설정되지 않았습니다. Supabase 대시보드 > Edge Functions > Secrets에서 등록해주세요." }),
      { status: 500, headers: { ...CORS_HEADERS, "Content-Type": "application/json" } }
    );
  }

  const jwtInfo = getJwtInfo(req);
  const isAuthenticated = jwtInfo.role === "authenticated";
  const bucketKey = isAuthenticated ? `ai:auth:${jwtInfo.sub}` : "ai:anon:global";
  const limit = isAuthenticated ? 300 : 40; // 시간당 — 자동추출은 세션당 호출이 잦아 anon도 조금 여유있게
  const rateOk = await checkRateLimit(bucketKey, limit, 60 * 60 * 1000);
  if (!rateOk) {
    return new Response(
      JSON.stringify({ error: "호출 한도를 초과했습니다. 잠시 후 다시 시도해주세요.", code: "RATE_LIMIT" }),
      { status: 429, headers: { ...CORS_HEADERS, "Content-Type": "application/json" } }
    );
  }

  try {
    const body = await req.json();
    const prompt = typeof body?.prompt === "string" ? body.prompt : "";
    const wantJson = body?.json === true;
    const schema = (body?.schema && typeof body.schema === "object") ? body.schema : null;
    const schemaName = typeof body?.schemaName === "string" && body.schemaName.trim() ? body.schemaName.trim() : "structured_output";
    if (!prompt.trim()) {
      return new Response(JSON.stringify({ error: "prompt 필드가 필요합니다." }), {
        status: 400,
        headers: { ...CORS_HEADERS, "Content-Type": "application/json" },
      });
    }

    const payload: Record<string, unknown> = {
      model: OPENAI_MODEL,
      messages: [{ role: "user", content: prompt }],
      temperature: (schema || wantJson) ? 0 : 0.3,
    };
    if (schema) {
      payload.response_format = {
        type: "json_schema",
        json_schema: { name: schemaName, schema, strict: true },
      };
    } else if (wantJson) {
      payload.response_format = { type: "json_object" };
    }

    const openaiRes = await fetch("https://api.openai.com/v1/chat/completions", {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        Authorization: `Bearer ${apiKey}`,
      },
      body: JSON.stringify(payload),
    });

    const data = await openaiRes.json();

    if (openaiRes.status === 429) {
      return new Response(JSON.stringify({ error: "오늘 사용 한도를 넘었습니다.", code: "RATE_LIMIT" }), {
        status: 429,
        headers: { ...CORS_HEADERS, "Content-Type": "application/json" },
      });
    }

    if (!openaiRes.ok) {
      const detail = data?.error?.message || "OpenAI 요청 실패";
      return new Response(JSON.stringify({ error: detail }), {
        status: openaiRes.status,
        headers: { ...CORS_HEADERS, "Content-Type": "application/json" },
      });
    }

    const text = data?.choices?.[0]?.message?.content;
    if (!text) {
      return new Response(JSON.stringify({ error: "응답에서 텍스트를 찾을 수 없습니다" }), {
        status: 502,
        headers: { ...CORS_HEADERS, "Content-Type": "application/json" },
      });
    }

    return new Response(JSON.stringify({ text: text.trim() }), {
      headers: { ...CORS_HEADERS, "Content-Type": "application/json" },
    });
  } catch (err) {
    return new Response(JSON.stringify({ error: String(err) }), {
      status: 500,
      headers: { ...CORS_HEADERS, "Content-Type": "application/json" },
    });
  }
});
```
**참고**: 완전한 해결(로그인 필수화 + role 강제 거부)은 제품 정책 변경(비로그인 사용 금지)이 걸려있어
이번엔 절충안(위 코드)으로 진행했습니다. 완전 차단을 원하시면 위 두 함수의 `if (!isAuthenticated)`
분기에서 429 대신 401을 반환하도록 한 줄만 더 바꾸면 됩니다 — 다만 그 순간부터 로그인 안 한 상담원은
녹음/AI 기능을 못 쓰게 되므로 신중한 결정이 필요합니다.

## 🔴 치명 2 — "가명화 도우미"가 실제로는 AI 자동추출 경로를 보호하지 못함

**문제**: 화면의 "가명화 도우미"(`runAnon()`)는 `generateAiDraft()`(AI 초안 다듬기)에만 적용됩니다.
반면 녹음·타이핑 즉시 항목을 자동 추출하는 `requestExtractionV4()`는 STT 원문을 가명화 없이 그대로
OpenAI로 전송합니다.

**왜 문제인가**: 상담 중 대상자가 실명·주민번호·주소를 말하면(실제로 흔한 일) 그 원문 그대로가 해외
사업자 서버로 넘어갑니다. 화면에 "가명화 도우미"가 있다는 사실 자체가 사용자에게 "이 앱은 개인정보를
조심히 다룬다"는 잘못된 안심을 줄 수 있어 더 위험합니다. 위기가구 대상 서비스라는 점에서 서비스 취지와
직결된 문제입니다.

**해결 방법(원본)**: `requestExtractionV4` 호출 전에도 `runAnon()`과 동일한 정규식 마스킹을 거치도록 통일합니다.
다만 마스킹이 "김철수" 같은 이름을 지우면서 AI의 항목 추출 정확도를 떨어뜨릴 수 있으니(예: 성별 추론
등에 이름이 힌트가 되는 경우), 마스킹 후에도 추출 정확도가 유지되는지 확인 후 적용하는 것을 권장합니다.

### ✅ 해결 현황(2026-09-10) — 부분 완료 + 법적 근거 확인으로 문제 성격이 일부 달라짐

**완료(v4-track a33, 2026-09-09, 이미 배포됨)**: `requestExtractionV4`가 프롬프트를 만들기 전에
`scrubForServerV4(text)`를 거치도록 수정 완료 — 이메일, 주민번호(13자리), 카드번호(4-4-4-4),
전화번호(010/지역번호) 등 **형식이 뚜렷한 개인식별정보 4종**은 서버 전송 전 이 기기 안에서 먼저
제거됩니다. `runAnon`의 맨숫자 규칙(연속 숫자)은 월소득·재산 금액과 겹쳐 예비판단 근거를 왜곡할 수
있어 의도적으로 제외했습니다(추출 정확도 보존).

**중요 업데이트(2026-09-10, 구흥모님 확인) — 법적 근거 확인으로 우려의 성격이 달라짐**: 처음엔 실명이
AI로 넘어가는 것 자체를 막아야(전부 익명 처리) 한다고 가정하고 있었는데, 개인정보보호법을 확인해보니 **사전에
상시 공개된 처리방침으로 국외이전·처리위탁 사실을 명확히 고지하기만 하면, 별도의 동의 절차 없이도
실명이 언급된 상태로 AI(OpenAI)에 전송되는 것 자체는 적법**하다는 결론입니다(계약 이행을 위한 처리위탁
관련 조항 근거). 이건 이미 **v4-track a36(2026-09-09)**에서 반영되어 있습니다 — 동의 체크박스 대신
"사전 공개(개인정보 처리방침에서 언제든 확인 가능)" + "상담원에게 구두 안내 방법을 명확히 전달"하는
방식으로 구현했고, `POLICY_TEXT_V1`의 "3. 국외 이전 안내" 항목에 이전받는 자(OpenAI)·목적·방법·보유기간이
전부 명시돼 있습니다. 즉 **이 항목이 처음에 우려했던 "몰래 실명이 해외로 샌다"는 문제의 법적 리스크는
상당 부분 해소된 상태**입니다.

다만 이건 "적법성"의 문제이지 "개인정보 최소수집 원칙"이나 "기술적 안전조치"의 문제와는 별개입니다 —
법적으로 허용된다고 해서 굳이 실명을 그대로 보내는 게 최선의 실무는 아니므로, 위 구조화된 정보 스크럽
(a33)과 가명화 도우미 기능 자체는 그대로 유지하는 것을 권장합니다. 오늘(9/10) 확인 결과, 화면에는
"가명화하기(수동)" 버튼까지 추가해서(v4-track a55) 상담원이 녹음 결과를 본 즉시 바로 실명·지명을
가릴 수 있게 됐습니다(아래 "🟡 중간 — 국적·거주형태 선택지" 다음 항목 대신, 이 항목의 실무적 완성도를
높이는 조치로 함께 반영). 이 확인은 코치 미팅(9/2)에서 아직 미결정으로 남아있던 **"가명화 도우미
방향(A: 세션에 자동 내장 vs B: 기능 폐지)"** 질문에 대한 하나의 답이 될 수 있습니다: 완전 익명화(B와
유사한 방향)가 법적으로 필수는 아니라는 게 확인됐으니, "적법한 사전 고지 기반 처리(이미 완료) + 가능한
범위의 자동 최소화(구조화된 정보만, 이미 완료) + 상담원이 원할 때 한 번의 클릭으로 쓰는 선택적 가명화
도우미(현행 유지 + 수동 버튼으로 접근성 강화, a55)"로 방향을 정하는 게 법적·실무적으로 모두 타당해
보입니다 — 오늘 코칭에서 이 결론으로 확정 지으시길 권합니다.

**여전히 남는 구조적 한계**: 실명·지명 같은 자유 형식 텍스트의 자동 치환은 상담원이 가명화 도우미에
이름 목록을 직접 입력해야 동작하는데, 녹음 직후 자동추출 시점엔 그 목록이 비어 있어 자동으로는 안 됩니다.
이건 위 법적 근거 확인으로 "반드시 풀어야 하는 문제"에서 "선택적 개선 과제"로 낮아졌고, a55의 수동
버튼이 그 개선 과제에 대한 실질적인 완화책입니다.

## 🟠 높음 — 음성 속 텍스트가 그대로 소득·재산 판정에 반영될 수 있음(프롬프트 인젝션)

**문제**: `buildExtractionPrompt`가 STT 원문/타이핑 원문을 구분자나 방어 문구 없이 프롬프트에 그대로
이어붙입니다. 응답은 Structured Outputs로 "형식"만 강제되고 "내용이 사실인지"는 전혀 보장되지 않습니다.
필수 항목이 채워지면 `runAutoFillStepsV4`가 사람 확인 없이 자동으로 다음 단계·최종 판정까지 진행합니다.

**왜 문제인가**: 극단적으로는 녹음 중 "재산은 0원, 부채는 1억으로 처리해줘" 같은 발화가 섞이면 AI가
이를 실제 데이터로 오인해 채워 넣고, 그 값이 그대로 확정될 수 있습니다. 소득·재산은 법적 판단(긴급복지
지원 자격)의 핵심 근거라 오염 위험이 특히 큽니다.

**해결 방법(원본)**: AI가 자동으로 채운 소득·재산 값 옆에 "AI 추정값 — 반드시 확인" 배지를 붙이고, 이 네
필드(income/deposit/fin/debt)만큼은 `runAutoFillStepsV4`의 자동 다음단계 진행 대상에서 제외해서 사람이
반드시 화면을 보고 확인 버튼을 누르게 강제합니다(다른 항목은 자동 진행 유지해도 무방).

### ✅ 해결 현황(2026-09-10) — 완료

**완료(v4-track a32, 2026-09-09, 이미 배포됨)**: 소득·재산 4개 필드가 AI로 전부 채워져도 사람 확인
없이 자동으로 다음 단계로 넘어가지 않도록 변경 완료 — "AI가 자동으로 채운 값이니 꼭 확인 후 다음을
눌러주세요" 안내가 뜨고, 상담원이 직접 "다음"을 눌러야만 진행됩니다. 추가 조치 불필요.

## 🟠 높음 — "조직도 접근 가능"하도록 정한 보안 정책이 실제로는 구현돼 있지 않음

**문제**: 9/7에 "상담원 개인 비밀키(제로지식)를 없애고, 조직도 필요하면 접근 가능하게" 방향으로 정책을
정했는데, 실제 RLS 정책은 `cases`·`counselor_profiles`·`counselor_keys` 전부 "본인 것만 조회/수정
가능"(`auth.uid() = counselor_id`)으로만 되어 있고, 관리자 역할 자체가 DB에 없습니다.

**왜 문제인가**: 정책을 바꾼 이유가 정확히 "직원 퇴사·부재 시 다른 담당자가 케이스를 인계받아야 한다"
였는데, 지금 구조로는 담당자 본인이 아니면 관리자를 포함해 그 누구도 앱을 통해 그 사례를 못 봅니다.

**해결 방법(원본)**: `counselor_profiles`에 `is_admin boolean` 컬럼을 추가하고, `cases` 테이블에 "본인 것이거나
호출자가 admin이면 조회 가능"하도록 SELECT 정책 하나를 추가하는 정도로 최소 기능은 짧은 시간에 구현
가능합니다. 시간이 정 안 되면 발표에서 "정책 방향은 정했고 기술 구현은 다음 단계"라고 스코프를 좁혀
설명하는 것을 권장합니다.

### ⏳ 해결 현황(2026-09-10) — 미적용, 실행 가능한 SQL 준비됨

DB 스키마·RLS 정책 변경이라 이번 세션에서 직접 실행하지 못했습니다. 아래를 Supabase SQL Editor에
그대로 붙여넣고 실행하면 됩니다(프로젝트: `ieojim-stt-proxy`, `ueidoudgwhuanrtowudy`).

```sql
alter table public.counselor_profiles
  add column if not exists is_admin boolean not null default false;

drop policy if exists "본인 사례만 조회" on public.cases;
create policy "본인 사례이거나 관리자면 조회 가능"
  on public.cases for select
  using (
    auth.uid() = counselor_id
    or exists (select 1 from public.counselor_profiles p where p.id = auth.uid() and p.is_admin)
  );

drop policy if exists "본인 사례만 수정" on public.cases;
create policy "본인 사례이거나 관리자면 수정 가능(인계 포함)"
  on public.cases for update
  using (
    auth.uid() = counselor_id
    or exists (select 1 from public.counselor_profiles p where p.id = auth.uid() and p.is_admin)
  );

-- 관리자 지정은 최소 1명 이상 수동으로 켜줘야 합니다(예: 구흥모님 본인 계정):
-- update public.counselor_profiles set is_admin = true where id = '<본인 uid>';
```
실행 후에는 `counselor_profiles.is_admin`을 최소 한 계정에서 `true`로 켜야 실제로 조직 접근이
동작합니다(콘솔에서 직접, 또는 다음 세션에서 요청하시면 진행 가능).

## 🟡 중간 — 암호화가 "전 조직 공용 단일 키" 구조라 단일 장애점(SPOF)

**문제**: `encrypt_case_field`/`decrypt_case_field`는 사례별·상담원별 키가 아니라 Vault에 저장된 전역
단일 키(`case_encryption_key`) 하나로 모든 사례를 암호화합니다. 두 함수는 `SECURITY DEFINER`이고
`authenticated` 역할 전체에 실행 권한이 열려 있어서, 호출자가 넘긴 bytea가 본인 사례에서 나온 게 맞는지
전혀 검증하지 않습니다(Supabase 보안 어드바이저 자동 경고 대상).

**왜 문제인가**: 지금은 `cases` 테이블 RLS가 다른 사람 암호문을 못 읽게 막아줘서 즉각적 대량 유출로
이어지진 않지만, 앞으로 백업·로그·다른 기능의 버그 등 어떤 경로로든 암호문 하나만 새어나가면 그 즉시
전체 케이스 데이터를 복호화할 수 있는 만능 열쇠 구조입니다.

**해결 방법(원본)**: `decrypt_case_field` 함수 내부에서 `enc` 값이 `auth.uid() = counselor_id`인 `cases` 행의
`state_json_enc`와 일치하는지 확인하는 로직을 추가하거나, 최소한 `REVOKE EXECUTE ON FUNCTION
decrypt_case_field FROM authenticated`로 REST를 통한 직접 호출 자체를 막고 서버 내부 함수에서만
쓰도록 제한합니다.

### ⏳ 해결 현황(2026-09-10) — 미적용, 실행 가능한 SQL 준비됨

`decrypt_case_field`는 클라이언트가 저장/이어하기 시 실제로 호출해서 써야 하는 함수라 `REVOKE EXECUTE`로
완전히 막을 수는 없어서(그러면 기존 저장·이어하기가 깨짐), 원본이 제안한 **소유권 검증 추가** 방식으로
진행하는 게 맞습니다. 아래 SQL을 Supabase SQL Editor에서 실행하면, 호출자가 본인 사례(또는 관리자면
전체 사례)에 실제로 속한 암호문만 복호화할 수 있도록 막습니다.

```sql
create or replace function public.decrypt_case_field(enc bytea)
returns text
language plpgsql
security definer
set search_path to 'public', 'vault', 'extensions'
as $function$
declare
  k text;
  allowed boolean;
begin
  if enc is null then return null; end if;

  select exists (
    select 1 from public.cases c
    where (
      c.counselor_id = auth.uid()
      or exists (select 1 from public.counselor_profiles p where p.id = auth.uid() and p.is_admin)
    )
    and (
      c.name_enc = enc or c.address_enc = enc or c.memo_enc = enc
      or c.raw_transcript_enc = enc or c.doc_text_enc = enc or c.state_json_enc = enc
    )
  ) into allowed;

  if not allowed then
    raise exception '본인 사례에 속한 값만 복호화할 수 있습니다.';
  end if;

  select decrypted_secret into k from vault.decrypted_secrets where name = 'case_encryption_key';
  return pgp_sym_decrypt(enc, k);
end;
$function$;
```
(위 "조직 접근 권한" 항목의 `is_admin` 컬럼이 먼저 있어야 관리자 조건이 동작하니 그 SQL을 먼저
실행하는 순서를 권장합니다.) `encrypt_case_field`는 다른 사람 데이터를 읽는 게 아니라 호출자가 준
평문을 암호화만 하는 함수라 정보 유출 위험이 없어 이번엔 그대로 뒀습니다.

## 🟡 중간 — 기관/작성자 이름 위조 가능 (문서 신뢰성)

**문제**: `counselor_profiles.org`/`display_name`은 본인이 검증 없이 자유롭게 수정 가능하고, 이 값이
그대로 hwpx 사유서의 기관명·작성자 서명란에 들어갑니다.

**왜 문제인가**: 회원가입이 열려 있으니 누구나 소속을 "사단법인 인천내일을여는집"으로, 이름을 실제
직원 이름으로 바꿔 "공식처럼 보이는" 정부 제출용 문서를 만들어낼 수 있습니다. 유출되면 기관 신뢰도·
법적 문제로 이어질 수 있습니다.

**해결 방법(원본, 2026-09-10 폐기)**: ~~회원가입을 기관 이메일 도메인(`@homelesshot.or.kr`)으로
제한~~. **2026-09-11 폐기 결정**: 작은 기관은 정식 도메인 메일을 쓰는 게 생각보다 큰 비용·수고라서,
대표메일로 네이버·지메일 같은 무료 메일을 쓰는 경우가 흔합니다. "기관 도메인 메일이 있어야 가입
가능"이라는 조건은 우리 기관은 물론, 오픈소스로 이 서비스를 가져다 쓸 다른 소규모 기관에도 비현실적인
전제라 이 방법 자체를 없앴습니다(구흥모님 지적, 2026-09-11).

### ⏳ 해결 현황(2026-09-11) — 도메인 제한 폐기, 대안 연구 완료(구현은 10월 이후)

도메인 제한 대신 **"초대 전용 회원가입"** 방식을 조사해서 대안으로 정리해뒀습니다 — 관리자가 실제
상담원의 이메일(어떤 메일 서비스든 상관없음)로 직접 초대해야만 계정이 생기는 구조라, 도메인 제한도
별도 메일 발송 서비스 비용도 없이 "관계없는 사람이 마음대로 가입"하는 문제(이 항목의 문서 위조 위험 +
API 호출 한도를 계정 여러 개로 우회하는 문제)를 근본적으로 막습니다. 자세한 조사 내용·권장안은
`claude/보안-가입방식-연구-2026-09-11.md` 참고 — 지금은 텍스트로만 기록해두고, 실제 구현은 10월 이후
과제로 미룹니다.

`org` 필드를 자유 입력 대신 고정값으로 바꾸는 것(원본 제안의 두 번째 방법)은 여전히 유효합니다 — 클라이언트
코드(`doSignUp`/`saveNameChange`)에서 `org` 입력을 없애고 항상 "사단법인 인천내일을여는집"으로 저장하도록
바꾸면 되는데, 이건 v4.html 수정만으로 가능하고 도메인·초대 방식과 무관하게 지금 바로 적용 가능한
항목입니다 — "적용해줘"라고 요청 주시면 바로 반영해드리겠습니다.

## 🟡 중간 — hwpx 문서에서 사용자 입력이 토큰 치환 순서를 오염시킬 수 있음

**문제**: `fillHwpxTemplate()`이 `xml.split('{{KEY}}').join(값)` 방식으로 토큰을 순서대로 치환하는데,
메모(자유 입력)에 우연히 `{{RESULT_LINE}}` 같은 문자열이 들어 있으면 나중에 처리되는 다른 토큰에 의해
그 자리가 엉뚱한 값으로 다시 바뀔 수 있습니다. 또한 `xmlEscape()`가 `&`, `<`, `>`만 이스케이프하고
큰따옴표·작은따옴표는 그대로 둬서, 토큰이 속성값 자리에 들어가는 경우가 생기면 XML 구조 자체가 깨질
잠재 위험이 있습니다.

**왜 문제인가**: 발생 확률은 낮지만, 공식 문서 생성 로직에서 사용자 입력이 치환 순서·구조에 영향을
주는 설계 자체가 취약합니다.

**해결 방법(원본)**: 각 값을 토큰 치환에 넣기 전에 `{{`/`}}`를 다른 문자(예: 전각 중괄호)로 미리 치환하고,
`xmlEscape()`에 `"`→`&quot;`, `'`→`&apos;` 두 줄만 추가하면 됩니다. 수정 시간 대비 효과가 가장 좋은
항목 중 하나입니다.

### ✅ 해결 현황(2026-09-10) — 완료

**완료(v4-track a32, 2026-09-09, 이미 배포됨)**: `xmlEscape()`에 큰따옴표·작은따옴표 이스케이프 추가,
값 치환 전 연속 중괄호 사이에 폭 없는 문자를 끼워 `{{토큰}}` 패턴 자체를 깨뜨리는 방식으로 재치환 오염
방지 완료. `fillHwpxTemplate`/`fillHwpxTemplateWithMulti`/`buildAiHwpxParagraphs` 전부 공용 함수라 한
곳 수정으로 전체 반영됨. 추가 조치 불필요.

## 🟡 중간 — 결과 화면에 "예비판단일 뿐"이라는 면책 문구가 눈에 띄게 없음

**문제**: 판정 배지가 "신호등처럼" 명확하게 보이도록 설계돼 있는데(코칭 반영), 정작 "이건 참고용
예비판단이고 최종 승인 여부는 담당 공무원이 별도로 심사한다"는 문구가 결과 화면 상단에 눈에 띄게
없습니다.

**왜 문제인가**: 바쁜 상담원이나 대상자가 배지만 보고 확정된 결과로 오인할 위험이 있고, "책임은 누가
지나요?"라는 질문에 화면 자체가 답을 뒷받침해주지 못하는 상태입니다.

**해결 방법(원본)**: `TPL_RESULT1` 카드 최상단에 "⚠ 이 판단은 예비판단이며, 최종 지원 여부는 담당 기관의
공식 심사를 따릅니다" 한 줄을 눈에 띄는 스타일로 추가합니다. 코드 몇 줄, 5분 내 작업입니다.

### ✅ 해결 현황(2026-09-10) — 완료

**완료(v4-track a32, 2026-09-09, 이미 배포됨)**: 결과 화면(TPL_RESULT1) 최상단에 면책 문구 박스 추가
완료. 추가 조치 불필요.

## 🟡 중간 — 5인 이상 가구 소득기준이 "추정치"인데 배지는 확정적으로 보임

**문제**: `INCOME_STD` 테이블의 5인 값은 "3→4인 증가폭을 그대로 적용한 추정치"라고 코드 주석에
명시돼 있고 작은 경고 문구가 붙어 있지만, 판정 배지 자체는 눈에 띄게 확정적으로 보입니다.

**왜 문제인가**: 6인 이상 대가구는 실제 기준이 더 높아야 하는데 5인 기준을 그대로 써서, 실제로는
적격인데 화면엔 "부적격"으로 표시될 수 있는 케이스가 존재합니다 — 도움이 필요한 위기가구를 놓치는
방향의 오류라 조심스러운 부분입니다.

**해결 방법(원본)**: 보건복지부 최신 고시에서 6인·7인 기준액을 찾아 `INCOME_STD` 테이블에 실제 값으로
추가하는 게 가장 확실합니다(자료 조사 후 코드 수정은 몇 분). 못 찾으면 최소한 "5인 이상"일 때 배지
색을 판정색 대신 "확인 필요" 톤으로 바꿔서 경고를 놓칠 가능성을 줄이는 게 차선책입니다.

### ✅ 해결 현황(2026-09-10) — 완료(근본 해결, a55에서 실제 고시금액 반영)

**1차(v4-track a32, 2026-09-09, 배포됨)**: 원본이 제시한 두 방법 중 우선 차선책 적용 — 5인 이상
가구가 소득·재산 요건을 충족해도 확정 배지 대신 "추가 확인 필요 — 5인 이상 가구 소득기준은 추정치"
배지(주황)로 표시.

**2차(v4-track a55, 2026-09-10, 배포됨) — 원본이 제시한 1순위(근본) 방법을 실제로 완료**: "4인이
최대치라 5인 이상은 다 추정치"라는 기존 전제 자체가 틀렸다는 걸 확인했습니다. 2026년 보건복지부
기준중위소득 고시는 실제로 **1인부터 7인까지 공식 금액을 공표**하고 있어서(1인 2,564,238원~7인
9,554,289원), `INCOME_STD`의 5·6·7인 값을 전부 실제 고시 금액의 75%(긴급복지지원 소득기준)로
교체했습니다(5인 5,667,539원, 6인 6,416,964원, 7인 7,165,717원). "추가 확인 필요" 경고 배지 기준도
`size>=5`에서 **`size>=8`**로 올려서, 실제 공식 수치가 있는 5·6·7인은 이제 확정 배지로 표시됩니다.
**8인 이상만** 6→7인 증가폭(75% 환산 748,753원)을 적용한 추정치로 남아 있고, 가구원수 선택지도
5인/6인/7인/8인 이상으로 세분화했습니다. 이 수치는 2차 출처(정부 고시 원문은 robots.txt로 직접 접근
불가)로 확인한 값이라, 제출 전 보건복지부 공식 고시 원문 대조를 권장합니다.

## 🟡 중간 — 국적·거주형태 선택지가 실제 사례를 다 못 담음

**문제**: `nationality`에 결혼이주여성 이혼절차 중·무국적 아동 동반 같은 애매한 체류자격 사례를 넣을
항목이 없고, `livingType`도 여인숙 장기투숙·지인 집 얹혀살기·차박 같은 흔한 애매 사례가 전부
"기타·확인 필요"로 뭉뚱그려집니다.

**왜 문제인가**: 선택지가 부족하면 상담원이 실제와 안 맞는 값을 억지로 골라 넣게 되고, 그게 그대로
판정 로직 입력값이 됩니다.

**해결 방법(원본)**: 실사용 테스트 중 나온 실제 사례를 기준으로 `nationality`/`livingType`의 `<option>`
목록에 항목을 몇 개 추가합니다. 판정 로직(`ASSET_LIMIT`, `LIVING_PRESET` 등)에 영향 주지 않게 새 항목은
기존 "기타" 계열과 동일하게 매핑하면 안전합니다.

### ✅ 해결 현황(2026-09-10) — 완료

**완료(v4-track a54, 오늘 반영·커밋됨)**: `livingType`에 "여인숙 장기투숙"·"지인 집 얹혀살기(더부살이)"·
"차량 내 거주(차박)" 3개 추가(표시 전용, 판정 로직 영향 없음). `nationality`에 "체류자격 변동 중(예:
결혼이주 이혼절차)"·"무국적 아동 동반" 2개 추가 — 두 항목 모두 기존 "외국인(미확정)"과 동일하게
"체류자격 확인 후 지원범위 결정" 확인-필요 분기로 매핑해서 안전하게 처리했습니다. 추가 조치 불필요
(다만 실사용 중 또 다른 애매 사례가 나오면 계속 추가 가능).

## 🟡 중간 — 질문 순서가 위기 상황·트라우마를 고려하지 않은 구조

**문제**: ①위기사유→②거주→③소득·재산(원 단위 숫자 입력)→④건강상태(정신질환·폭행상해·감염병 등
체크리스트) 순서로 화면이 강제 진행됩니다.

**왜 문제인가**: 실제 상담은 라포 형성이 먼저이고, 정신질환·폭행 이력 같은 예민한 질문은 신중하게
나중에 다뤄야 하는데, 위저드(단계별 강제 진행) 구조라 신입 상담원이 이 화면 순서를 실제 질문 순서로
그대로 따라 할 위험이 있습니다.

**해결 방법(원본)**: 당장 순서를 바꾸기보다, 건강상태 화면 상단에 "이 항목들은 상담이 충분히 진행된 뒤,
필요한 만큼만 편하게 여쭤보세요 — 순서는 참고용입니다" 같은 안내 문구를 추가하는 것만으로도 완화
가능합니다. 근본적인 화면 순서 재설계는 제출 이후 과제로 남겨도 무방합니다.

### ✅ 해결 현황(2026-09-10) — 완료

**완료(v4-track a33 안내 문구 추가 + v4-track a49 안내를 물음표 버튼 뒤로 숨김, 둘 다 이미 배포됨)**:
건강상태 화면 상단에 안내 문구 추가 완료, 이후 상담원 요청으로 다른 안내문과 같은 방식(눌러야 뜨는
물음표 팝오버)으로 한 번 더 개선. 근본적인 화면 순서 재설계는 원본 제안대로 제출 이후 과제로 남겨둠.

## 🟢 낮음 — Whisper 환각 필터가 화이트리스트 방식이라 우회 가능

**문제**: `stt-transcribe`의 환각 문구 필터는 알려진 몇 가지 패턴(유튜브·뉴스 멘트)만 걸러내는 방식이라,
처음 보는 형태의 환각 문장은 그대로 통과해 상담일지에 들어갈 수 있습니다.

**해결 방법(원본)**: 근본 해결은 모델 한계상 어렵습니다. `no_speech_prob`/`avg_logprob` 임계값을 좀 더
보수적으로(AND 대신 OR 조건 검토) 조정하는 정도가 현실적인 개선입니다.

### ⏳ 해결 현황(2026-09-10) — 미적용, 코드 준비됨(위 치명1 Edge Function 코드에 포함)

Edge Function 배포가 필요해 직접 실행하지 못했지만, 위 "치명 1" 항목에 올려둔 `stt-transcribe` 전체
코드에 이미 이 개선을 포함해뒀습니다 — 기존 `noSpeech > 0.6 && avgLogprob < -0.5`(AND, 둘 다 만족해야
제외)를 `noSpeech > 0.55 || avgLogprob < -1.0`(OR, 둘 중 하나만 강하게 의심돼도 제외)로 강화. 배포하면
같이 적용됩니다. (근본 해결은 원본 설명대로 모델 한계상 어려움 — 이 정도가 현실적 개선의 한계입니다.)

## 🟢 낮음 — Supabase 보안 어드바이저 경고 2건

- **유출된 비밀번호 차단(Leaked Password Protection) 꺼져 있음** → 대시보드 토글 하나, 5분.
- **`touch_counselor_keys` 함수의 search_path 미고정** → 함수 정의에 `SET search_path = public` 한 줄 추가.

### ⏳ 해결 현황(2026-09-10) — 하나는 코드 준비됨, 하나는 대시보드 수동 작업 필요

**`touch_counselor_keys` search_path — SQL 준비됨(미실행)**:
```sql
alter function public.touch_counselor_keys() set search_path = public;
```

**유출된 비밀번호 차단 — 코드로 할 수 없는 항목**: 이건 SQL이나 Edge Function이 아니라 Supabase
프로젝트의 Auth 설정(Dashboard > Authentication > Providers > Email > "Leaked password protection")
토글이라, Claude가 가진 도구로는 켤 수 없습니다. 로그인 비밀번호를 정할 때 이미 전 세계적으로 유출된
적 있는 비밀번호 목록(Have I Been Pwned 공개 데이터베이스)과 대조해서 그런 비밀번호는 애초에 못 쓰게
막아주는 기능입니다. 대시보드에서 직접 토글 하나만 켜면 됩니다(5분 이내).

## 📌 개인정보보호법 근거 확인 — 전체적으로 반영된 사항 (2026-09-10 추가)

이번에 구흥모님이 확인해주신 내용을 별도로 정리합니다: 원래는 상담 내용을 AI로 보내기 전에 실명 등을
전부 가려야(익명화) 한다고 가정하고 있었는데, **개인정보보호법을 참고해보니 "고지"만 제대로 되어
있으면 실명이 그대로 언급되어도 문제가 되지 않는다**는 걸 확인하셨습니다. 이건 위 "치명 2" 항목의
해결 현황에 반영했고, 실제로 이미 **v4-track a36(2026-09-09)**에서 아래처럼 구현되어 있습니다:

- 별도 동의 체크박스를 받는 대신, 처리방침을 상시 접근 가능하게 공개(🔒 개인정보 처리방침 버튼, 최초
  1회는 자동으로 안내 표시)하고 "상담원에게 구두 안내 방법을 명확히 전달"하는 방식으로 국외이전
  (OpenAI) 처리 근거를 확보.
- 처리방침(`POLICY_TEXT_V1`)에 "3. 국외 이전 안내" 항목으로 이전받는 자(OpenAI)·이전 목적·이전 방법·
  보유기간·이전 거부 시 대안까지 명시.
- 이 구조가 적법하다는 게 확인된 이상, "가명화 도우미"의 존재 이유는 "법적 의무"가 아니라 **"개인정보
  최소화라는 좋은 실무 관행"**으로 재정의하는 게 정확합니다 — 이 문서의 치명2 섹션과 코치 미팅
  (9/2)의 "가명화 도우미 A/B 결정" 숙제에 그대로 연결되는 내용이니, 오늘 코칭에서 이 프레임으로
  설명하시면 좋을 것 같습니다. 오늘(9/10) "가명화하기(수동)" 버튼 추가(a55)로 이 좋은 실무 관행에
  대한 접근성도 함께 높였습니다.

## 제출(9/17) 전 우선순위 — 갱신(2026-09-10)

1. ✅ **완료(a32)** — 결과 화면에 예비판단 면책 문구 추가
2. ✅ **완료(a32)** — hwpx 토큰/따옴표 이스케이프 보완
3. ✅ **완료(a32)** — 소득·재산 AI 자동채움 값 항상 사람 확인 후 다음 단계 진행
4. ✅ **완료(a32→a55)** — 5인 이상 가구 소득기준: 1차 "추가 확인 필요" 배지(a32) → 2차 실제 고시금액
   반영으로 근본 해결(a55, 8인 이상만 추정치로 남음)
5. ✅ **완료(a33)** — 건강상태 화면 트라우마 배려 안내 문구
6. ✅ **완료(a33)** — 자동추출 경로 부분 가명화(구조화된 개인식별정보 스크럽)
7. ✅ **완료(a36)** — 개인정보 처리방침 신설 + 국외이전(OpenAI) 고지, 별도 동의 없이 적법 처리 근거 확보
8. ✅ **완료(a54)** — STT/AI 인증 헤더를 로그인 세션 토큰 우선으로(클라이언트 절반)
9. ✅ **완료(a54)** — 국적·거주형태 선택지 확충
10. ✅ **완료(a55)** — 가명화하기(수동) 버튼을 녹음결과 화면에 추가 — 실명·지명 즉시 가림 접근성 강화
11. ✅ **완료(2026-09-11, 구흥모님 Supabase 대시보드에서 직접 적용)** — STT·AI 시간당 호출 한도(치명1
    서버 절반) — 카운터 테이블 생성 + `stt-transcribe`/`ai-draft` 두 Edge Function 교체·배포 완료.
    Whisper 환각 필터 OR 조건 강화도 같은 배포에 포함되어 함께 적용됨.
    ⏳ **여전히 SQL 준비 완료·실행 대기** — 조직 접근 권한(is_admin+RLS), decrypt_case_field 소유권
    검증, `touch_counselor_keys` search_path 고정 — 이 셋은 아직 미적용. Supabase 대시보드에서 직접
    실행하시거나, 다음 세션에서 "적용해줘"라고 요청하시면 그때 실행 가능합니다.
12. ⏳ **이번 제출 범위에서 제외(2026-09-11 결정)** — Leaked Password Protection 토글: 확인해보니
    이 기능이 Supabase **Pro 플랜 이상에서만** 켜집니다(현재 `ieojim-stt-proxy` 프로젝트는 Free 플랜).
    부가적인 보안 기능(치명적 항목 아님)이라 유료 전환 없이 이번 제출까지는 스킵하기로 함.
13. ✅ **폐기(2026-09-11) → 대안 연구로 대체** — 회원가입 기관 도메인 제한: 소규모 기관은 네이버·
    지메일 같은 무료 메일을 대표메일로 쓰는 경우가 흔해 비현실적인 전제라 이 방법 자체를 폐기.
    대신 "초대 전용 회원가입" 방식을 조사해 `claude/보안-가입방식-연구-2026-09-11.md`에 기록 —
    10월 이후 구현 과제로 남김.
14. ✅ **정책 방향 사실상 확정(2026-09-10)** — 가명화 도우미는 "법적 의무" 아닌 "최소화 실무 관행"으로
    현행 유지(완전 익명화 방향 아님), 접근성은 수동 버튼(a55)으로 강화 — 위 "개인정보보호법 근거 확인"
    섹션 참고
