# 이어짐 — 검증·실사용 기록 템플릿 (2026-09-14)

> 목적: 인터뷰(가상) 및 타기관 실사용을 **표 한 행 = 데이터 한 건**으로 남겨서, 발표·제출물의
> 정량/정성 근거로 쓸 수 있게 함. 심사기준 ①완성도 ②현장변화 ③문제해결 ④임팩트에 직접 대응.
>
> ⚠️ **정직성 원칙 (반드시 지킬 것)** — 데이터를 두 종류로 **라벨을 분리**해서 기록·발표한다:
> - **[가상]** 우리가 만든 시나리오로 돌린 것 → **기능 검증**(다양한 상황 처리 능력, ①완성도) 근거로만.
>   "현장에서 실제로 쓰였다"로 포장하지 않는다.
> - **[실사용]** 타기관·현장 상담원이 실제로 써본 것 → ②③④의 진짜 근거.
> - 발표·문서에서 두 숫자를 **절대 합치지 않는다**(예: "가상 검증 8건 / 실사용 3건" 따로 표기).

---

## A. 케이스 기록 표 (가상·실사용 공용) — 인터뷰 1건마다 1행

| # | 구분 | 날짜 | 기관 | 시나리오(가명 위기유형) | 입력방식 | 이어짐 소요(분) | AI자동추출 (채운수/전체) | 사람수정 건수 | "확인필요" 건수 | 판정결과 | 상담일지 완성섹션(/5) | 가명화 확인 | 비고 |
|---|------|------|------|------------------------|----------|-----------------|--------------------------|---------------|-----------------|----------|----------------------|-------------|------|
| 1 | [가상] | 2026-09-14 | (자체) | 예) 독거노인·주거상실 | 녹음 | | / | | | | /5 | 실명 [___] 처리✔ | |
| 2 | [실사용] | | (기관명) | | | | / | | | | /5 | | |
| 3 | | | | | | | / | | | | /5 | | |
| 4 | | | | | | | / | | | | /5 | | |
| 5 | | | | | | | / | | | | /5 | | |

**각 칸 채우는 법**
- **이어짐 소요(분)**: 시작~상담일지 초안 완성까지 실측(가상 케이스여도 앱 조작 시간은 실제 값이라 유효).
- **AI자동추출 (채운수/전체)**: 위저드에서 AI가 자동으로 채운 항목 수 / 물어본 전체 항목 수 → **자동화율**.
- **사람수정 건수**: AI가 채웠는데 상담원이 고친 항목 수 → 낮을수록 정확.
- **"확인필요" 건수**: 노란 안내로 뜬(=AI가 불확실) 항목 수.
- **상담일지 완성섹션(/5)**: 위기상황·생계재산·건강·요청지원·법적근거 5개 중 채워진 수.
- **가명화 확인**: 상담일지에 실명·주민번호·주소가 `[___]`로 남았는지 눈으로 확인.

---

## B. 타기관용 피드백 양식 (구글폼/카톡 붙여넣기용)

> 타기관 담당자가 이어짐으로 **인터뷰→상담일지 초안 작성**을 해본 직후 이 양식으로 회신 요청.
> 자유서술이 아니라 아래 항목대로 받아야 집계가 됩니다.

### [기관 정보]
1. 기관명 / 유형 (예: 노숙인시설·지역아동센터·종합사회복지관 등)
2. 응답자 역할 (사회복지사 / 팀장 / 기타)
3. 담당 위기가구 규모 (대략 월 몇 건)

### [사용 경험 — 정량, 숫자로]
4. 이어짐으로 상담일지 초안 1건 만드는 데 걸린 시간: 약 ____분
5. 기존 방식(수기+입력+서류)으로는 1건에 보통: 약 ____분
6. 만족도 (1 매우 불만 ~ 5 매우 만족): ____
7. 업무 부담 체감 변화 (1 훨씬 늘음 ~ 3 그대로 ~ 5 훨씬 줄음): ____
8. AI가 녹음/입력에서 항목을 얼마나 잘 채웠나 (1 거의 못함 ~ 5 대부분 맞음): ____
9. 실제 업무에 계속 쓸 의향 (예 / 아니오 / 조건부): ____

### [사용 경험 — 정성, 서술]
10. 가장 도움이 된 점 1가지:
11. 불편하거나 오류가 났던 점 1가지 (어느 화면에서 무엇을):
12. 꼭 개선됐으면 하는 점 1가지:
13. (선택) 우리 기관 말고 다른 기관에도 권할 만한가? 이유:

### [개인정보]
14. 실제 대상자 정보가 아니라 **가상/재연 사례**로 테스트하셨음을 확인 (예/아니오)
   → 실제 사례로 하셨다면 상담일지의 실명·주민번호·주소가 `[___]`로 비워졌는지 확인 부탁드립니다.

---

## C. 집계 지표 (표 A·양식 B가 쌓이면 계산)

발표·제출물에 넣을 숫자. **[가상]과 [실사용]을 나눠서** 각각 계산한다.

- **평균 처리시간**: 표 A "이어짐 소요" 평균 → baseline의 수기 합계와 Before/After 비교
- **자동화율**: Σ(채운수) / Σ(전체) × 100%
- **정확도**: 1 − Σ(사람수정) / Σ(채운수)
- **커버리지**: 처리한 서로 다른 위기유형·대상유형 조합 수 (①완성도)
- **피드백 요약**: 만족도 평균, "계속 쓰겠다" 비율, 개선요청 태그별 건수(예: STT정확도 n건·UI n건)
- **확장성**(④): 실사용 기관 수 + 기관 유형 다양성

---

## D. 결과물 어디에 넣나
- **발표자료**: 「[가상] 검증 N시나리오 / [실사용] M기관」 지표 카드 + 대표 상담일지 초안 캡처(`[___]` 강조) + Before/After 막대
- **README / 제출물**: 검증 방법론 + 외부 도입 사례(기관 유형·피드백 요약)
- **이 문서**: 표 A와 회신받은 B를 계속 이어서 기록(원본 데이터 보관)

> 관련 도구: 처리시간 Before/After는 예선 때 만든 [`이어짐_1주차_baseline측정.html`](이어짐_1주차_baseline측정.html)에도
> 그대로 입력 가능(① 처리시간 ② 공적연계율 ③ 업무부담 측정칸 있음). 표 A와 병행해서 쓰면 됨.

---

## 부록 E. B 양식 → 구글폼

타기관에 보낼 때 자유서술로 받으면 집계가 안 되므로 **구글폼**으로 받는다. 아래 사양대로 만들면
응답이 표 A와 같은 형태로 자동 누적된다(폼 편집 → 응답 → 스프레드시트로 연결).

**폼 제목**: 이어짐 사용 피드백
**설명**: 이어짐으로 인터뷰→상담일지 초안을 만들어보신 뒤 응답해 주세요. 약 3분. 실제 대상자 정보 대신 가상/재연 사례로 테스트해 주세요.

### E-1. 문항 사양(수동 입력용)

| # | 섹션 | 문항 | 유형 | 선택지 / 비고 | 필수 |
|---|------|------|------|---------------|------|
|1|기관 정보|기관명|단답형|—|✔|
|2||기관 유형|객관식|노숙인 시설 / 지역아동센터 / 종합사회복지관 / 장애인복지관 / 노인복지시설 / 기타|✔|
|3||응답자 역할|객관식|사회복지사 / 팀장·관리자 / 기타|✔|
|4||담당 위기가구 규모(월)|객관식|월 10건 미만 / 10~30건 / 30~50건 / 50건 이상|✔|
|5|정량|이어짐으로 초안 1건 소요(분)|단답(숫자)|숫자 검증|✔|
|6||기존 방식 1건 소요(분)|단답(숫자)|숫자 검증|✔|
|7||전반 만족도|선형배율 1~5|1 매우 불만 ~ 5 매우 만족|✔|
|8||업무 부담 체감 변화|선형배율 1~5|1 훨씬 늘었다 ~ 5 훨씬 줄었다|✔|
|9||AI 자동 채움 정확도|선형배율 1~5|1 거의 못함 ~ 5 대부분 맞음|✔|
|10||계속 쓸 의향|객관식|예 / 아니오 / 조건부(개선되면)|✔|
|11|정성|가장 도움이 된 점|장문형|—|✔|
|12||불편·오류(어느 화면서 무엇)|장문형|—|—|
|13||개선됐으면 하는 점|장문형|—|—|
|14||다른 기관에 권할 만한가+이유|장문형|—|—|
|15|개인정보|가상/재연으로 테스트했나|객관식|예(가상·재연) / 아니오(실제 사례)|✔|
|16||(실제였다면) 실명·주민번호·주소 `[___]` 처리됐나|객관식|예 확인함 / 아니오 / 해당없음|—|

> 배율 문항(7·8·9)은 모두 **5가 좋음** 방향으로 통일 — 평균 낼 때 헷갈리지 않게.

### E-2. 자동 생성 스크립트 (한 번에 폼 만들기)

[script.google.com](https://script.google.com) → 새 프로젝트 → 아래 붙여넣기 → ▶ 실행 →
실행 로그의 편집/공유 URL 사용.

```javascript
function createIeojimFeedbackForm() {
  var f = FormApp.create('이어짐 사용 피드백');
  f.setDescription('이어짐으로 인터뷰→상담일지 초안을 만들어보신 뒤 응답해 주세요. 약 3분. 실제 대상자 정보 대신 가상/재연 사례로 테스트해 주세요.');

  f.addSectionHeaderItem().setTitle('1. 기관 정보');
  f.addTextItem().setTitle('기관명').setRequired(true);
  f.addMultipleChoiceItem().setTitle('기관 유형').setChoiceValues(
    ['노숙인 시설','지역아동센터','종합사회복지관','장애인복지관','노인복지시설']).showOtherOption(true).setRequired(true);
  f.addMultipleChoiceItem().setTitle('응답자 역할').setChoiceValues(
    ['사회복지사','팀장·관리자','기타']).setRequired(true);
  f.addMultipleChoiceItem().setTitle('담당 위기가구 규모(월 대략)').setChoiceValues(
    ['월 10건 미만','10~30건','30~50건','50건 이상']).setRequired(true);

  f.addSectionHeaderItem().setTitle('2. 사용 경험 (정량)');
  var num = FormApp.createTextValidation().requireNumber().build();
  f.addTextItem().setTitle('이어짐으로 상담일지 초안 1건 만드는 데 걸린 시간(분)').setValidation(num).setRequired(true);
  f.addTextItem().setTitle('기존 방식(수기+입력+서류)으로는 1건에 보통(분)').setValidation(num).setRequired(true);
  f.addScaleItem().setTitle('전반 만족도').setBounds(1,5).setLabels('매우 불만','매우 만족').setRequired(true);
  f.addScaleItem().setTitle('업무 부담 체감 변화').setBounds(1,5).setLabels('훨씬 늘었다','훨씬 줄었다').setRequired(true);
  f.addScaleItem().setTitle('AI가 녹음/입력에서 항목을 잘 채웠는지').setBounds(1,5).setLabels('거의 못함','대부분 맞음').setRequired(true);
  f.addMultipleChoiceItem().setTitle('실제 업무에 계속 쓸 의향').setChoiceValues(
    ['예','아니오','조건부(개선되면)']).setRequired(true);

  f.addSectionHeaderItem().setTitle('3. 사용 경험 (정성)');
  f.addParagraphTextItem().setTitle('가장 도움이 된 점 1가지').setRequired(true);
  f.addParagraphTextItem().setTitle('불편하거나 오류가 난 점 (어느 화면에서 무엇을)');
  f.addParagraphTextItem().setTitle('꼭 개선됐으면 하는 점 1가지');
  f.addParagraphTextItem().setTitle('다른 기관에도 권할 만한가? 이유');

  f.addSectionHeaderItem().setTitle('4. 개인정보 확인');
  f.addMultipleChoiceItem().setTitle('이번 테스트를 가상/재연 사례로 하셨나요?').setChoiceValues(
    ['예(가상·재연)','아니오(실제 사례)']).setRequired(true);
  f.addMultipleChoiceItem().setTitle('(실제 사례였다면) 상담일지에 실명·주민번호·주소가 [___]로 비워졌나요?').setChoiceValues(
    ['예, 확인함','아니오','해당없음(가상이었음)']);

  Logger.log('편집 URL: ' + f.getEditUrl());
  Logger.log('응답(공유) URL: ' + f.getPublishedUrl());
}
```

응답은 폼 편집 화면 "응답 → 스프레드시트로 연결"을 누르면 이 문서 표 A와 같은 형태로 자동 누적된다.

---

## 부록 F. ⭐ HTML 양식 → 구글 시트 자동 저장 (권장 · 타기관은 계정 불필요)

구글폼 UI가 안 될 때, **이어짐과 같은 방식의 HTML 양식**([`이어짐_피드백양식.html`](이어짐_피드백양식.html))을
링크로 열어 타기관이 채우면 → **본인(sunny/구흥모) 구글 시트에 응답이 한 줄씩 자동으로 쌓인다.**
타기관은 구글 로그인도, 앱 설치도 필요 없다. 아래 3단계면 끝.

### F-1. 구글 시트 + Apps Script 준비 (한 번만)
1. 새 구글 시트 하나 만든다(예: "이어짐 피드백 응답"). — [sheets.new](https://sheets.new)
2. 그 시트에서 **확장 프로그램 → Apps Script** 열고, 기본 코드를 지우고 아래를 붙여넣는다:

```javascript
function doPost(e){
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var sh = ss.getSheetByName('응답') || ss.insertSheet('응답');
  var cols = ['시각','기관명','기관유형','역할','규모','이어짐분','기존분',
    '만족도','업무부담','AI정확도','의향','도움된점','불편오류','개선점','권유이유','테스트유형','가명화'];
  if (sh.getLastRow() === 0) sh.appendRow(cols);
  var p = (e && e.parameter) ? e.parameter : {};
  sh.appendRow([ new Date(), p['기관명']||'', p['기관유형']||'', p['역할']||'', p['규모']||'',
    p['이어짐분']||'', p['기존분']||'', p['만족도']||'', p['업무부담']||'', p['AI정확도']||'',
    p['의향']||'', p['도움된점']||'', p['불편오류']||'', p['개선점']||'', p['권유이유']||'',
    p['테스트유형']||'', p['가명화']||'' ]);
  return ContentService.createTextOutput('ok');
}
```

3. 💾 저장 → 상단 **배포 → 새 배포** → 유형 **웹 앱** 선택 →
   - **실행 계정: 나**, **액세스 권한: 모든 사용자** 로 설정 → 배포.
   - 처음이면 권한 승인 창이 뜬다: 계정 선택 → "Google에서 확인하지 않은 앱" 화면이면 **고급 → (안전하지 않음)으로 이동 → 허용**. (본인이 만든 스크립트라 안전함)
   - 배포 완료 후 나오는 **웹 앱 URL**(`https://script.google.com/.../exec`)을 복사한다.

### F-2. HTML 양식에 URL 연결 (한 줄)
`이어짐_피드백양식.html` 상단의 이 줄에 위 URL을 붙여넣고 저장:
```javascript
var SCRIPT_URL = "";   // ← 따옴표 안에 .../exec URL 붙여넣기
```
(비워두면 자동 전송 대신 "복사해서 카톡/메일로 회신" 방식으로 안전하게 동작 — URL만 넣으면 시트 자동 저장으로 바뀜.)

### F-3. 배포·공유
- `이어짐_피드백양식.html`을 저장소에 push하면 이어짐과 같은 GitHub Pages 링크로 열린다:
  `https://homelesshot.github.io/ieojim/이어짐_피드백양식.html`
- 이 링크를 타기관에 보낸다 → 채우고 "제출하기" → **구글 시트 "응답" 탭에 한 줄씩 자동 누적.**
- 그 시트가 곧 이 문서 표 A의 원본 데이터가 된다(집계 지표 C로 바로 계산).

> 참고(기술): HTML→Apps Script는 브라우저 CORS 때문에 응답 본문을 못 읽어서, 양식은 `mode:'no-cors'`로
> 전송하고 로컬에서 "제출 완료"를 표시한다(전송 자체는 정상 기록됨). 혹시 전송이 막히면 양식이 자동으로
> "복사해서 회신" 화면으로 넘어가 데이터가 유실되지 않는다.
