# 이어짐 — 상담원 개인별 비밀키 + 복구 코드 설계

> 작성: sunny님 결정 + Claude 설계 · 2026-09-04
> 구흥모 쌤 검토용. **SQL은 구흥모 쌤이 Supabase SQL Editor에서 실행**해야 합니다(Claude는 DB 실행 권한 없음).

## 목표 (sunny님 결정)
- 개인정보(상담 스냅샷)는 **상담원 본인의 비밀키(암호문구)를 아는 사람만** 열람.
- 서버(Supabase)·관리자·DB 덤프로도 원문을 볼 수 없어야 함 = **제로지식(zero-knowledge)**.
- 비밀키를 잊으면 **복구 코드**로 본인이 직접 접근을 되살리고 새 비밀키로 재설정(자가 복구).

## 핵심 원리 — 봉투 암호화(envelope encryption)
비밀키로 데이터를 직접 암호화하지 않습니다. 대신:

1. 상담원마다 **데이터 키(DEK, 랜덤 AES-256)** 를 1개 생성.
2. 실제 상담 데이터(`cases.state_json_enc`)는 **DEK로 암호화**.
3. DEK 자체를 **두 번 감싸서(wrap)** 서버에 저장:
   - `dek_by_pass` = DEK를 **비밀키(passphrase)**에서 유도한 키로 암호화
   - `dek_by_recovery` = DEK를 **복구 코드**에서 유도한 키로 암호화
4. 서버에는 **암호문(wrapped DEK)만** 저장 → 서버는 DEK도, 비밀키도, 복구 코드도 **모름**.

```
[비밀키] ──PBKDF2──▶ 래핑키A ──▶ (DEK 암호화) ──▶ dek_by_pass      ┐
                                                                    ├─ 서버 저장(암호문만)
[복구코드] ─PBKDF2──▶ 래핑키B ──▶ (DEK 암호화) ──▶ dek_by_recovery  ┘

DEK ──▶ (상담 스냅샷 암호화) ──▶ cases.state_json_enc  (서버 저장)
```

## 3가지 흐름
### ① 최초 설정 (비밀키 만들기)
- 랜덤 DEK 생성 + 랜덤 **복구 코드**(예: `A1B2-C3D4-E5F6-G7H8-J9K0`) 생성.
- 상담원이 비밀키(암호문구) 입력.
- DEK를 비밀키·복구코드로 각각 래핑 → `counselor_keys`에 저장.
- **복구 코드를 화면에 1회만 표시** → "안전한 곳에 보관하세요"(인쇄/금고). 다시 볼 수 없음.

### ② 평소 잠금 해제
- 상담원이 비밀키 입력 → 래핑키A 유도 → `dek_by_pass` 복호화 → **DEK를 세션 메모리에만 보관**.
- 이후 저장/열람은 메모리의 DEK로 암복호화. (비밀키·DEK는 서버로 안 나감)

### ③ 복구 (비밀키를 잊었을 때)
- 상담원이 **복구 코드** 입력 → 래핑키B 유도 → `dek_by_recovery` 복호화 → DEK 회복.
- **새 비밀키** 입력 → DEK를 새 비밀키로 다시 래핑 → `dek_by_pass` 갱신.
- 데이터는 그대로 유지. 접근만 되살아남.
- 📌 **복구 절차의 상세 UX(복구 코드 발급 화면 문구·재발급·검증 단계 등)는 sunny님이 이후 더 구체화 예정.** 이번 구현은 "복구 코드를 넣을 수 있는 구조"(dek_by_recovery 저장 + 복구 코드 입력창)까지 갖추고, 세부 절차는 추후 보강.

> ⚠️ **비밀키와 복구 코드를 둘 다 잃으면 영구 복구 불가.** (제로지식의 불가피한 특성 — 이게 "본인만 열람"의 대가)

## 암호 파라미터 (클라이언트 Web Crypto API)
- 키 유도: **PBKDF2**, SHA-256, 반복 200,000회, salt 16바이트(랜덤).
- 대칭 암호: **AES-GCM 256비트**, IV 12바이트(랜덤).
- 저장 형식: 각 암호문 = `base64( salt(16) | iv(12) | ciphertext )` (자기완결형).
- 복구 코드: 20자 base32(Crockford), 4자씩 하이픈 구분.

## 서버 스키마 변경 (구흥모 쌤 실행) — `db/2026-09-04_counselor_keys.sql`
- 새 테이블 `counselor_keys` (상담원별 래핑된 DEK 보관), RLS로 **본인 것만** 접근.
- 기존 `encrypt_case_field`/`decrypt_case_field`(서버키 방식) RPC는 **더 이상 사용 안 함**(클라이언트 암호화로 대체). 당장 삭제하지 않아도 되지만, 새 방식 적용 후 정리 권장.

## 배포 순서(중요)
1. **구흥모 쌤**: `db/2026-09-04_counselor_keys.sql`을 SQL Editor에서 실행(테이블+RLS 생성).
2. **Claude**: 클라이언트(v3)에 봉투 암호화·비밀키/복구 UI 반영해 푸시.
3. 로그인한 상담원이 최초 1회 비밀키 설정 → 복구 코드 보관.
4. ⚠️ 기존 fake data의 상담 스냅샷(구 서버키 암호문)은 새 방식으로 복호화 불가 → **새로 저장분부터 적용**(본선 fake data라 문제 없음).

## 변경 전/후 요약
| | 변경 전(현재) | 변경 후 |
|---|---|---|
| 암호화 위치 | 서버 RPC(pgcrypto) | **브라우저(Web Crypto)** |
| 키 보관 | 서버 | **아무 데도 저장 안 함**(비밀키/복구코드는 사람이 보관) |
| 서버가 원문 열람 | 이론상 가능 | **불가(암호문만)** |
| 열람 조건 | 로그인 본인 | 로그인 본인 **+ 비밀키 입력** |
| 비밀키 분실 | 해당 없음 | **복구 코드로 자가 복구** |
