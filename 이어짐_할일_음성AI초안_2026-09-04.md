---
title: 다음 작업 — 핵심기능 우선, UI는 실사용 후 (2026-09-04 재정리)
date: 2026-09-04 — 오후 현장 실사용 대응 + 비밀키 암호화 클라이언트 구현(v0.41~v0.48) 배포 완료
---

# 🔐 2026-09-04(저녁) — 개인정보 비밀키(제로지식 암호화) 진행 상황 — 최우선 확인

**역할 분담(협업자 문서 `보안_비밀키_복구_설계.md` 기준)**:
- **구흥모님**: `db/2026-09-04_counselor_keys.sql`을 Supabase SQL Editor에서 실행 → `counselor_keys` 테이블 생성.
  **아직 미실행**(이 세션에서 `list_tables`로 확인 — `counselor_profiles`/`cases`/`anon_tokens`만 있고
  `counselor_keys`는 없음, 2026-09-04 저녁 기준).
- **Claude(저)**: 클라이언트(v3 파일) 쪽 봉투 암호화 UI·로직 구현 → **v0.48로 완료해서 배포함**.

**지금 상태**: 클라이언트 코드는 이미 반영되어 있지만, 서버 테이블이 없어서 계정 설정(⚙️)을 열면
"🔐 개인정보 비밀키" 섹션에 "이 기능은 서버 설정이 끝나면 사용할 수 있어요(준비 중)"만 뜨고 비활성 —
**구흥모님이 SQL을 실행하면 별도 배포 없이 바로 활성화**됨(같은 파일이 그 테이블 유무를 확인해서 동작 전환).
그 전까지는 기존 방식(서버측 encrypt_case_field/decrypt_case_field RPC)으로 저장/이어하기가 계속 정상 동작 —
이 기능 때문에 실사용이 막히는 일은 없음.

**구현 내용(v0.48)**: PBKDF2(SHA-256, 200,000회)로 비밀키/복구코드에서 래핑키 유도 → AES-GCM 256으로
DEK(상담원별 랜덤 32바이트) 암호화 → 서버(`counselor_keys`)에는 암호문(wrapped DEK)만 저장. 최초 설정 시
복구 코드를 1회만 화면에 보여주고(캡처/보관 안내), 이후 로그인마다 비밀키를 입력해야 그 브라우저에서
상담 내용을 열람·저장 가능(새로고침하면 잠김 — 의도된 동작). 비밀키를 잊으면 복구 코드로 재설정 가능.
`saveCaseProgress`/`resumeCase`는 비밀키가 잠금 해제된 상태(`_dek` 존재)면 새 방식(`v2:` 접두어)으로,
아니면 기존 서버측 방식으로 자동 분기 — 예전에 저장된 사례(서버 암호화 방식)도 그대로 열람 가능.

**다음에 확인할 것**:
1. 구흥모님께 `db/2026-09-04_counselor_keys.sql` 실행 요청(아직 안 했으면).
2. 실행 후 실제 로그인 상태에서 비밀키 설정 → 저장 → 로그아웃 → 재로그인 → 비밀키로 잠금해제 → 이어하기까지
   실제로 되는지 한 번 실사용 테스트 필요(이 세션은 DB에 테이블이 없어 끝까지 테스트 못 함).
3. 기존 `encrypt_case_field`/`decrypt_case_field` RPC는 설계 문서상 "당장 삭제 안 해도 되지만 정리 권장" —
   9/18 제출 전 최종 정리 여부 결정.

# 작업 순서 방침 (사용자 확정)

**핵심기능 먼저 → 그 다음 실사용 하면서 UI 수정.** 코칭에서 나온 UI 피드백은 3주차(실사용 점검)에 한 번에
반영 예정 — `coach-meeting-2026-09-02.md` 참고. 발표(10/2)·제출(9/18) 준비는 `presentation-prep.md`에서
별도로 관리.

# 💡 다음에 만들 기능 — 녹음 → 사유서 자동 초안 (사용자 아이디어, 2026-09-04)

비밀키 암호화(위 항목) 다음으로, 협업자 작업과 조율해서 착수할 기능:
- 손으로 단계별로 눌러 진행하는 지금 방식은 그대로 유지.
- **별도로**, 녹음(또는 파일업로드)만으로 바로 사유서 초안까지 만들어주는 트랙 추가 — 녹음을 변환한
  텍스트에서 Google Gemini가 필요한 정보를 파악해서 바로 사유서로 만들고, 부족한 정보만 나중에
  사람이 확인/수정·보완하도록.
- 추가로: 녹음으로만 진행한 경우, 사유서의 "비고(메모)" 항목에 녹음 내용을 정리해서 자동으로 채워
  넣으면 좋겠다는 의견도 있었음(단계별 항목에 못 담기는 세부 내용을 비고에 요약).
- 이미 Edge Function `ai-draft`(Gemini)가 배포돼 있고 STT→AI초안 매핑 골격은 있음(핵심기능 목록 5번) —
  이 골격을 확장해서 "녹음 하나로 사유서 초안까지" 트랙을 만드는 방향으로 설계하면 될 듯. 착수 전에
  단계별 트랙과 UI/데이터 구조를 어떻게 공유할지 설계 필요.

# 🚨 2026-09-04 — 실사용 중 발견된 문제들 + 긴급 안전패치, 배포 완료(v0.41~v0.48)

## v0.48 — 개인정보 비밀키(제로지식 암호화) 클라이언트 구현

위 "🔐 개인정보 비밀키" 항목 참고.

## v0.47 — 재녹음 시 녹음 중 표시가 너무 약해서 확인이 안 되던 문제 수정

단계형(위저드) 화면에서는 하단 입력창(마이크·타이머 포함)이 숨겨져 있어서, 녹음 결과 카드의 작은 빨간
글씨 하나("🔴 녹음 중…")만으로는 재녹음 시 실제로 녹음 중인지 확인하기 어렵다는 재신고 — 눈에 훨씬 잘
띄도록 빨간 배경 박스 + 펄스 애니메이션 점 + 카드 안에서 직접 올라가는 초 단위 타이머로 바꾸고, 재녹음
버튼을 누르면 해당 카드로 자동 스크롤해서 화면 밖에 있어도 놓치지 않게 함.

## v0.46 — 첫 화면·입력창 라벨이 커진 글씨 때문에 아래로 밀리던 문제 수정(모바일)

v0.42의 "가장 작은 글씨 2배" 규칙을 기계적으로 적용하면서 두 곳이 추가로 회귀:
1. 입력창 위 "대상자 유형을 선택하세요" 안내(`.typehint`)가 .74rem 안팎이던 것이 1.48rem까지 커지면서,
   좁은 화면에서 드롭다운·녹음타이머와 한 줄에 안 들어가 다음 줄로 밀려나 보였음 — 크기를 .92rem으로
   줄이고, 480px 이하 화면에서는 아예 숨김(드롭다운 자체가 이미 선택된 유형을 보여주므로 중복 정보).
2. 첫 화면(웰컴)의 제목·설명·유형칩 글씨가 커지면서 상단 여백(7vh)과 합쳐져 좁은 화면에서 화면 아래로
   많이 밀려나 스크롤해야 보이는 문제 — 480px 이하에서만 상단 여백과 글씨 크기를 다시 적당히 줄여 한
   화면에 들어오도록 조정(넓은 화면·태블릿은 기존 크기 유지).

*이런 식으로 "가장 작은 글씨 2배" 규칙이 다른 곳에서도 과하게 적용돼 레이아웃을 깰 수 있음 — 이상한
부분 보이면 알려주시면 바로 조정하겠음(v0.44에서도 같은 안내를 했었고 이번이 두 번째 발견 사례).*

## v0.45 — STT 한국어 강제(모델 교체) + 안내문 팝오버 화면밖 삐져나감 수정 + 안내문 추가 정리

1. **STT 영어 혼입 재발 → 모델 교체(whisper-1)**: v0.42에서 `stt-transcribe`에 `language:"ko"`를 붙였는데도
   실사용에서 영어가 계속 섞여 나온다는 재신고 — 확인해보니 OpenAI 공식 문서는 `gpt-4o-transcribe-diarize`가
   `language` 파라미터를 지원한다고 되어있지만, 실제로는(OpenAI 커뮤니티·Microsoft Q&A에 다수 보고) 언어모델
   기반 STT 계열(`gpt-4o-transcribe`, `gpt-4o-transcribe-diarize`)에서 `language`가 "강제"가 아니라 "힌트"로만
   취급되는 알려진 문제였음. 반면 `whisper-1`은 디코더 단계에서 language가 실제로 강제되는 구모델이라 훨씬
   안정적으로 한국어만 나옴 — Edge Function을 `whisper-1`(+`response_format: verbose_json`)로 교체, 배포 완료
   (버전 5). **트레이드오프**: whisper-1은 화자분리(diarization)를 지원하지 않아 화자별 구분(bySpeaker)은
   당분간 비워둠(화면은 bySpeaker가 없으면 자동으로 그 영역을 안 보여주도록 이미 처리되어 있어 깨지지 않음).
   화자분리는 OpenAI 쪽 언어 강제 문제가 정리되면(또는 다른 방식으로) 재검토.
   **참고**: 배포 직후 "위스퍼1 허용해줬는데도 오류" 재보고가 있었으나, OpenAI 프로젝트의 모델 허용
   목록(whisper-1/gpt-4o-transcribe-diarize 둘 다 이미 허용되어 있었음, 스크린샷 확인)에 반영되는 데
   딜레이가 있었던 것으로 재시도 후 정상 동작 확인됨(2026-09-04 저녁) — 실제 코드/설정 문제는 아니었음.
2. **안내문 접기(?) 팝오버 화면 밖으로 삐져나가는 버그 수정**: 모바일에서 물음표 버튼이 화면 오른쪽 끝에
   가까이 있을 때 안내문(최대 320px 너비)이 화면 밖으로 나가버리는 문제 — `position:absolute+left:0`
   방식에서 `position:fixed`로 바꾸고, 열 때마다 버튼 위치·화면 크기를 기준으로 좌우/상하로 안 넘치게 JS로
   좌표를 직접 계산(clamp)하도록 수정(`toggleInfoPop`). 화면 회전/크기 변경 시 열려있던 안내문은 자동으로 닫힘.
3. **안내문 3곳 추가로 접기 패턴 적용**: "실제 일정관리 프로그램 자동 연동은…", 사유서 화면의 "개인식별정보는
   [___]로 두었습니다…", ".hwpx는 한글 프로그램에서 곧바로 열리는…" — 모두 짧은 제목만 보이고 눌러야 자세한
   설명이 뜨도록 변경.
4. **"AI로 초안 다듬기(선택)" 섹션 임시 숨김**: 기능·코드는 그대로 두고 화면에서만 숨김(`display:none`) —
   삭제 아님, 필요할 때 다시 켜면 됨.
5. **변환 중 애니메이션 추가**: 녹음 결과 화면 "음성을 텍스트로 변환하는 중이에요" 문구 옆에 CSS 애니메이션
   (움직이는 점 ·/··/··· + 작은 회전 스피너)을 추가해 처리 중임을 시각적으로 표시.

## v0.41 — 비로그인 상태에서 녹음/입력이 사라지는 문제 (신고: 9/3 사고)

**원인**: `saveCaseProgress()`(서버 자동저장)는 로그인 상태일 때만 동작(v0.17). 현장에서는 대부분
로그인 없이 빠르게 쓰는 것으로 보이고, 그 경우 녹음→변환된 텍스트가 서버에 전혀 저장되지 않고 화면
메모리(DOM)에만 존재 — 새로고침·배포·브라우저 재시작 시 사라짐.

**조치**: 로그인 여부와 무관하게 항상 동작하는 브라우저 로컬 임시저장(localStorage) 추가.

## v0.42 — 실사용 중 확인된 4가지 문제 반영

1. 녹음/파일업로드 후 확인 없이 바로 다음으로 넘어가던 문제 → 1차 수정(이후 v0.43에서 재설계).
2. STT 한국어/영어 혼합 → Edge Function `stt-transcribe`에 `language: ko` 추가, 배포(버전 4) — **이후 v0.45에서
   모델 자체를 whisper-1로 교체하며 근본 해결**.
3. 모바일 글씨 너무 작음 → 가장 작았던 라벨류는 2배, 본문류는 약 1.3배 확대. **(이 규칙이 v0.44·v0.46에서
   총 세 곳의 회귀를 만듦 — 사이드바 안내문, 입력창 유형힌트, 첫 화면)**
4. 가로 스크롤 생기는 문제 → 전역 `box-sizing:border-box` 리셋 추가.

**질문 답변 — 녹음파일 서버 용량(500MB) 괜찮은지**: 원본 음성 파일은 서버에 저장되지 않는 구조입니다.
DB엔 변환된 텍스트만 저장 — 500MB면 수십만 건 이상 저장 가능, 용량 걱정 없습니다.

## v0.43 — 녹음 결과 화면 재설계 + 안내문 접기 도입

카드를 하나만 만들고 상태(녹음중→변환중→결과/빈결과/오류)에 따라 갱신하도록 재설계. "이전"이 "재녹음하기"로
동작. 안내문 접기(물음표 버튼) 컴포넌트 최초 도입, 거주지역 필드에 우선 적용.

## v0.44 — v0.43 후속 다듬기 + 회귀 버그 수정

1. **재녹음/다음 버튼 5:5 + 이름 축약**: 34:66이던 비율을 5:5로, "재녹음하기"/"다음으로"를 "재녹음"/"다음"으로.
2. **안내문 접기 위치 버그 수정**: `.card .field>.lab{display:block}` 규칙 때문에 물음표(?) 버튼이 라벨
   오른쪽이 아니라 아래 줄로 밀려났던 것 수정(라벨 span *안쪽*에 붙이는 방식으로 변경) — 이후 위기사유
   (법령근거·자동정렬 표시), 거주형태·연령대·성별·국적체류자격·특수상황, 예비판단 결과의 지원항목 사용법
   안내, 노숙인 전용 날씨위젯 설명문까지 같은 패턴으로 확대 적용.
3. **사이드바 회귀 버그 수정**: v0.42의 "가장 작은 글씨 2배" 규칙이 사이드바 맨 아래 1회성 안내문
   (`.side-note`)에도 기계적으로 적용돼 줄바꿈이 늘어나며 "최근" 목록 영역을 가리던 문제 — 크기를
   다시 줄임(1.36rem → .86rem).
4. **로컬 임시저장 팝업 제거**: 새로고침마다 뜨던 확인창(confirm)이 실사용에 방해된다는 피드백 →
   팝업 없애고, 로그인 여부와 무관하게 사이드바 "최근" 목록 맨 위에 "💾 임시 저장본(이 기기)" 항목으로
   조용히 노출, 눌렀을 때만 이어서 진행.
5. **RTF 다운로드 삭제**: 버튼과 관련 기능(downloadDoc의 rtf 분기) 전체 제거, hwpx 실패 안내문에서도
   .rtf 언급 제거. 이제 사유서는 hwpx/txt만 지원.

# ⛔ 2026-09-04(오전) — v3 코드 수정 보류 방침 → 오후 긴급 상황으로 일시 해제됨 → 저녁 협업자 설계문서로 재개

오전엔 "협업자 작업 중이니 건드리지 말자"였으나, 오후 현장 실사용 데드라인 때문에 안전패치와 피드백
반영(v0.41~v0.47)을 예외적으로 진행. 저녁에 협업자가 git에 `보안_비밀키_복구_설계.md`를 올렸고(pull은
충돌 없이 깨끗하게 병합됨), 사용자 확인 하에 그 설계의 클라이언트 파트(v0.48)를 바로 진행함 — 위 "🔐"
항목 참고. 최신 커밋들(`147898c` 이후)은 여전히 diff 상세 미확인 상태.

# 핵심기능 목록 (진행 상황)

1. ✅ **상담원 로그인 기능** (Supabase Auth) — 구현 완료.
2. ✅ **DB 저장 연동 + 진행상태 자동 저장** — 구현 완료(로그인 시) + 비로그인 시 로컬 임시저장 안전망(v0.41).
3. ✅ **최근 상담내역 저장/불러오기(이어하기)** — 구현 완료. 로컬 임시저장본도 이제 같은 목록에 노출(v0.44).
4. ✅ **사유서 양식 확정** — 완료(hwpx). RTF는 v0.44에서 삭제, txt는 유지.
5. ⏳ **입력 방식별 양식 연결** — 실사용 테스트로 정확도 검증 필요. **다음 기능 아이디어 참고 —
   녹음 하나로 사유서 초안까지 자동 생성하는 트랙을 추가할 예정.**
6. ⏳ **가명화 기능 방향 최종 결정** — (A) 자동 임베딩 vs (B) 기능 삭제, 아직 미결정. 9/18 전 결정 필요.
7. ⏳ **`anon_tokens` 실제 토큰화 저장 로직** — 6번 결정 후 착수.
8. ⏳ **단계형(페이지 넘김) UI 세부 완성** — 진행바(1/7), 사이드바 개편, 각 단계 내용을 한 화면에 담기
   위한 단계 세분화(콘텐츠 자체를 잘게 나누는 건 아직 안 함), 나머지 안내문 접기 확대 등.
9. 🆕 ⏳ **개인정보 비밀키(제로지식 암호화)** — 클라이언트(v0.48) 완료, 서버 SQL(구흥모님) 실행 대기 중.
   위 "🔐" 항목 참고.

# 버그 현황

## ✅ 수정 완료
- 아이폰 직접 녹음 오류, 이름 저장 안 됨, 새 상담 시작 버튼 사라짐, 완료 버튼 사라짐, 카카오톡 인앱브라우저
  저장 문제, hwpx 이어하기 다운로드 먹통, 단계형 UI 이어하기 빈 화면
- 비로그인 상태 녹음/입력 소실 (v0.41), STT 언어 혼합 1차 완화 (v0.42) → **모델 교체로 근본 해결 (v0.45)**,
  모바일 글씨/가로스크롤 (v0.42)
- 녹음 결과 확인 막다른 상태·재녹음 불가 (v0.43), 안내문 위치 버그·사이드바 회귀 (v0.44)
- 안내문 팝오버 화면 밖 삐져나감, AI초안 다듬기 섹션 숨김, 변환중 애니메이션 없음 (v0.45)
- 첫 화면·입력창 유형힌트가 커진 글씨로 밀리던 문제 (v0.46)
- 재녹음 시 녹음 중 표시가 너무 약함 (v0.47)

## 🐛 상태 재확인 필요
- **PC 크래시·갤럭시 hwpx 생성 실패** — 실기기로 재확인 필요.
- **단계 콘텐츠가 길어 한 화면에 안 들어가는 문제** — 8번 항목 참고, 단계 세분화 설계 필요.
- **STT 화자분리(diarization) 재도입 여부** — whisper-1로 교체하며 당분간 비활성. OpenAI 쪽 language
  강제 문제가 해결되거나 다른 화자분리 방법을 찾으면 재검토.
- **비밀키 암호화 실사용 테스트** — 위 "🔐" 항목 참고, 구흥모님 SQL 실행 후 진행.

# 발표(10/2)·제출(9/18) 준비

`presentation-prep.md`에서 별도 관리 — 현장 변화·문제 해결(배점 70점)에 쓸 실사용 테스트 데이터가 가장
시급. (v0.41~v0.48 — 실사용 중 문제 발견 → 당일 대응 배포 — 자체가 "현장 변화 대응 사례"로 쓸 수 있음.
비밀키 제로지식 암호화도 "개인정보 보호 강화" 근거로 발표에 쓸 수 있음.)

# DB 마이그레이션 기록

`cases` 테이블(2026-09-02, project `ueidoudgwhuanrtowudy`):
```sql
alter table public.cases
  add column status text not null default 'in_progress',
  add column state_json_enc bytea;
alter table public.cases
  add constraint cases_status_check check (status in ('in_progress','completed'));
-- updated_at 자동 갱신 트리거(최근 목록 정렬용)도 함께 추가, SECURITY INVOKER로 안전하게 처리
```

`counselor_keys` 테이블(2026-09-04, `db/2026-09-04_counselor_keys.sql`) — **구흥모님 실행 대기 중**,
상세는 위 "🔐" 항목 및 `보안_비밀키_복구_설계.md` 참고.

# 스토리지 용량 관련

Supabase 무료 티어 DB 용량은 500MB. 원본 음성 녹음 파일은 서버에 전혀 저장되지 않음 — DB에는 변환된
텍스트/JSON만 저장(암호화해도 사례당 몇 KB 수준) — 500MB면 수십만 건 이상 저장 가능.

# 이번 세션(9/2)까지 완료한 것 요약

## STT·AI 백엔드
- Supabase 서울 리전 프로젝트 `ieojim-stt-proxy` 생성, RLS·Vault 암호화·anon_tokens 스키마 완료.
- Edge Functions `stt-transcribe`(OpenAI), `ai-draft`(Gemini) 배포 — API 키 서버에만 보관.
- 클라이언트 STT 실연결 + 가명화 자동연결 + AI초안 구조화 데이터 참고 + 입력창 잠금 반영.

## 로그인·저장·이어하기·사유서 양식
- Supabase Auth 로그인/회원가입, DB 저장 자동화, 사이드바 최근내역 실연결, 상담 이어하기 기능 구현 완료.
- 사유서 hwpx 양식을 협업자가 실제 한글 프로그램에서 만든 것으로 교체 완료.

## 코칭
- 1주차(8/28), 2주차(9/2) 코칭 완료 — `coach-meeting-2026-08-28.md`, `coach-meeting-2026-09-02.md` 참고.
- 3주차(실사용 점검)는 진행 중.

# 참고 — vault 시크릿 생성 관련, 클라우드 네트워크 제한 우회 방법

vault.create_secret() 래퍼 사용 필수, Supabase 직접 호출 불가 시 로컬 PC에서 curl.exe로 대신 호출하는
우회 방식.
